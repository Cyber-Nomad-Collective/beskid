#!/usr/bin/env node
console.error(
	[
		'Not implemented yet.',
		'Planned usage:',
		'  node scripts/convert-architecture-graph-dot.mjs --in <input.dot> --out <output.json>',
		'Converter target format: { title?, description?, groups?, nodes[], edges[] }.',
	].join('\n'),
);
process.exit(2);
