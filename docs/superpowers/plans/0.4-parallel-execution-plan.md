# Beskid 0.4 — Parallel execution plan

> For agentic workers: use `superpowers:subagent-driven-development`. Exclusive file ownership per wave. No HIR/`Lowerable`/Rust-runtime/kit-fallback reintroduction.
>
> Pins at plan time: superrepo `c765ef51`, compiler `ec164ff9`.
> Research basis: Agents [A](9f040367-62e5-417c-bb49-d9ab0dbade91)–[G](676656b6-af9d-47ee-a6b9-a13182603ee0) under `docs/superpowers/plans/2026-07-23-0.4-closure-research/`.

**Goal:** Reach Linux x86-64 compile-and-run on exact ABI-v5 kits, then three-target matrix, retirement, and sign-off — without preserving production fallbacks.

**Architecture:** Production path is already `TypedProgram → CodegenInput → ISLE → stock-verified CLIF`. Critical path is Corelib-surface ISLE/fact gaps → post-JIT SIGILL → Linux kit proof → expand Beskid runtime only as demanded → delete Rust soft builtins → retire HIR.

**Tech stack:** Rust compiler workspace, generated ISLE, ABI-v5 kits, Corelib gate, GitHub Actions.

## Global constraints

- No production HIR, `Lowerable`, legacy codegen success path, Rust runtime/bridge/host for release claim, legacy ABI dispatch, or embedded/nearest-version kit discovery.
- Stock CLIF verification with AST sites where applicable.
- Linear ownership: Codex owns compiler/runtime parents; Cursor only labeled evidence leaves.
- Do not check OpenSpec boxes without fresh commands/results.

---

## Wave 0 — Reproduction and baseline

**Parallel**

| Task | Owner | Scope | Acceptance |
| --- | --- | --- | --- |
| Tip SyscallWrite MissingRuleOrFact repro | codegen | `SyscallWriteTests.bd` first fail | Exact `isle.missing` site logged |
| Tip Syscall.Read Block repro | codegen | `Syscall.bd` Read@91 | Site logged |
| Tip OutputWriteLine SIGILL repro | kit/JIT | `SystemOutputWriteLineTests` + staged kit | Confirm CLIF ok + exit 132 |
| Clippy Rust-gate fix | CI/hygiene | `beskid_analysis` collapsible_if / manual_contains / question_mark | Compiler `Rust gate` green |
| Fix Linux verify test-name filter | kit | `verify-native-runtime-kit-linux.sh` vs `staged_runtime_kit_executes_a_canonical_entrypoint` | Script runs intended smoke |

**Sequential:** none beyond recording pin pair.

**Exclusive ownership:** analysis clippy ≠ isle ≠ engine.

**Checkpoint:** research folder evidence index updated; CYB-137 / new W5.9d / CYB-32 ready.

**Artifacts:** failure sites, SHAs, gate URLs.

---

## Wave 1 — Replacement authorities (residual facts + ISLE)

W1–W4 Linear parents are Done. This wave closes **residual** authorities only.

**Parallel**

| Task | Owner | Files | Issue |
| --- | --- | --- | --- |
| Discard-payload / SyscallWrite match facts+ISLE | semantic + codegen | `beskid_queries` match facts; `beskid_isle` `emit_match*` | CYB-137 → CYB-133 |
| Syscall.Read if/error Block lowering | codegen | ISLE statements/control_flow + facts | leaf under CYB-133 |
| Array / string / `u8[]` ABI facts | semantic | `abi_type_from_syntax` Array arm | feeds RC4 / Syscall |
| Generic call ABI specialization harvest | codegen | specialization in `isle_adapter` / module_emission | CYB-140 / CYB-162 |

**Sequential:** match facts before match emitters if shared symbols.

**Checkpoint:** `SystemSyscallWriteTests` + `SystemSyscallApiTests` generate CLIF without MissingRuleOrFact.

**Acceptance:**
```bash
cargo test -p beskid_queries --tests -- --test-threads=1
cargo test -p beskid_codegen --all-targets
cargo test -p beskid_isle --all-targets
# targeted Corelib:
target/release/beskid_cli test --project corelib_tests.bproj --target SystemSyscallWriteTests --plain
```

---

## Wave 2 — Consumer cutover (execute path)

**Parallel after Wave 1 CLIF green for OutputWriteLine dependency set**

| Task | Owner | Scope | Issue |
| --- | --- | --- | --- |
| Diagnose/fix JIT SIGILL after Finalize | kit/JIT | `beskid_engine` soft builtins vs kit exports; Output→Syscall call ABI | **new W5.9d** |
| Keep harness green; no HIR fallback | codegen | `parsed_project_isle_harness.rs` | regression |
| LSP prepare-spine diagnostics (optional parallel) | LSP | `beskid_lsp` diagnostics off analysis typing | residual W4 debt; not Linux-critical |

**Checkpoint:** `output_writeline_smoke` executes on staged Linux kit without SIGILL.

---

## Wave 3 — Canonical runtime (demand-driven)

**Do not expand full GC/scheduler before Linux smoke executes.**

**Sequential**

1. CYB-32 Linux empty-prefix JIT+AOT smoke using current Bootstrap kit (after Wave 2).
2. CYB-28 acceptance: lifecycle/TLS/trap capability tests; document remaining soft builtins as debt (do not mark Done while Engine registers Rust).
3. Expand Bootstrap only as smoke demands (alloc/roots already present).

**Parallel after Linux cell green**

| Task | Owner | Issue |
| --- | --- | --- |
| Mark/sweep GC + barriers | runtime memory | CYB-29 |
| Strings/collections | runtime collections | CYB-30 |
| Scheduler (replace fiber stub) | runtime scheduler | CYB-31 |
| Latent ANSI/string handle cluster | codegen+runtime | CYB-134/156/158 (after gate reaches them) |

**Checkpoint:** kit exports absorb symbols currently process-linked; soft-builtin list shrinks.

---

## Wave 4 — Target/profile kit matrix

**Sequential:** Linux (CYB-32) → macOS (CYB-33) → Windows (CYB-34).

**Parallel within a target:** debug/release static/shared (+ Windows import lib).

**Exclusive:** kit scripts / AOT publish vs Engine soft-builtin deletion (deletion waits for matrix cells that still need soft builtins — prefer migrate symbols into kit first).

**Acceptance:** `stage-native-runtime-kit-matrix.sh`; hosted Windows job; macOS kit evidence (may need new CI job — Agent G).

**Also:** wire AOT PreferDynamic to kit shared if release requires shared AOT (Agent D gap).

---

## Wave 5 — Legacy retirement

**Strict order (Agent F):**

1. Engine soft-builtins cutover (CYB-85/86) — after kits supply symbols.
2. Delete Rust runtime/bridge/host/handlers.
3. Delete public `Lowerable` / legacy API (CYB-36).
4. Delete HIR model (CYB-35/84) after materializer/docs callers moved.
5. Obsolete deps (CYB-67).
6. Authoritative gates (CYB-39): refine scan categories; wire into CI; real artifact provenance (not fixture-only).

**Do not start CYB-35/36** while Engine still process-links Rust.

---

## Wave 6 — Verification and release sign-off

| Task | Issue |
| --- | --- |
| Fresh workspace tests | CYB-40 |
| Corelib + installed-prefix + packages | CYB-41 |
| Docs/OpenSpec reconcile (checkboxes ↔ evidence) | CYB-42 Done historically — refresh for tip |
| GitNexus changed-scope + whole-branch | CYB-43 |
| Evidence bundle + sign-off | CYB-44 |

Platform/trudoc/Coolify reds are **orthogonal** — track separately; do not block language proof.

---

## Agent assignment plan

| Agent | Directory ownership | First issue | Concurrent OK | Must not touch | Handoff |
| --- | --- | --- | --- | --- | --- |
| codegen/ISLE | `beskid_codegen`, `beskid_isle` | CYB-137 | CYB-140 after match | `beskid_engine` soft builtins; runtime `.bd` | CLIF green for Syscall/Output |
| semantic facts | `beskid_queries` (+ analysis indexes if needed) | array/string ABI + match facts | stale-gen regressions | Engine/AOT | Facts for ISLE |
| LSP | `beskid_lsp` | prepare-spine diagnostics | after facts | codegen ISLE | no-analysis diags |
| runtime memory/GC | `runtime/beskid`, GC portions | CYB-29 after Linux smoke | CYB-28 tests | Engine deletion | kit symbols |
| runtime scheduler/collections | same crate, different modules | CYB-30/31 | after CYB-29 as needed | HIR deletion | capability tests |
| kit/AOT/JIT | `beskid_aot`, `beskid_engine`, `beskid_abi` kit | W5.9d + CYB-32 | verify script fix | HIR tree | empty-prefix proof |
| CI/evidence | `.github`, `scripts/ci` | clippy unlock; artifact retention | trudoc lockfile (orthogonal) | compiler semantics | green gates |
| retirement/provenance | scan scripts, then deletion PRs | CYB-39 design → CYB-85 | after W5 | early HIR delete | zero-violation CI |

---

## Integration checkpoints

1. After Wave 1: Syscall suites CLIF-clean.
2. After Wave 2: OutputWriteLine executes.
3. After Wave 3/Linux: CYB-32 Done with hashes.
4. After Wave 4: three-target matrix evidence.
5. After Wave 5: retirement scan CI green.
6. After Wave 6: CYB-44 sign-off.
