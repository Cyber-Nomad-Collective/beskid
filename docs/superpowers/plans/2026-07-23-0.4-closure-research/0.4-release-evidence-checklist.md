# Beskid 0.4 — Release evidence checklist

> Language release proof first (Linux JIT+AOT on exact ABI-v5 kit). Packaging/Coolify secondary.
> Tip candidates: superrepo `c765ef51`, compiler `ec164ff9` — replace when green.

## A. Pin identity

- [ ] Superrepo SHA recorded
- [ ] Compiler submodule SHA recorded (`git submodule status compiler`)
- [ ] Corelib / nested pins coherent with CI `setup-compiler-submodule`
- [ ] Dirty local submodules disclosed (do not mix with CI pins)

## B. Compiler gate (hosted)

- [ ] Workflow Compiler green on tip — run URL
- [ ] `Rust gate` green (`scripts/ci/compiler-rust-gate.sh` / clippy `-D warnings`)
- [ ] LSP command contract gate green
- [ ] Artifact: compiler SHA from `Resolve release version`

**Tip blocker:** [29977866956](https://github.com/Cyber-Nomad-Collective/beskid/actions/runs/29977866956) clippy in `beskid_analysis`.

## C. Linux compile-and-run proof (CYB-32) — definition of language readiness

On one coherent pin pair:

- [ ] Empty prefix created
- [ ] Exact kit staged: `lib/beskid-runtime/abi-5/x86_64-unknown-linux-gnu/{debug,release}/` with `abi.json` + static + shared
- [ ] Provenance verify on static + shared symbol lists
- [ ] Real project (prefer `SystemOutputWriteLineTests` / hello via Core.Output) compiles `TypedProgram → CodegenInput → ISLE → stock-verified CLIF` with no HIR/`Lowerable` construction
- [ ] JIT Engine smoke succeeds under `BESKID_RUNTIME_PREFIX` + profile
- [ ] AOT linked executable smoke succeeds (static kit)
- [ ] Fail-closed: mismatched ABI/version/hash/profile/form rejected
- [ ] Commands, logs, artifact hashes attached to CYB-32 and linked from CYB-41

**Tip blocker:** Corelib [29977866969](https://github.com/Cyber-Nomad-Collective/beskid/actions/runs/29977866969) exit 132 after Finalize JIT; also MissingRuleOrFact on Syscall suites earlier in gate.

**Script fix required:** `verify-native-runtime-kit-linux.sh` must filter `staged_runtime_kit_executes_a_canonical_entrypoint` (not `staged_linux_…`).

## D. Corelib verification (CYB-41)

- [ ] `CORELIB_REPORT_DIR=… bash scripts/ci/corelib-gate.sh` green on tip
- [ ] Report artifact archived beyond 14-day retention
- [ ] RC1 (Syscall ISLE) and RC2 (SIGILL) issues Done or explicitly waived with owner sign-off (no silent waiver)

## E. Three-target kit matrix

- [ ] Linux debug/release static/shared (CYB-32)
- [ ] macOS arm64 debug/release static/shared + JIT/AOT smokes (CYB-33) — may need new hosted kit job beyond CLI-only builds
- [ ] Windows x86_64 debug/release static/shared + import libs (CYB-34) — Compiler `Windows ABI-v5 runtime-kit matrix` job
- [ ] Kit hashes + allowlist/export audits per cell

## F. Retirement / provenance (W6 → OpenSpec 1.4 / 4.* / 6.10.2)

- [ ] `bash compiler/scripts/verify-hir-free-abi-v5.sh` zero unapproved violations
- [ ] Scan categories refined (test-support vs legacy-implementation) — CYB-39
- [ ] Gate wired into `.github/workflows` Compiler/Corelib/kit jobs
- [ ] Per-artifact provenance on real `.a`/`.so`/`.dylib`/EXE/REPL/CLI (not fixture-only)
- [ ] Engine has **no** `process_linked_soft_builtins` / `interop_dispatch_*` for release claim
- [ ] HIR / `Lowerable` / Rust runtime crates removed or quarantined per deletion graph

## G. Full workspace and packages

- [ ] `cargo test --workspace --all-targets` fresh green (CYB-40)
- [ ] Focused suites: queries, isle, codegen, aot, engine, lsp, abi
- [ ] Package-content / distribution checks as required by CYB-41/Distribute
- [ ] CLI/LSP/bundle three-OS publish on tip (historical green [29917413684](https://github.com/Cyber-Nomad-Collective/beskid/actions/runs/29917413684) is **not** tip)

## H. Documentation / OpenSpec

- [ ] OpenSpec tasks checked only with evidence links
- [ ] Reconcile Linear Done vs unchecked OpenSpec `2.3`, `2.6`, `2.7`, `3.*`, `4.*`, `5.*`, `6.*`
- [ ] CHANGELOG / GLOSSARY / GUIDE updated
- [ ] `isle-lowering-coverage.md` refreshed (Spawn/Lambda drift)

## I. GitNexus / review

- [ ] `detect_changes` / changed-scope analysis recorded (CYB-43)
- [ ] Whole-branch review: no unresolved HIGH/CRITICAL release risks

## J. Orthogonal (must not block language proof; track separately)

- [ ] Tracker trudoc GH Packages registry + Bun lockfile
- [ ] Platform delivery Coolify / production approval queue
- [ ] GHCR sibling `beskid-pckg` Write grants
- [ ] Distribute / Open VSX (cascade after Compiler green)

## K. Final sign-off (CYB-44)

- [ ] All parents CYB-9/10/11 Done with children Done or Duplicate
- [ ] Evidence bundle: SHAs, commands, CI URLs, kit hashes, provenance logs, review outcomes
- [ ] Explicit statement: HIR-free production path; exact ABI-v5 kits only; three-target matrix green; retirement scan green
- [ ] No compatibility fallbacks remain in production

## Definition of Done (task-level)

A task is complete only when:

- implementation + regression tests land together
- scoped tests freshly green
- stock CLIF verification used
- no fallback introduced
- diagnostics preserve source spans where applicable
- issue records exact commands/results
- cross-target claims have hosted evidence
- OpenSpec/docs consistent
