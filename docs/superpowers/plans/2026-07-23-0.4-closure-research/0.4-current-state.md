# Beskid 0.4 — Current-state verdict

> Evidence date: 2026-07-23. Pins: superrepo `c765ef51`, compiler `ec164ff9`.
> Supporting artifacts: `baseline-pins.md`, `linear-issues.tsv`, `corelib-build-report-29977866969.md`.
> Prior plan `docs/superpowers/plans/2026-07-14-0.4-release-closure.md` is **stale** against Linear (W1–W4 parents Done).

## Verdict table

| Question | Verdict | Evidence | Confidence |
| --- | --- | --- | --- |
| Does the compiler workspace build? | **No on CI main.** Local tree may compile, but Compiler gate fails clippy-as-error. | Actions run [29977866956](https://github.com/Cyber-Nomad-Collective/beskid/actions/runs/29977866956): Rust gate fails in `beskid_analysis` (`clippy::collapsible_if`, `manual_contains`, `question_mark`). Downstream CLI/LSP/kit publish jobs skipped. | Confirmed |
| Does a real Beskid project compile through the intended HIR-free path? | **Partially.** Fixture/harness projects do; Corelib/real I/O projects do not yet. | `compiler/crates/beskid_codegen/tests/parsed_project_isle_harness.rs` asserts production `CodegenInput`/ISLE route and rejects public `Lowerable`. Linear CYB-5/12/15 Done. Corelib gate still fails on syntax ISLE emission and post-JIT abort. | Confirmed (harness) / Confirmed failing (Corelib) |
| Do canonical runtime sources compile through that path? | **Bootstrap only — incomplete corpus.** | `compiler/runtime/beskid/` contains `beskid-runtime-native.bproj` + `src/Runtime/Bootstrap.bd`. Corelib CI traces show Bootstrap items selecting ISLE rules (`AllocationSize`, etc.) before later test failures. CYB-28/29/30/31 still open. | Confirmed thin corpus; Bootstrap ISLE path implemented but unverified as sole production runtime |
| Do Linux JIT and AOT work from a clean exact-prefix kit? | **Not release-proven.** Kit staging succeeds; execution aborts. | Corelib report: stage native runtime kit PASS (3 files, 96K); tests FAIL exit 132. Log: `Generate CLIF` + `Finalize JIT module` then `Illegal instruction` / `timeout: the monitored command dumped core` on OutputWriteLine path. AOT exact-kit API exists (`beskid_aot/src/bundled.rs`, `RuntimeKitRequest`). | Confirmed staging; confirmed JIT abort; AOT clean-prefix matrix not hosted-green |
| Are macOS and Windows complete? | **No.** | Linear CYB-33/34 In Progress; Windows ABI-v5 matrix job skipped when Compiler gate fails; no fresh green three-target evidence on this tip. | Confirmed incomplete |
| What blocks the release? | **Both implementation and evidence.** | Product: ISLE/fact gaps on Corelib Syscall/Output, thin Beskid runtime, live Rust `interop_dispatch_*` in Engine, retirement scan red (3402 active production hits). Evidence: clippy gate red, Corelib red, three-target matrix not retained, platform/tracker packaging red (orthogonal). | Confirmed |

## Pipeline status (intended production path)

```text
expanded syntax
→ generation-safe Salsa facts     [Linear W2 Done; residual Corelib typing/layout bugs remain under W5.9*]
→ TypedProgram                    [present; beskid_queries TypedProgram]
→ CodegenInput                    [present; beskid_codegen codegen_input.rs]
→ generated ISLE rules            [partial; MissingRuleOrFact still hits TestDefinition/Block/Syscall]
→ stock-verified CLIF             [harness-proven; Corelib not]
→ exact validated ABI-v5 kit      [API present; Engine still registers Rust interop_dispatch]
→ JIT or AOT executable           [JIT reaches finalize then SIGILL on OutputWriteLine smoke]
```

## Linear wave status (authoritative for backlog, not for OpenSpec checkboxes)

| Wave | Parent | Status | Implication |
| --- | --- | --- | --- |
| W1 compiler spine | CYB-5 | Done | Spine landed; residual Corelib lowering bugs tracked under W5.9* |
| W2 semantic facts | CYB-6 | Done | Authority exists; not every Corelib surface is covered |
| W3 exact kits | CYB-7 | Done | Route claimed; fallback deletion still W6 |
| W4 consumer migration | CYB-8 | Done | Remaining live `Lowerable`/HIR code is retirement debt |
| W5 runtime + matrix | CYB-9 | In Progress | **Critical path** |
| W6 retirement | CYB-10 | In Progress | Blocked on W5 consumers + Engine/runtime deletion |
| W7 sign-off | CYB-11 | In Progress | Blocked on green gates + evidence |

## OpenSpec task drift (must reconcile during W7)

Unchecked items that still match repository reality:

- `1.4` retired-pattern/provenance scans green
- `2.3` exhaustive ISLE inventory/compiler
- `2.6` / `2.7` canonical runtime + kit discovery for all targets/profiles
- `3.*` consumer migration completeness vs residual live legacy
- `4.*` deletion
- `5.*` / `6.*` verification waves

Checked items that Linear treats as Done (`2.1`, `2.2`, `2.4`, `6.1`, `6.2`, …) must not be re-opened without fresh counter-evidence; instead file residual bugs under W5/W6.

## Misleading CI (not product blockers)

| Failure | Run | Class |
| --- | --- | --- |
| `@cyber-nomad-collective/trudoc` 404 / frozen lockfile | Tracker [29977866963](https://github.com/Cyber-Nomad-Collective/beskid/actions/runs/29977866963) | Platform packaging / registry |
| platform-spec lockfile had changes | same | Bun lockfile drift |
| Distribute skipped | [29977914167](https://github.com/Cyber-Nomad-Collective/beskid/actions/runs/29977914167) | Dependent on green upstreams |
| Compiler clippy | [29977866956](https://github.com/Cyber-Nomad-Collective/beskid/actions/runs/29977866956) | Gate hygiene — blocks evidence collection, not HIR-free architecture itself |

## Agent consensus (Auto research A–G)

| Claim | Agents | Resolved as |
| --- | --- | --- |
| Production spine exists | A, D | Confirmed; harness CYB-15 Done |
| Kit discovery fail-closed | D | Confirmed; soft builtins still live in Engine |
| Bootstrap-only Beskid corpus | C, F | Confirmed; GC/strings/scheduler Rust-live |
| Tip Corelib = ISLE gaps + SIGILL | E, G | Confirmed; ANSI latent |
| W2 facts mostly complete | B | Confirmed; array ABI hole + LSP prepare-spine residual |
| Retirement scan red / not CI-wired | F, G | Confirmed; delete only after W5 |
| Prior July-14 plan stale | all | W1–W4 Done; critical path is W5 execute |

## Bottom line

beskid 0.4 is **not release-ready**. The intended HIR-free spine exists and focused harnesses prove it for curated sources. Release is blocked by (1) Corelib/real-program ISLE and post-JIT execution failures, (2) incomplete Beskid-source runtime corpus, (3) live Rust runtime/dispatch linkage, (4) unfinished three-target kit matrix, and (5) red retirement/sign-off evidence.
