# Beskid 0.4 — Root-cause map

> Do not treat hundreds of Corelib failures as independent bugs. Cluster by underlying cause.
> Fresh tip evidence: Corelib run 29977866969 + retirement scan on compiler `ec164ff9`.

## RC1 — Incomplete syntax-ISLE coverage for Corelib item shapes

| Field | Value |
| --- | --- |
| Symptom | `Lowering(MissingRuleOrFact … TestDefinition@…)` and `… Block@…` during `Generate CLIF` for SyscallWrite/Ergonomics/Api tests |
| Subsystem | `beskid_isle` item/statement selection + `beskid_codegen` `SyntaxNodeFacts` |
| Minimal reproduction | Corelib `SystemSyscallWriteTests` / `syscall_write_empty_string_returns_non_negative` (first FAIL in gate log) |
| Estimated blast radius | All Corelib tests whose executable item body is a `TestDefinition` or whose foundation helpers use currently unmatched `Block`/match/syscall shapes; dozens of tests collapse to this family |
| Owning issues | CYB-132 (In Progress), CYB-133 (In Progress), leaves CYB-137 (Backlog), CYB-140/162 (generic ABI specialization), related W5.9b.* |
| Blockers | Generation-safe facts for generic enum layouts / match / imports (partially Done under CYB-136/142) |
| Dependants | CYB-41 Corelib verification; any Linux compile-and-run proof using Core.Output/Syscall |

**Status:** confirmed missing for specific node families on tip; inventory exists (CYB-13 Done) but bijective executable coverage for Corelib surfaces is incomplete (OpenSpec `2.3` unchecked).

**Agent E refinement ([agent-e-corelib.md](./agent-e-corelib.md)):** `TestDefinition` is not universally missing — OutputWriteLine’s TestDefinition selects successfully. Tip SyscallWrite/Ergonomics failures are item-root failures overlapping discard/binding matches (CYB-137). ApiTests fail on `Syscall.Read` Block@91. Recommend **new W5.9d** for SIGILL (do not fold into CYB-134). CYB-132 description is stale vs tip (now ISLE, not semantic generics). Latent ANSI/CYB-134/140 never reached this run because SIGILL kills the gate.

## RC2 — Post-JIT native abort on OutputWriteLine path (SIGILL / exit 132)

| Field | Value |
| --- | --- |
| Symptom | CLIF generation reports success for OutputWriteLine smoke (`clif.end outcome=ok`), JIT finalizes, process dies with Illegal instruction / core dump; gate exit 132 |
| Subsystem | JIT attachment + ABI-v5 kit symbols + possibly incorrect call/ABI for Output/Syscall facades; may also involve remaining Rust `interop_dispatch_*` |
| Minimal reproduction | Corelib report path ending at `output_writeline_smoke` after `Finalize JIT module` (see `corelib-build-report-29977866969.md`) |
| Estimated blast radius | Any Corelib/runtime smoke that reaches JIT execute after generating CLIF; blocks CYB-32 Linux proof |
| Owning issues | **Needs new W5.9d under CYB-9** (Agent E); not CYB-134 (allocation overflow). Engine soft-builtins residual (Agent D/F). |
| Blockers | Attribute illegal instruction; exact kit exports vs process-linked soft builtins |
| Dependants | CYB-32/33/34 matrix; CYB-41; release sign-off |

**Status:** confirmed failing on hosted Linux gate.

**Agent D refinement ([agent-d-abi-kits.md](./agent-d-abi-kits.md)):** kit discovery is already fail-closed (no nearest/search-path). JIT loads **shared** kit then still registers live Rust soft builtins; AOT links **static** kit only. Linux verify script filters wrong test name (`staged_linux_…` vs `staged_runtime_kit_…`).

## RC3 — Canonical runtime corpus still Bootstrap-thin; Rust runtime remains live

| Field | Value |
| --- | --- |
| Symptom | Retirement scan: `active production=3402`, `deprecated fallback=40`, `retired dependencies=16`; Engine registers `beskid_runtime::interop_dispatch_*` |
| Subsystem | `compiler/runtime/beskid` (only `Bootstrap.bd`), `beskid_runtime`, `beskid_runtime_bridge`, `beskid_host`, `beskid_engine` |
| Minimal reproduction | `bash compiler/scripts/verify-hir-free-abi-v5.sh` → non-zero; inspect `beskid_engine/src/jit_module.rs` dispatch symbol registration |
| Estimated blast radius | Entire W6 deletion graph; provenance gates; claim “canonical Beskid runtime only” |
| Owning issues | CYB-28/29/30/31 (Todo/Backlog), CYB-85/86, CYB-35/36/39/67, CYB-84 |
| Blockers | W5 capability implementation + consumer cutover off Rust objects |
| Dependants | CYB-10 parent closure; OpenSpec `4.*` |

**Status:** confirmed incomplete Beskid corpus; confirmed live Rust fallback symbols.

**Agent F refinement ([agent-f-legacy-retirement.md](./agent-f-legacy-retirement.md)):** only ~14 deprecated-fallback hits are true production (`interop_dispatch_*` in Engine + `beskid_runtime` + manifest string). HIR/`Lowerable` raw counts inflate burn-down. Provenance gate is fixture-only and **not wired** into `.github/workflows`. Deletion order must be Engine soft-builtins → Rust crates → Lowerable → HIR → obsolete deps → authoritative CI gates. Do not start CYB-35/36 while Engine still process-links Rust.

## RC4 — Generic / enum / aggregate ABI specialization gaps for reachable Corelib helpers

| Field | Value |
| --- | --- |
| Symptom | Historical and ongoing: `Verification(generic item has no call-derived ABI specialization)`; field projections on event-bearing aggregates; string handle corruption / huge allocation sizes |
| Subsystem | `beskid_codegen` specialization harvest + `beskid_queries` generic enum layout facts + aggregate layout emission |
| Minimal reproduction | CYB-140 cites `ConsoleControlsProgressBarTests.progress_bar_clamps_percent`; CYB-134 cites `ansi_csi_bold_red` allocation overflow |
| Estimated blast radius | Console/ANSI/collections Corelib modules; managed aggregates |
| Owning issues | CYB-140, CYB-162, CYB-134, CYB-156/158, CYB-29/30 |
| Blockers | Concrete generic enum layouts (CYB-136 Done) must be consumed by specialization harvest |
| Dependants | Console Corelib green; GC-backed string/collection smokes |

**Status:** partially implemented; residual Backlog/Todo leaves remain.

## RC5 — Legacy HIR/`Lowerable` still present as internal/test surface

| Field | Value |
| --- | --- |
| Symptom | Large `crates/beskid_codegen/src/lowering/**` tree still implements `Lowerable`; public re-export rejected by harness, but deletion not done |
| Subsystem | codegen lowering + analysis HIR |
| Minimal reproduction | `rg Lowerable compiler/crates/beskid_codegen`; harness asserts `Lowerable` not publicly re-exported |
| Estimated blast radius | W6.1/W6.2; false sense of “HIR-free” while sources remain |
| Owning issues | CYB-35, CYB-36, CYB-84 |
| Blockers | Prove zero production callers (Engine/AOT/CLI already on syntax entrypoints) |
| Dependants | Retirement scan green; OpenSpec `4.1`/`4.2` |

**Status:** production entrypoints migrated (Linear W4 Done); deletion pending.

## RC6 — Release evidence and CI plumbing noise

| Field | Value |
| --- | --- |
| Symptom | Clippy gate red; tracker/platform bun install red; distribute skipped; matrix jobs skipped |
| Subsystem | `.github/workflows/compiler.yml` Rust gate; tracker-platform-delivery; package registry |
| Minimal reproduction | Runs 29977866956 (clippy), 29977866963 (trudoc 404 / lockfile) |
| Estimated blast radius | Blocks artifact/publish evidence even when product path is being fixed; confuses burn-down |
| Owning issues | CYB-40/41/43/44; Cursor CI parity historically CYB-130 (Done); need fresh packaging issues only if absent |
| Blockers | Product RCs for Corelib/matrix; packaging for site installs |
| Dependants | Sign-off |

**Status:** confirmed; separate product vs plumbing when reporting.

## Secondary / historical clusters (do not reopen as primary)

| Cluster | Notes |
| --- | --- |
| Mixed project-session prepared-syntax | Tracked historically; W2/W4 Done — reopen only with fresh failing fixture |
| `unknown value type` / `expected string, got u8[]` | Likely subsumed by RC4 typing/layout; map to CYB-140/string issues before new tickets |
| `Contracts.bd` parse | Treat as parser/fixture only if still failing on tip; not seen in latest Corelib report excerpt |
| Broad retirement `DISPATCH_` false positives | Scan refinement is CYB-39; do not use raw count as burn-down |

## Root-cause → critical path mapping

```text
RC1 + RC4  → unblock Corelib CLIF generation
RC2        → unblock Linux JIT execute proof
RC3        → unblock retirement + provenance
RC5        → deletion after RC3 consumers move
RC6        → continuous evidence; clippy fix can parallel early
```
