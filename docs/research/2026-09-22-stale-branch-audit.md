# Stale-branch audit — conflicting origin branches vs. current origin/main

Date: 2026-09-22. Read-only analysis; no merges, pushes, or resets were performed.

Scope: the 28 origin branches marked `CONFLICT` (merge-tree conflicts against `origin/main`)
across root and submodules. Each branch is classified SUPERSEDED (intent already in main, cite
where), PARTIAL (some pieces missing), RELEVANT (meaningful work missing), or OBSOLETE (targets
infrastructure/architecture main has replaced).

**Headline result: none of the 28 branches carry work that is still missing from main.**
23 are SUPERSEDED (same intent landed, usually via a rewritten/rebased commit with a different
hash) and 5 are OBSOLETE (they target CI providers, crates, or repo layouts main has since
replaced: AppVeyor, GitHub Actions publish pipelines, Terraform-based deploy, the pre-submodule
`compiler/corelib`, and the Rust `beskid_runtime` crate).

| Repo | Branch | Unique commits | Last commit | Class | What it contains | Evidence (main) | Impact | Recommended action |
|---|---|---|---|---|---|---|---|---|
| root | codex/appveyor-macos-cross-validation | 6 | 12d ago | OBSOLETE | AppVeyor macOS cross-validation gate, Rust toolchain bootstrap for AppVeyor workers, ripgrep install | `appveyor.yml` does not exist on main; `.woodpecker/{linux,macos,windows}.yml` + `deploy/woodpecker/compose.yml` + `docs/operations/woodpecker.md` are the sole CI/build path (matches AGENTS.md "Woodpecker runs Linux validation and native ... builds"). Woodpecker's native workers are pre-provisioned persistent local runners, not fresh per-build VMs, so the ripgrep/toolchain-bootstrap problem this branch solves does not exist under the new model. | LOW | Delete branch |
| root | codex/appveyor-rustup-bootstrap | 1 | 12d ago | OBSOLETE | Same AppVeyor Rust bootstrap fix, subset of the branch above | Same as above | LOW | Delete branch |
| root | codex/corelib-release-gate-followup | 1 | 13d ago | SUPERSEDED | Bumps `compiler` submodule pointer + CHANGELOG line for "restore corelib release authority" | Target compiler commit is an ancestor of main's current compiler pointer `1bc4ec21` (`git merge-base --is-ancestor` = YES) | LOW | Delete branch |
| root | codex/editor-release-gate-followup | 1 | 13d ago | SUPERSEDED | Bumps `compiler` submodule pointer for "advance compiler release gate pin" | Target compiler commit `c3e2aa30` is an ancestor of main's compiler pointer | LOW | Delete branch |
| root | codex/fix-pairing-all-services | 2 | 5w ago | SUPERSEDED | Bumps `beskid_web_common` pointer to re-export `AuthHubPairing*` types as type-only, adding `AuthHubPairingApprovalResult` | `beskid_web_common` commit `0173c05c` (exact same diff) is not an ancestor of `beskid_web_common` main, but main's `packages/beskid-auth-client/src/index.ts` already exports the same type-only set including `AuthHubPairingApprovalResult`, just reorganized alongside other exports | LOW | Delete branch |
| root | codex/fix-rust-gate-605 | 1 | 6w ago | SUPERSEDED | Bumps compiler/distrib/web_common pointers for "advance Windows executable entrypoint" | All three target submodule commits are ancestors of main's current pointers | LOW | Delete branch |
| root | codex/linux-fiber-cet-pin | 3 | 12d ago | SUPERSEDED | Bumps compiler pointer for Linux fiber shadow-stack / CET / context-switch ABI fixes | Target compiler commit `59d9c373` is an ancestor of main's compiler pointer | LOW | Delete branch |
| root | codex/manual-release-0.4.744 | 2 | 12d ago | SUPERSEDED | Bumps `beskid_distrib` (WiX x64 installer fix) and `compiler` (macOS `dyld_stub_binder` runtime-provenance audit) pointers | `beskid_distrib` commit `f99bb1c` content (WiX v4, per-machine scope, UTF-8, `-arch x64`) is identical to main's `41b4548 fix Windows WiX x64 installer build`; compiler commit `95449279`'s `DARWIN_MACHO_SHARED_TOOLCHAIN_IMPORTS = ["dyld_stub_binder"]` is present verbatim in main's `crates/beskid_abi/src/runtime_provenance.rs` | LOW | Delete branch |
| root | codex/vscode-lsp-release-followup | 16 | 13d ago | SUPERSEDED | Mixed: compiler pin (Linux fiber fix, scheduler/runtime fixes), zed BSOL grammar packaging + `zed-extension-package.test.sh` bsol.wasm assertions, GLOSSARY additions, `openspec/catalog.json` refresh, and a `.github/workflows/platform-delivery.yml` "checkout beskid_vscode before editor release gate" fix + new `platform-integration-checkout-contract.test.sh` | Compiler pointer `edf39dab` is a main ancestor; `editors/zed/grammars/bsol.wasm` exists on main and `scripts/ci/test/zed-extension-package.test.sh` already asserts `grammars/bsol.wasm` presence/non-empty. The GH-Actions-specific piece is moot: `.github/workflows/platform-delivery.yml` does not exist on main at all (GitHub Actions is now "limited to editor-marketplace and repository-native maintenance work" per AGENTS.md; Woodpecker replaced it), so `platform-integration-checkout-contract.test.sh` (which greps that exact file) targets a removed pipeline | LOW | Delete branch |
| root | codex/woodpecker-openspec | 3 | 12d ago | SUPERSEDED | Removes `openspec/specs/staged-delivery-observability` spec and touches several other specs' `## Implementation` wording alongside the same distrib/compiler pins as manual-release-0.4.744 | `openspec/specs/staged-delivery-observability` does not exist on main (already removed) | LOW | Delete branch |
| root | codex/woodpecker-pipeline | 3 | 12d ago | SUPERSEDED | Adds `.woodpecker/{linux,macos,windows}.yml`, `deploy/woodpecker/compose.yml`, `docs/operations/woodpecker.md`, `scripts/ci/woodpecker-build-platform.sh` — i.e. proposes the whole Woodpecker pipeline | All of these files exist on main today, with `docs/operations/woodpecker.md` matching the branch's own wording almost verbatim ("Woodpecker ... validates Linux pushes and can build the three native release targets, but it does not publish a release...") | LOW | Delete branch |
| root | codex/woodpecker-provider-purge | 3 | 12d ago | SUPERSEDED | Removes appveyor/coolify-CI references from docs, `scripts/local-preflight*.sh`, `validate-ci-local.sh` (drops `security-policy-gate.sh` call), deletes `tooling-04-appveyor-watchtower-boundary.md`, updates `docs-coverage.ts` sourceBoundary from `appveyor.yml` to `.woodpecker` | `validate-ci-local.sh` on main already has no `security-policy-gate.sh` call; `docs-coverage.ts` line 37 already points to `.../tree/main/.woodpecker`; `tooling-04-appveyor-watchtower-boundary.md` does not exist on main; no AppVeyor/Coolify-CI mentions remain in `scripts/local-preflight*.sh` | LOW | Delete branch |
| beskid_bsol | codex/complete-tree-sitter-bsol | 18 | 2w ago | SUPERSEDED | Full BSOL tree-sitter grammar workspace bootstrap through "fix: complete BSOL editor grammar" | `git diff origin/main origin/codex/complete-tree-sitter-bsol` is empty — **trees are byte-identical** (`1dacbc63...`). Main's tip is literally `Merge pull request #2 from .../codex/complete-tree-sitter-bsol`; the branch was already merged, just under a different (rebased) commit graph so merge-tree sees no common ancestor | LOW | Delete branch |
| beskid_distrib | codex/branded-macos-dmg | 1 | 4d ago | SUPERSEDED | Brands the macOS DMG (background image, Applications-folder symlink) via a live `hdiutil attach` + `osascript` Finder-window scripting pass | Main's `macos/build-dmg.sh` already brands the DMG, but via `dmgbuild` (python) instead of live Finder AppleEvents, with an explicit comment explaining why: "dmgbuild writes Finder's .DS_Store directly... AppleEvents to the desktop Finder consistently time out" — a strictly more robust fix for the exact same problem | LOW | Delete branch |
| beskid_distrib | codex/wix-x64-release-fix | 1 | 12d ago | SUPERSEDED | Same `f99bb1c` WiX x64 fix as root's manual-release-0.4.744 pointer | Identical commit content lands on main as `41b4548 fix Windows WiX x64 installer build` | LOW | Delete branch |
| beskid_distrib | cursor/cyb-w5-runtime-04 | 1 | 9w ago | SUPERSEDED | `scripts/resolve-version.sh`: fail-closed regex check that `cli-latest`'s version matches `^0\.4\.(0\|[1-9][0-9]*)$` | Main's `resolve-version.sh` was rewritten with a proper `validate_distribution_version` helper (sourced from `version.sh`), supporting both `cli-stable`/`cli-unstable` rolling tags with a stable fallback — same "fail closed" intent, more complete implementation | LOW | Delete branch |
| beskid_nexus | fix/auth-pairing-repair-token | 1 | 2w ago | SUPERSEDED | "feat: replace auth hub with authentik proxy identity" — removes OAuth hub-handoff/github-ownership flow, adds `authentik-identity.ts` | Main has `gitnexus/src/server/nexus/authentik-identity.ts` and no `hub-handoff.ts`/`github-ownership.ts`, landed via a same-subject commit `62fa0a0 feat: replace auth hub with authentik proxy identity` (matches AGENTS.md: pckg/nexus resolve users from Authentik forward-auth headers) | LOW | Delete branch |
| beskid_tracker | cursor/cyb-130-docker-tsconfig | 1 | 9w ago | SUPERSEDED | Orphan-root commit ("fix(docker): copy web_common tsconfig.base.json into image") on top of an old 678-file tracker snapshot | Main's `Dockerfile` already resolves this via a named BuildKit context (`--build-context web_common=../beskid_web_common`) plus `COPY --from=web_common . /app/beskid_web_common` and a `pnpm install --dir /app/beskid_web_common`, matching AGENTS.md's documented fact about `file:` `@beskid/*` resolution | LOW | Delete branch |
| beskid_tracker | cursor/cyb-130-tracker-file-deps | 1 | 9w ago | SUPERSEDED | Same orphan snapshot, "build tracker image from superrepo for file: UI deps" | Same Dockerfile evidence as above | LOW | Delete branch |
| beskid_tracker | cursor/cyb-130-tracker-root-docker | 1 | 9w ago | SUPERSEDED | Same orphan snapshot, "build tracker image from superrepo root context" | Same Dockerfile evidence as above | LOW | Delete branch |
| beskid_tracker | fix/auth-client-0.2.9 | 53 | 9w ago | OBSOLETE | Entire early tracker lineage, no merge-base with main (orphan root at "initial Beskid roadmap and issue tracker app"); later commits include "Move to Terraform", "move from actions", GHA-based publish/auth work | Main's tracker has `COOLIFY.md`, `docker-compose.coolify.yml` and follows the Coolify/Watchtower deploy model plus Woodpecker CI documented in AGENTS.md — Terraform and GitHub Actions deploy are not the current model | MEDIUM (size) / LOW (actionable) | Delete branch |
| beskid_web_common | fix/auth-client-export-pairing-result | 2 | 5w ago | SUPERSEDED | Same `0173c05c` pairing-types-as-type-only change as root's fix-pairing-all-services | Same evidence: main's `packages/beskid-auth-client/src/index.ts` already exports the type-only set including `AuthHubPairingApprovalResult` | LOW | Delete branch |
| compiler | local/verify | 106 | 7w ago | OBSOLETE | Very old corelib bootstrap history (corelib as an inline directory, not a submodule), Dagger-based CI ("CICD: Dagger migration"), GitHub-Actions-based `pckg` publish workflow | Main's `compiler/corelib` is a submodule pointer (`32e23ecc...`), matching AGENTS.md ("Corelib in `compiler/corelib`... not a Rust crate move"); CI is Woodpecker-only now. The corelib content itself now lives and evolves in the separate `beskid_standard` repo | MEDIUM (size) / LOW (actionable) | Delete branch |
| compiler | codesmith/main-integration-merge-wip | 1 (multi-merge-base) | 8w ago | OBSOLETE | Large WIP integration merge (110 files, +7118/-1109): parse-recovery engine rewrite, ABI-v5 contract additions, and `crates/beskid_runtime/src/builtins/{args,math,strings}.rs` | `crates/beskid_runtime/src/` has **zero files** on main — the Rust `beskid_runtime` crate has been fully removed/ported to native Beskid runtime sources (`runtime/beskid/...`), consistent with AGENTS.md ("Cranelift ISLE remains the path for gradually porting runtime from Rust to Beskid"). The branch targets a crate structure main no longer has | MEDIUM (size) / LOW (actionable) | Delete branch (re-derive any still-wanted parse-recovery ideas fresh against current `beskid_analysis`, since the crate layout has moved on) |
| compiler | codex/manual-release-0.4.744 | 1 | 12d ago | SUPERSEDED | Same `95449279` macOS `dyld_stub_binder` runtime-provenance fix as root's pointer bump | `DARWIN_MACHO_SHARED_TOOLCHAIN_IMPORTS` present verbatim on main | LOW | Delete branch |
| compiler | codex/synthetic-source-isolation | 1 | 13d ago | SUPERSEDED | `materialize_source_path_for_lowering`: per-directory (not per-file) scratch isolation for synthetic/`<memory>` sources | Main's `crates/beskid_codegen/src/services.rs` has the identical `SCRATCH_DIRECTORY_ID` / per-process-and-id scratch-directory logic, slightly extended (generalized `<...>` virtual-label filter instead of just `<memory>`) | LOW | Delete branch |
| pckg | cursor/cyb-130-pckg-web-common-copy | 1 | 9w ago | SUPERSEDED | One-line Dockerfile fix: copy `beskid_web_common` into the pckg image build context | Main's pckg `Dockerfile` already `COPY beskid_web_common/packages/beskid-ui-react ...` and `beskid_web_common/packages/beskid-ui/src/styles ...` from the root build context | LOW | Delete branch |
| beskid_standard | cursor/cyb-w5-runtime-04 | 1 | 9w ago | SUPERSEDED | Rewrites `Testing/Assert.bd` to use explicit if/else (instead of bare-if-then-fallthrough) because ISLE's reachable-merge lowering used to drop statements after a bare `if` (CYB-129 workaround) | Main's `Assert.bd` keeps the original bare-if style, with a comment stating the opposite is now true: "after a bare if remain reachable, so keep the nonempty path without `else`" — i.e. the ISLE lowering bug itself was fixed upstream (sibling branch `cursor/cyb-w5-assert-isle-fix` is marked ABSORBED), so the workaround this branch proposes is unnecessary | LOW | Delete branch |

## Summary by class

- SUPERSEDED: 23 branches
- OBSOLETE: 5 branches (root appveyor-macos-cross-validation, root appveyor-rustup-bootstrap, beskid_tracker fix/auth-client-0.2.9, compiler local/verify, compiler codesmith/main-integration-merge-wip)
- PARTIAL: 0
- RELEVANT: 0

No branch in this set contains work that is both (a) not already present on main in some form and (b) still a good fit for main's current architecture. All 28 are safe to delete from a content-loss perspective; deletion itself (a destructive/authorization-gated action) was intentionally not performed as part of this read-only audit.
