i32 sum_to(n: i32) {
    let mut acc: i32 = 0;
    for i in range(0, n) {
        acc = acc + i;
    }
    return acc;
}

i32 max(a: i32, b: i32) {
    if a > b {
        return a;
    }
    return b;
}
