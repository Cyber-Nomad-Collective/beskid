## Learned User Preferences

- **Book images:** Do not change `![alt](url)` under `site/website/src/content/docs/book/` unless the user names the file and exact tag. Remote HTTPS (Tenor, Giphy, Imgur, etc.) is intentional—never `/book/reactions/`, co-located swaps, or `public/book/reactions/`. On `ImageNotFound`/404: report path, suggest `bun run dev:clean`; do not rewrite tags for green CI. User Ctrl+Z on image edits is authoritative.
- **Plans & git:** Execute every listed plan task; split multi-task work across parallel subagents with non-overlapping scopes. When asked: commit/push (often `main`, submodules, all remotes). Fresh tree: `./scripts/setup-environment.sh` (subprocess helpers—never `source` scripts that `exit`). No `Co-authored-by: Cursor` (`git commit-tree` or strip trailers). Recursive commit/push across submodules: **lazygit only** (`P` via `scripts/lazygit/config.yml` → `~/.config/lazygit/`; invokes `git-commit-push-recursive.sh`—not for direct shell use). **Mission Control** is external—no repo MC sync or task-board mirroring.
- **Quality & docs tone:** CI root-cause fixes; triage with `gh run list` / `gh run view`. No stub normative spec or implementation. Public README/entry docs: no emoji; prefer practical Coolify/container fixes over abstract guidance.
- **pckg UI:** Fluent Blazor patterns; `api.json` hierarchy drives package docs (nav aligned, Monaco explicit height, header search with symbol search in overflow, Fluent icon + chip + path on source headers). Grid actions: icon-only squares, tooltips, primary first, shared tokens/wrapper. `@beskid/beskid-ui` hub launcher leftmost in app navbars (site, tracker, nexus, pckg).
- **corelib / compiler / VS Code:** Corelib stays Beskid in `compiler/corelib` submodule (trudoc colocated)—no Rust lib crate move. VS Code: modular/DRY, status bar for LSP/pckg progress; LSP commands via `ExecuteCommandFeature` + middleware only. Bootstrap `cli-latest` + `beskid lsp` (log install failures). Dev: `compiler/target/release/beskid_lsp` → `cargo run -p beskid_lsp`; bundle LSP for F5 when `server/` missing.
- **Platform spec & site:** Spec at `site/website/src/content/docs/platform-spec/` leads code—extend before behavior changes; book samples match spec + `beskid.pest`/parser tests. **Bun** for `site/website` (`dev`, `build`, `verify:trudoc`). Public docs: `/platform-spec/` + `/book/` only; `book/reference/` for guides; shared `platform-spec-nav-*` chrome; chapter numbers from `nav.order.json` (not title prefixes); Architecture: dagre + SVG. One typed-HIR spine + `beskid_pipeline` for analyze/run/build/LSP—no divergent paths or JIT without `prepared_workspace`. pckg: registry-assigned versions. `///` `@arg` on callable parameters only. Compiler Rust: `beskid_<domain>`, export-only hubs, `beskid_pipeline` phase IDs.

## Platform specification (agents)

- **Tree:** `site/website/src/content/docs/platform-spec/` — normative `/platform-spec/`; `main` is the version axis. See [spec authority](site/website/src/content/docs/platform-spec/community/spec-maintenance/spec-authority-and-decisions.mdx) and [release policy](site/website/src/content/docs/platform-spec/community/spec-maintenance/release-and-versioning-policy.mdx).
- **Spec leads code:** Update normative spec before (or with) observable behavior changes. No circular-canon stubs, placeholder-only bundles, or scaffold `status: Standard` hubs.
- **Ownership:** User-visible semantics → **language-meta**; pipeline/runtime/CLI/manifests/crates → **compiler**, **execution**, **core-library**, or **tooling** with `relatedTopics` deferring to language-meta. Skill: `.cursor/skills/platform-spec-compiler-spec-docs/SKILL.md`.
- **Standard hubs:** `## Decisions` or `decisions-record.mdx` per [feature hub template](site/website/src/content/docs/platform-spec/community/spec-maintenance/feature-hub-article-bundle-template.mdx). `relatedTopics[].type`: `Domain` | `Area` | `Feature` only.
- **Verify:** `cd site/website && bun run verify:trudoc -- --preset ci` after edits (`--strict` on platform-spec content for scaffold/circular-canon/missing Decisions).

## Learned Workspace Facts

### Superrepo & services

- Aggregate superrepo: submodules `compiler`, `pckg`, `beskid_vscode`, `beskid_nexus`, `beskid_tracker`, `beskid_web_common` (`trudoc`, `packages/beskid-ui` → `@beskid/beskid-ui` / `@cyber-nomad-collective/beskid-ui` via GitHub Packages or Bun workspaces). Manifests: `manifests/` + `./scripts/setup-environment.sh` (`repo`/`sync`, submodule fallback, optional `bun install`).
- **site/auth:** Go combined GitHub OAuth hub (`site/auth/`, port **8090**); one OAuth app handoffs to tracker, nexus, and pckg (`AUTH_HUB_PUBLIC_URL` + shared `AUTH_HUB_SECRET`, issuer `beskid-auth-hub`); see `site/auth/README.md`.
- **beskid_nexus:** Standalone Coolify repo (`docker-compose.yml`, `gitnexus serve` **8452**, `GITNEXUS_HOME` volume, runtime catalog/OAuth/analyze—no build-time compiler bake)
- **beskid_treesitter:** Tree-sitter grammar submodule; sync from `compiler/.../beskid.pest` via `./scripts/sync-from-pest.sh`; npm **`@cyber-nomad-collective/beskid-tree-sitter`** on GitHub Packages (import alias `@beskid/tree-sitter`; `.npmrc` scopes `@beskid` + `@cyber-nomad-collective`)
- **pckg:** Container deploy; first admin via in-app onboarding when empty. EF migrations on startup by default (`Pckg:Database:AutoMigrateOnStartup`). **Server.Tests:** xUnit parallelization off via `xunit.runner.json`. Optional **iconUrl** on packages (HTTP/S, capped).

### Corelib, CLI, VS Code

- **corelib:** Nested `compiler/corelib` → `beskid_corelib/`, pckg id **`corelib`**. `Core.Results.Result<T,E>`; App **Std** → `Std::Core::Results`. `BESKID_CORELIB_ROOT` = repo root with `beskid_corelib/Project.proj`. `api.json` is the pckg docs contract (hierarchical, compiler-derived `typeRef`, null doc fields when absent). I/O: `packages/runtime/src/System` split surfaces; console/ANSI in separate package—not monolithic `IO.bd`.
- **CLI releases:** `cli-v{semver}` + rolling `cli-latest` on GitHub (`cli-version.txt`, `compiler/ci/version.py`). `bun run --cwd site/website sync:cli-version` → gitignored `cli-version.json`, `vscode-extension.json`. Install scripts: `BESKID_RELEASE_TAG` (default `cli-latest`). CDN: `cdn.beskid-lang.org`.
- **VS Code:** Open VSX matrix `darwin-arm64`/`darwin-x64` (`OPENVSX_RUST_TARGET=x86_64-apple-darwin` for Intel on ARM CI). Package: `bundle-lsp-host.mjs` → `npm prune --omit=dev` → `vsce package` → `bun install`. Keep `@types/vscode` within `engines.vscode`. Compiler CI publishes **corelib** to pckg via `BESKID_PCKG_KEY`.

### Docs site (`site/website`)

- **Build:** Astro 6 / Node ≥22.12; root `.dockerignore`; container may set `BESKID_SKIP_TRUDOC_VERIFY=1` (CI enforces trudoc). MDX: escape `` `Foo<T>` ``; mermaid via `astro-mermaid`. Prebuild: `website-prebuild.mjs`, `verify:book-images`, `generate-platform-spec-git-meta.mjs` (24 workers default).
- **Platform-spec UI:** `PlatformSpecHome*` browse/map; dagre Architecture graphs in `@beskid/beskid-ui`; ADRs (`specLevel: adr`, `SpecAdrPanel`, GFM via `parseAdrSections`/`renderAdrMarkdown`); path linkification `remark-inline-repo-paths.mjs`. CSS mirrored in `beskid-ui` + `trudoc` `platform-spec-core.css` (edit both). Hidden tabpanels: off hit-testing in `platform-spec-graph-and-reader.css`; land on **Current document** via `platform-spec-doc-tabs.ts`. Zod validation under `trudoc/src/platform-spec/`.
- **Nav & book:** Two areas only (`/platform-spec/`, `/book/`); empty Starlight sidebar; `trailingSlash: 'always'`; legacy in `src/legacy-bridge/`. Book: `nav.order.json` → `book-nav-tree.json`, `BookNavChrome`/`BookReaderShell`, `verify:book-images` + `verify:book-layout`. Tutorial `00-…`–`22-…`; `book/reference/` for guides; author-owned images (see Preferences). Downloads: `DownloadsPage.astro`, `load-download-versions.ts` (no static import of gitignored version JSON from MDX). `ClientRouter` + `view-transition-lifecycle.ts`.

### Compiler

- Mods: `Mod`/`CompilerMod`, compiler-sdk contracts, AOT-only; no language `meta` blocks; host composition is Rust-only. Compiler never in Beskid; `Option<T>` only; Rust AST canonical (`beskid_ast_reflect_gen`). **`beskid run`** uses `lower_resolved_input_with_pipeline` + full `ResolvedInput` like **`beskid build`**. `Module::Type::Variant` may be unimplemented—fixtures may use local enums.
