## Learned User Preferences
- **Book image tags are hands-off:** Do not change, replace, relocate, delete, or “fix” any Markdown `![alt](url)` line in `site/website/src/content/docs/book/` unless the user explicitly names that file and the exact new tag in the same message. Especially `book/00-why-beskid-exists/`. The author uses **remote HTTPS URLs** (Tenor, Giphy, Imgur, etc.) on purpose—never swap them for `/book/reactions/`, co-located files, text-only captions, or other “normalization.” Do not create or use `public/book/reactions/`. Never remove an image tag the user set; **Ctrl+Z restores are authoritative** when the user reverts agent image edits. On `ImageNotFound` or dev 404s, report the path and suggest `bun run dev:clean`—do **not** rewrite image tags to make CI/dev green.
- User often asks to execute implementation plans exactly as provided through every listed task without stopping early, prefers multi-task plans split across parallel subagents with non-overlapping scopes rather than end-to-end delegation to a single subagent, and frequently wants git work carried end-to-end: commit and push fixes, often to `main`, across relevant submodules and the superrepo, sometimes to every configured remote, not only the default. Commits must not include `Co-authored-by: Cursor` trailers (IDE hooks may append them on plain `git commit`; use `git commit-tree` or strip trailers). **Mission Control** is an external prioritization utility (brain dump, manual tasks)—do not add repo-local MC sync scripts, wave organizers, or task-board mirroring into the superrepo.
- User prioritizes CI reliability, expects robust root-cause fixes over hacky stopgaps, and pushes back on stub or placeholder spec/implementation when a normative deliverable is expected; for CI failures, triage with GitHub CLI (`gh run list` / `gh run view`) before fixes.
- User prefers welcoming public-facing superrepo README and similar entry docs without emoji (including toolchain links and optional CI or version badges where they add clarity) and practical deployment-oriented fixes (especially for Coolify/container setups) over abstract guidance.
- User expects pckg dashboard, admin, and package documentation UI work to lean on Fluent UI Blazor patterns and cohesive spacing (menus, nav, cards, stock search controls) rather than ad-hoc markup; package docs pages should render the hierarchical `api.json` model (types, members, cross-links) as the sole structure driver, keep the main content column aligned with navigation (not displaced below the sidebar), use explicit height for Monaco source previews, use Fluent-aligned file-type icons, put primary doc search in the header with symbol search behind an overflow/more action rather than a dedicated symbol-search panel, and use Fluent icon plus neutral chip plus muted path label on package source preview headers.
- User expects the corelib implementation to remain Beskid source in the compiler `corelib` submodule, with trudoc-compatible documentation colocated alongside the Beskid sources; they push back on introducing separate Rust crates or otherwise moving library implementation out of Beskid.
- User wants pckg data-grid row actions unified: icon-only square buttons with labels in tooltips, primary action first, with shared layout tokens or a small shared wrapper so styling stays consistent across grids.
- User wants the Beskid VS Code extension modularized (smaller entry files, DRY) with a reusable status-bar item for live LSP progress (for example rescan counts) and future pckg workflow states such as init or dependency resolution; LSP-advertised execute commands (for example `beskid.refreshWorkspace`) must be owned by LanguageClient `ExecuteCommandFeature`, not manually registered in the extension—UI side effects belong in client middleware after the server handler runs.
- User treats `site/website/src/content/docs/platform-spec/` as the canonical baseline for v0.2 and ongoing alignment across compiler, pckg, CI, and docs; before any implementation work, expects the platform spec to be extended first to identify and close design gaps with explicit questions plus recommendations, with distributed normative updates to existing hubs preferred over new stub hubs, so the spec leads code rather than the reverse; **book Beskid code samples** must match normative platform-spec and `compiler/crates/beskid_analysis/src/beskid.pest` / parser tests (not invented syntax—e.g. `test Name { }`, not `test MyCase()`); for `site/website` dev/build/verify prefer **Bun** (`bun run dev`, `bun run build`, `bun run verify:trudoc`) over `npm run`; the public docs site should expose only **Platform specification** (normative) and **Beskid Book** (informative, reading-first)—not a default Starlight sidebar for guides, execution, corelib, packages, or API—with merged guide content under `book/reference/` and book chrome built for reading (dedicated nav rail, prose measure, chapter prev/next), not a platform-spec rail clone; book chapter display numbers come from `nav.order.json` / trudoc nav numbering—do not hardcode `1.x` (or similar) prefixes in page titles or frontmatter; platform-spec **Architecture** tab pipeline graphs use **dagre** layered layout with SVG connectors (not force-directed D3); compiler tooling should share one cross-crate compilation and progress model (CLI, analysis, codegen/AOT) that stays DRY and avoids narrow one-off crates or dependency tangles when threading phase reporting through the pipeline; public docs hierarchy nav should treat **platform-spec tree/chrome as the single style** for both `/platform-spec/` and `/book/`—shared `platform-spec-nav-*` tokens and BEM, not parallel book-only nav styling.
- User expects pckg to use registry-assigned package versions rather than publisher-supplied manual versioning in normal flows.
- User expects `///` documentation to use `@arg` only on **callable parameters** (functions, methods, and contract methods), not on type members, record fields, or mirrored compiler-sdk struct fields; for generated or mirrored Beskid sources (for example the compiler SDK syntax model), explain semantics and non-obvious invariants without restating obvious identifier names or types already visible in the declaration.
- User expects Rust in the compiler workspace to follow conventions aligned with the in-repo **bsharp** reference and existing crates: `beskid_<domain>` crate names, `mod.rs`/`lib.rs` hubs as exports-only, pipeline progress through `beskid_pipeline` phase IDs (not ad-hoc logging in library crates), and responsibility-split modules instead of growing hub files.

## Platform specification (agents)

- **Canonical tree:** `site/website/src/content/docs/platform-spec/` — single normative entry at `/platform-spec/`; Git on `main` is the version axis (no URL version segments). See [Specification authority and embedded decisions](site/website/src/content/docs/platform-spec/community/spec-maintenance/spec-authority-and-decisions.mdx) and [Release and versioning policy](site/website/src/content/docs/platform-spec/community/spec-maintenance/release-and-versioning-policy.mdx).
- **Spec leads code:** Extend or update normative spec text before (or in the same change set as) implementation that changes observable language or platform behavior. Do not commit circular “canonical chapter is this page” stubs, placeholder-only article bundles, or scaffold-only feature hubs as `status: Standard`.
- **Domain boundary check:** Before adding or materially editing a platform-spec feature, decide ownership: user-visible semantics → **language-meta** first; pipeline, runtime, CLI, manifests, and crate layout → **compiler**, **execution**, **core-library**, or **tooling** with `relatedTopics` deferring to language-meta (no duplicated normative key tables). Skill: `.cursor/skills/platform-spec-compiler-spec-docs/SKILL.md`.
- **Embedded decisions:** **Standard** feature hubs need `## Decisions` or a linked `decisions-record.mdx` per the [feature hub template](site/website/src/content/docs/platform-spec/community/spec-maintenance/feature-hub-article-bundle-template.mdx).
- **Frontmatter:** `relatedTopics[].type` must be `Domain`, `Area`, or `Feature` only (not `Article`).
- **Validation:** Run `cd site/website && bun run verify:trudoc -- --preset ci` (includes `verify:platform-spec-content`; use `--strict` on that script to fail on scaffold/circular-canon/missing-Decisions) after platform-spec edits; fix trudoc/Zod and layout errors before claiming done.

## Learned Workspace Facts
- This workspace is an aggregate superrepo where core code lives in submodules, especially `compiler`, `pckg`, and `beskid_vscode` (VS Code extension sources for aggregate/Open VS X workflows).
- Corelib (standard library) is expected via the nested `compiler/corelib` submodule (`beskid_standard` repo, not a top-level superrepo submodule); the canonical Beskid package sources live at `beskid_corelib/` with **pckg package identity `corelib`** (legacy `standard_library` paths may still be accepted by tooling). The separated workspace layout uses the repo root as `BESKID_CORELIB_ROOT` (must contain `beskid_corelib/Project.proj`); corelib CI publish/pack must not assume `.ci-cache/beskid_corelib`. Arrays are intended as a primitive Beskid type with unified compile-time length semantics where the type system allows. Syscall-backed runtime I/O lives under `packages/runtime/src/System` as split `Input` / `Output` / `Error` surfaces; higher console/ANSI work belongs in a dedicated `corelib_console` (or equivalent) package, not monolithic `IO.bd`. Beskid CLI packing can place generated Markdown and structured `api.json` under `.beskid/docs/`; registry listing, serving, and `beskid_pckg` ingestion should treat those paths as first-class. Structured **`api.json` is the primary contract** for pckg docs: hierarchical members/types, compiler-derived signatures and `typeRef` links for all resolved symbols (documented or not, with null doc fields when prose is absent)—not parallel string-key grouping outside the JSON schema.
- The `pckg` service is deployed in containers with compose-managed database connectivity, HTTP port binding, artifact storage paths, and optional captcha keys; when no users exist yet, the first administrator is created through the interactive in-app onboarding flow. EF Core migrations apply at application startup by default (`Pckg:Database:AutoMigrateOnStartup`); routine deploys should rely on that path (including legacy schema repair when history drifts) unless operators explicitly disable auto-migration.
- Prebuilt Beskid CLI binaries are published from the `compiler` repo CI to **GitHub Releases** as both an immutable **`cli-v{semver}`** release per publish and a rolling **`cli-latest`** pointer (each carries `cli-version.txt` and platform binaries). Rolling semver is resolved in `compiler/ci/version.py` from the latest `v*` git tag plus commits since that tag (same model as corelib), not ephemeral Cargo.toml patch bumps in CI; `bun run --cwd site/website sync:cli-version` runs `packages/trudoc/scripts/sync-cli-version.mjs`, which prefers GitHub `cli-version.txt`, syncs `compiler/crates/beskid_cli/Cargo.toml`, and writes gitignored `site/website/src/data/cli-version.json` (including `downloadTag` for versioned asset URLs) plus `vscode-extension.json` (Open VSX `beskid/beskid-vscode` latest, else local `beskid_vscode/package.json`). Install scripts under `site/website/public/` accept `BESKID_RELEASE_TAG` (default `cli-latest`); the downloads page uses `downloadTag` for direct links. User-facing download docs may also reference `cdn.beskid-lang.org` under the `beskid-lang.org` domain.
- Open VS X publishing for the VS Code extension uses a matrix that includes `darwin-arm64` and `darwin-x64`; the `darwin-x64` job sets `OPENVSX_RUST_TARGET` to `x86_64-apple-darwin` so the bundled Intel LSP matches the `darwin-x64` VSIX when CI runs on Apple Silicon hosts (`noxfile.py` / `ci/open_vsx.py` / `.github/workflows/publish-open-vsx.yml`). Local VSIX packaging bundles the LSP from `compiler/` via `beskid_vscode/scripts/bundle-lsp-host.mjs` into gitignored `server/<platform>-<arch>/beskid_lsp`, then `scripts/package-vsix.mjs` runs build → `npm prune --omit=dev` → `vsce package` with `BESKID_VSCODE_SKIP_PREBUILD=1` → `bun install` (Bun `node_modules` fails `vsce` npm validation without the prune step). `vsce package` rejects `@types/vscode` versions above the declared `engines.vscode` range, so typings must stay aligned with the extension engine pin.
- Compiler CI can publish the **`corelib`** package to **pckg** using the `BESKID_PCKG_KEY` GitHub Actions secret (alongside CLI release automation).
- The repo uses `.cursorignore` with a negated `!references/` pattern so gitignored reference trees stay available for Cursor indexing and `@` mentions; opinionated formatter work is often compared to the in-repo **bsharp** reference under `references/bsharp` (C#-style `Emit` patterns).
- `pckg` **Server.Tests** disables xUnit assembly and collection parallelization via `xunit.runner.json` copied into the test output to reduce intermittent failures when the hosted test server starts under full-suite load.
- Package upsert and summaries support an optional **iconUrl** (HTTP/HTTPS only, length-capped to match storage); dashboard metadata flows and package detail pages surface it with image fallback when loading fails.
- Canonical cross-cutting specification content for the project is maintained under `site/website/src/content/docs/platform-spec/` (normative pages and nav/graph only under that tree—not legacy `src/content/docs/execution/` or other Starlight paths); Astro 6.x site builds expect Node ≥22.12 for `astro build` in CI and production (avoid relying on a Bun-reported Node 20 stub for Astro). Website container builds use a root `.dockerignore` to keep Docker/Podman build contexts small; `site/website` image builds may set `BESKID_SKIP_TRUDOC_VERIFY=1` when full trudoc prebuild verify cannot run under Bun in Linux containers while CI still enforces contracts separately; shared `packages/trudoc/scripts/website-prebuild.mjs` orchestrates prebuild steps (including `verify:book-images`). In platform-spec MDX, escape angle-bracket generics in tables and prose (for example `` `Spanned<Program>` `` or `` `Channel<T>` ``) so MDX does not treat them as JSX components; use `` ```mermaid `` fences (via `astro-mermaid`) for diagrams in spec and book where appropriate. Platform-spec **index** uses DRY `PlatformSpecHome*` components: flat **Browse** tab (tabs-only header, domain tiles, latest changes/guide—search/stats not on browse; Map tab keeps graph search), legacy spec-mapping card removed, `data-active-tab` browse/map visibility with a layout guard and ~88rem centered browse column; home browse/map use off-canvas nav rail (full-width main until header toggle opens drawer). In-site docs navigation uses Astro `ClientRouter` with `fallback="animate"` (directional slide on Starlight `<main>`, `view-transition-lifecycle.ts` re-init on `astro:after-swap`). Website `Dockerfile` installs Node 22.12, runs generators, then `npx astro build` with `BESKID_SKIP_TRUDOC_VERIFY=1` (matches `.github/workflows/docs-site.yml`; trudoc CI stays in Actions). Platform-spec **Architecture** tab pipeline graphs are rendered in `@beskid/docs-ui` with **dagre** layered layout and SVG connectors from JSON `nodes`/`edges` (not basicprimitives/FamDiagram). Normative decisions are first-class **`specLevel: adr`** pages under each feature's `adr/` directory (plus inception ADRs under `community/project-inception/adr/`); feature hubs surface an **ADRs** tab with expandable `SpecAdrPanel` detail, while standalone ADR pages hide On-this-page / right TOC and omit the ADRs tab on `SpecAdrChrome`. ADR section bodies (Context, Decision, …) are parsed with `parseAdrSections.ts` and rendered as GFM HTML via `renderAdrMarkdown.ts` (remark-gfm), not plain `<pre>` text; feature hub `## Decisions` should index `adrId` entries rather than duplicating long prose only in `index.mdx`. Repo file paths in backtick prose on platform-spec and book pages are linkified via `packages/trudoc/scripts/remark-inline-repo-paths.mjs` (wired in site remark config). Predev/prebuild runs `packages/trudoc/scripts/generate-platform-spec-git-meta.mjs` (parallel git, default 24 workers via `PLATFORM_SPEC_GIT_META_CONCURRENCY`, one `git log --follow` per file, full revision count only when history ≥50 commits). Platform-spec reader shell rules are mirrored in `packages/beskid-docs-ui/src/styles/platform-spec-core.css` and `packages/trudoc/css/platform-spec-core.css`; update both together when changing typography, layout, or base `font-family`, and regression-test platform-spec top navigation plus tab panels so visible content tracks the active tab after shell CSS edits. On feature/article reader pages, inactive Starlight `[role='tabpanel'][hidden]` with flex descendants can stay painted and block link clicks—force hidden panels off hit-testing in `platform-spec-graph-and-reader.css` (and trudoc mirror); article pages must land on **Current document** (not **Articles** restored from Starlight `localStorage`); use `platform-spec-doc-tabs.ts` for article/tile link navigation and `selectCurrentDocumentTab()`. Platform-spec structure and metadata are validated with trudoc/Zod under `packages/trudoc/src/platform-spec/`; CI surfaces frontmatter, graph link, and template drift errors.
- Public docs navigation is two-area only (`/platform-spec/`, `/book/`): empty Starlight `Sidebar.astro`, flat header (Home, Platform specification, Book, Download), `data-platform-spec` / `data-book` HTML attrs; `site/website` uses `trailingSlash: 'always'` and keeps de-navigated legacy trees under `src/legacy-bridge/` (not Starlight content) so redirects do not duplicate static routes. `/docs/downloads/` uses `DownloadsPage.astro` and `downloads.css` (open layout, platform-spec underline tabs, pill buttons, row list for extra platforms—not nested landing cards); versions load via `src/lib/load-download-versions.ts` with fs fallbacks (do not static-import gitignored `cli-version.json` / `vscode-extension.json` from MDX). Book uses `book/nav.order.json`, `generate:book-nav-tree` → `book-nav-tree.json`, `BookNavChrome` / `BookReaderShell` (same `platform-spec-nav-chrome` / `platform-spec-nav-tree__*` classes as platform-spec), and `book.css` / `doc-areas.css`; platform-spec nav rail defaults collapsed on desktop (`data-rail-collapsed="true"`), off-canvas drawer on index browse/map; book prose renders inside `BookReaderShell` from custom `Page.astro` (server-side `ContentPanel` slot—not Banner + client DOM moves). Tutorial spans edgy slug folders `00-why-beskid-exists` through `22-so-you-want-to-contribute` per `nav.order.json`; flat manifest paths nest in the rail via trudoc `nestTutorialNavNodes` while prev/next stays flat; folder branch nodes in `BookNavBranch` are expand-only (no `href`); former `guides/` content lives under `book/reference/`; narrative copy keeps the sarcastic `compiler/README.md` voice; colocated markdown image paths must match on-disk filenames for Astro's content image pipeline; `bun run verify:book-images` fails on missing co-located book images; Astro `ImageNotFound` on a bad local path can corrupt `.astro` content cache and cause site-wide dev 404s—add the asset and run `bun run dev:clean`, never rewrite author image tags; static figures (not reaction GIFs) are centered and scaled without shrinking the prose column; the book TOC stays in a right rail sized with `minmax(--book-toc-min, min(--book-toc-max, max-content))` and must override Starlight `PageSidebar` `.sl-container` width (`--sl-sidebar-width` is 0 for book); `book.css` guards in `STARLIGHT_BOOK_LAYOUT_GUARD`; `bun run verify:book-layout` fails if removed. Book chapter `![...](...)` image tags are **author-owned** remote or local URLs (see Learned User Preferences — agents must not edit them). Do not use `public/book/reactions/`; new explainers prefer mermaid and Starlight `<Aside>` over adding reaction media unless the author adds images.
- Compiler mods use a dedicated project type (`Mod`/`CompilerMod`) discovered through compiler-sdk contracts (`Generator`, `Analyzer`, `Rewritter`) with AOT-only execution; language-level `meta` block grammar is removed, and Rust-only compiler composition (host/composition syntax, pipeline phases) stays separate from Beskid-side mod SDK behavior. The compiler itself is never written in Beskid. Beskid optional types are `Option<T>` only (not an `optional` keyword). Rust AST is canonical: `Beskid.Compiler.*` SDK sources (including `Query.bd`) are generated via `beskid_ast_reflect_gen`, not hand-maintained parallel syntax. IoC/DI is compile-time inside the Rust host and read-only to mods/SDK. The corelib repo is split into interlinked workspace packages.
