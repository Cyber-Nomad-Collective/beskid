node_modules
dist
.nitro
.output
.vinxi
.turbo
*.local
.env*
.DS_Store
coverage

# deploy/ — never commit secrets or rendered runtime state.
# The htpasswd contains a bcrypt hash of the registry password; generate and
# rotate it outside Git as documented in deploy/README.md.
deploy/registry/htpasswd
deploy/.env
deploy/.env.*
