---
title: "Execution: ABI, host, and runtime"
description: What happens after the compiler stops pretending. ABI contracts, host integration, fiber scheduling, and syscall bridges.
tableOfContents: true
---

<Aside type="caution">
This chapter is informative. Enforceable rules live under the [Platform specification](/platform-spec/).
</Aside>

Compilation is the part you can screenshot for a slide deck. Execution is the part that actually runs: it panics, schedules fibers, and talks to the OS without asking your permission first.

```text
beskid_codegen / beskid_engine ──► beskid_abi ──► beskid_runtime ──► OS / syscalls
```

The pages that follow: where the execution domain's authority starts and where language-meta's ends, the ABI surface and how extern calls cross into the host, the fiber scheduler, panic and syscall policy, and how spec claims tie back to conformance tests.
