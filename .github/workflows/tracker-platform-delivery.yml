name: Tracker platform delivery

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  packages: read

jobs:
  openspec-projection:
    runs-on: ubuntu-latest
    env:
      NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN || github.token }}
      BUN_INSTALL_CACHE_DIR: /tmp/beskid-bun-install-cache
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      # Catalog revision hashes informative docs under these submodules; without
      # them, regen drifts from the committed openspec/catalog.json.
      # beskid_web_common is required because the root workspace members
      # (site/website, site/platform-spec) and beskid_tracker resolve @beskid/*
      # via file: links into it, so --frozen-lockfile installs fail without it.
      - name: Initialize catalog document submodules
        run: >
          ./scripts/ci/init-submodules.sh
          beskid_bsol
          beskid_distrib
          beskid_infra
          beskid_nexus
          beskid_tracker
          beskid_web_common
          compiler
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - name: Validate OpenSpec strictly
        run: bun run openspec:validate | tee openspec-validation.txt
      - name: Validate deterministic projection/reconciliation report
        run: |
          bun run openspec:catalog
          git diff --exit-code -- openspec/catalog.json
          bun install --cwd beskid_tracker --frozen-lockfile
          bun run --cwd beskid_tracker reconcile:plan | tee projection-reconciliation.txt
      - uses: actions/upload-artifact@v4
        with:
          name: openspec-projection-reports
          path: "*-validation.txt\nprojection-reconciliation.txt"

  tracker:
    runs-on: ubuntu-latest
    env:
      NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN || github.token }}
      BUN_INSTALL_CACHE_DIR: /tmp/beskid-bun-install-cache
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      # beskid_web_common supplies the file:../beskid_web_common @beskid/* deps
      # (e.g. @beskid/ui-react), so --frozen-lockfile fails without it.
      - name: Initialize tracker and shared web packages submodules
        run: ./scripts/ci/init-submodules.sh beskid_tracker beskid_web_common
      - uses: oven-sh/setup-bun@v2
      - run: bun install --cwd beskid_tracker --frozen-lockfile
      - run: bun run --cwd beskid_tracker test | tee tracker-tests.txt
      - run: bun run --cwd beskid_tracker check | tee tracker-check.txt
      - uses: actions/upload-artifact@v4
        with:
          name: tracker-reports
          path: "tracker-tests.txt\ntracker-check.txt"

  platform-spec:
    runs-on: ubuntu-latest
    env:
      NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN || github.token }}
      BUN_INSTALL_CACHE_DIR: /tmp/beskid-bun-install-cache
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Initialize shared web packages submodule
        run: ./scripts/ci/init-submodules.sh beskid_web_common
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run --cwd site/platform-spec test | tee platform-spec-tests.txt
      - run: bun run --cwd site/platform-spec typecheck | tee platform-spec-typecheck.txt
      - uses: actions/upload-artifact@v4
        with:
          name: platform-spec-reports
          path: "platform-spec-tests.txt\nplatform-spec-typecheck.txt"

  website-nexus:
    runs-on: ubuntu-latest
    env:
      NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN || github.token }}
      BUN_INSTALL_CACHE_DIR: /tmp/beskid-bun-install-cache
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      # beskid_web_common supplies the file:../../beskid_web_common @beskid/*
      # deps for site/website, so --frozen-lockfile fails without it.
      - name: Initialize nexus and shared web packages submodules
        run: ./scripts/ci/init-submodules.sh beskid_nexus beskid_web_common
      - uses: oven-sh/setup-bun@v2
      - run: bun install --cwd site/website --frozen-lockfile
      - run: bun test --cwd site/website src/lib/tracker-delivery.test.ts | tee latest-delivery.txt
      - run: bun run --cwd site/website build | tee website-build.txt
      - run: bun install --cwd beskid_nexus/gitnexus --frozen-lockfile
      - run: cd beskid_nexus/gitnexus && ./node_modules/.bin/vitest run test/unit/tracker-delivery-link-index.test.ts | tee ../../nexus-delivery.txt
      - uses: actions/upload-artifact@v4
        with:
          name: website-nexus-reports
          path: "latest-delivery.txt\nwebsite-build.txt\nnexus-delivery.txt"

  workflow-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - run: bash scripts/ci/test/run-tracker-platform-delivery-tests.sh
