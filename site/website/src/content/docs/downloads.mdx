---
title: Download Beskid CLI
description: Install Beskid quickly or download platform binaries directly.
template: splash
---

import LandingTemplate from '../../components/LandingTemplate.astro';
import cliVersion from '../../data/cli-version.json';

<LandingTemplate
  title="Download Beskid CLI"
  tagline="Install quickly, then run the same kind of code you see in the examples folder."
  subtagline="The CLI targets practical workflows: small typed programs, module-first structure, and direct builds."
  versionNote={`Current rolling CLI: ${cliVersion.version} (semver like beskid-vscode package.json, e.g. 0.1.0 — run beskid --version to confirm).`}
  codeFile="examples/control_flow.bd"
  codeLang="beskid"
  codeLines={[
    "i32 sum_to(i32 n) {",
    "    i32 mut acc = 0;",
    "    for i in range(0, n) {",
    "        acc = acc + i;",
    "    }",
    "    return acc;",
    "}",
  ]}
  actions={[
    { text: "Read the Language Spec", href: "/platform-spec/language-meta/", variant: "secondary" },
    { text: "Platform specification", href: "/platform-spec/", variant: "secondary" },
    { text: "Open the Book", href: "/book/", variant: "secondary" },
    { text: "Open VSX Extension", href: "https://open-vsx.org/extension/beskid/beskid-vscode", variant: "secondary" },
    { text: "Go to GitHub", href: "https://github.com/Cyber-Nomad-Collective/beskid", variant: "ghost" },
  ]}
  sectionTitle="Install options"
  features={[
    {
      title: "Quick install (macOS + Linux)",
      body: "curl -fsSL https://beskid-lang.org/install.sh | bash",
    },
    {
      title: "Quick install (Windows PowerShell)",
      body: "iwr https://beskid-lang.org/install.ps1 -useb | iex",
    },
    {
      title: "Direct latest binaries",
      body: "Linux, macOS (Apple Silicon), and Windows binaries are published on GitHub Releases under the rolling tag cli-latest. The same release includes cli-version.txt (plain semver for scripts and docs).",
    },
    {
      title: "Pinned version downloads",
      body: "For reproducible environments, use a release URL with an explicit Git tag. Tags use a v prefix in the URL path (for example …/download/v0.1.0/…), while beskid --version and the VS Code extension use plain semver (0.1.0) without the v.",
    },
    {
      title: "Editor extensions",
      body: "Install the Beskid VS Code-compatible extension from Open VSX: https://open-vsx.org/extension/beskid/beskid-vscode",
    },
  ]}
  calloutTitle="Direct links"
  calloutText="Use cli-latest for convenience, or a v-prefixed tag in the URL for reproducible installs. Semver in beskid --version matches the extension package version style (0.1.0)."
  calloutCodeLabel="urls"
  calloutCode={`rolling (cli-latest):
https://github.com/Cyber-Nomad-Collective/beskid_compiler/releases/download/cli-latest/cli-version.txt
https://github.com/Cyber-Nomad-Collective/beskid_compiler/releases/download/cli-latest/beskid-linux-amd64
https://github.com/Cyber-Nomad-Collective/beskid_compiler/releases/download/cli-latest/beskid-darwin-arm64
https://github.com/Cyber-Nomad-Collective/beskid_compiler/releases/download/cli-latest/beskid-windows-amd64.exe

pinned (example Git tag v0.1.0 — CLI reports 0.1.0):
https://github.com/Cyber-Nomad-Collective/beskid_compiler/releases/download/v0.1.0/beskid-linux-amd64
https://github.com/Cyber-Nomad-Collective/beskid_compiler/releases/download/v0.1.0/beskid-darwin-arm64
https://github.com/Cyber-Nomad-Collective/beskid_compiler/releases/download/v0.1.0/beskid-windows-amd64.exe`}
  calloutNote="Default install locations: ~/.beskid/bin/beskid (macOS/Linux) and %USERPROFILE%\\.beskid\\bin\\beskid.exe (Windows)."
/>

<style is:global>{`
  .s1-container:has([id$="_top"]),
  .sl-container:has([id$="_top"]) {
    display: none;
  }
`}</style>
