---
title: beskid
description: A comprehensive reference and architecture overview for the Beskid language.
template: splash
---

import LandingTemplate from '../../components/LandingTemplate.astro';

<LandingTemplate
  title="beskid"
  tagline="A statically typed language focused on readable semantics and compiler architecture."
  subtagline="Where language features are language features, not ten layers of corelib and DI containers."
  codeFile="examples/modules.bd"
  codeLang="beskid"
  codeLines={[
    "pub mod net.http;",
    "use net.http.Client;",
    "",
    "type ClientConfig {",
    "    string endpoint,",
    "}",
    "",
    "Client make_client(ClientConfig config) {",
    "    return Client { config: config };",
    "}",
  ]}
  actions={[
    { text: "Read the Language Spec", href: "/platform-spec/language-meta/", variant: "primary" },
    { text: "Platform specification", href: "/platform-spec/", variant: "secondary" },
    { text: "Read the Book", href: "/book/", variant: "secondary" },
    { text: "Download CLI", href: "/downloads/", variant: "ghost" },
  ]}
  sectionTitle="The Beskid Architecture"
  features={[
    {
      title: "Language Specification",
      body: "A comprehensive reference covering Beskid's type system, grammar, control flow structures, and memory safety model. Includes detailed examples and a formal language definition.",
    },
    {
      title: "Corelib Abstraction Sprawl",
      body: "Core library functionality is designed as native language features, not complex chains of nested interfaces, eliminating dependency on bloated external wrappers.",
    },
    {
      title: "Reflection as a Crutch",
      body: "Replaces heavy runtime reflection with powerful, verifiable compile-time analysis and metaprogramming. Get static safety, not slow runtime inspection.",
    },
    {
      title: "CIL Stagnation",
      body: "Beskid targets efficient, modern machine code directly, escaping the overhead and historical limitations of Common Intermediate Language (CIL) interpretations.",
    },
    {
      title: "First-Class Enumeration",
      body: "Native, optimized language support for collection iteration. Enumeration is a first-class citizen, not a library-level afterthought.",
    },
    {
      title: "Metaprogramming over Reflection",
      body: "Perform code generation, analysis, and optimization at build time with powerful metaprogramming, ensuring full type safety and performance.",
    },
    {
      title: "Assembly Output without IL Cuffs",
      body: "Directly outputs optimized, native assembly language or machine code. Your programs run directly on the hardware, with no intermediate runtime needed.",
    },
  ]}
  calloutTitle="Building Beskid"
  calloutText="Clone with submodules (required for the embedded corelib):"
  calloutCodeLabel="bash"
  calloutCode={`git clone --recurse-submodules https://github.com/Cyber-Nomad-Collective/beskid_compiler.git
# or, after a clone without submodules:
git submodule update --init --recursive`}
  calloutNote="In an aggregate repo that vendors this tree as a submodule (e.g., pecan/), initialize recursively, so compiler/corelib is populated: git submodule update --init --recursive (or clone with --recurse-submodules). You can override the corelib source path with BESKID_CORELIB_SOURCE. Corelib sources are pinned as a Git submodule at corelib/ (repository: beskid_standard), under corelib/beskid_corelib/; the **pckg** package id is **corelib**. The CLI embeds that tree at build time (crates/beskid_cli/build.rs)."
/>

<style is:global>{`
  .s1-container:has([id$="_top"]),
  .sl-container:has([id$="_top"]) {
    display: none;
  }
`}</style>
