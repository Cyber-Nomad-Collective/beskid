---
title: Salsa stores generated graph Mermaid strings
description: beskid_queries graph_mermaid tracked queries memoize Mermaid output with revision fingerprints.
specLevel: adr
status: Standard
adrId: D-TOOL-GRAPH-0002
adrStatus: Accepted
adrDate: 2026-05-30
lastReviewed: 2026-05-30
owner:
  name: Piotr Mikstacki
  email: pmikstacki@cybernomad.it
submitter:
  name: Piotr Mikstacki
  email: pmikstacki@cybernomad.it
relatedTopics:
  - type: Feature
    title: Graph visualization
    href: /platform-spec/tooling/graph-visualization/
    relation: Parent feature hub
  - type: Feature
    title: Stage ordering and lowering
    href: /platform-spec/compiler/build-pipeline/stage-ordering/
    relation: Salsa ADR 0005 parent
---

import SpecAdrChrome from '@beskid/beskid-ui/platform-spec/SpecAdrChrome.astro';

<SpecAdrChrome />

## Context

LSP `getProjectGraph` rebuilt domain graphs from disk on every call. CLI printed a flattened ASCII tree from `CompilePlan`, losing multi-hop structure.

## Decision

1. **`graph_fingerprint`** and **`graph_mermaid`** are `#[salsa::tracked]` queries in `beskid_queries::graph`.
2. LSP and CLI **must** read graphs through `compilation_db`, not ad-hoc rebuilds.
3. Invalidation follows `ManifestGenerationId`, `FileText`, and assembly/composition revision inputs per graph kind.

## Consequences

- Extension and CLI share the same cached Mermaid revision.
- Manifest or lock edits invalidate project/workspace graphs without full process restart.

## Status

Accepted — extends [ADR D-COMP-BUILD-0021](/platform-spec/compiler/build-pipeline/stage-ordering/adr/0005-salsa-database-incremental-memoization/).
