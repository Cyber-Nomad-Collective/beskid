---
title: Backends (JIT and AOT) - Contracts and edge cases
description: Required backend guarantees for output kinds, entrypoints, and shared lowering semantics.
template: splash
tableOfContents: true
specLevel: article
status: Standard
owner:
  name: Piotr Mikstacki
  email: pmikstacki@cybernomad.it
submitter:
  name: Piotr Mikstacki
  email: pmikstacki@cybernomad.it
relatedTopics:
  - type: Feature
    title: Backends (JIT and AOT)
    href: /platform-spec/compiler/build-pipeline/backends-jit-aot/
    relation: Parent feature hub
  - type: Area
    title: Codegen and IR
    href: /platform-spec/compiler/codegen-and-ir/
    relation: Shared artifact producer
---

import SpecArticleChrome from '@beskid/docs-ui/platform-spec/SpecArticleChrome.astro';

<SpecArticleChrome />



- JIT and AOT must not diverge in front-end semantic results for the same source.
- Entrypoint contract for executables requires an exported symbol (default `main`) with valid signature.
- AOT must support `Exe`, `SharedLib`, `StaticLib`, and `ObjectOnly`.
- Backend failures should report compile/build diagnostics, not silently fallback to alternate modes.
