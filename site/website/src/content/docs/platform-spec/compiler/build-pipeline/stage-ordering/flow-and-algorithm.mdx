---
title: Stage ordering and lowering - Flow and algorithm
description: Ordered lowering algorithm used by build and run command paths.
template: splash
tableOfContents: true
specLevel: article
status: Standard
owner:
  name: Piotr Mikstacki
  email: pmikstacki@cybernomad.it
submitter:
  name: Piotr Mikstacki
  email: pmikstacki@cybernomad.it
relatedTopics:
  - type: Feature
    title: Stage ordering and lowering
    href: /platform-spec/compiler/build-pipeline/stage-ordering/
    relation: Parent feature hub
  - type: Feature
    title: Diagnostics parity (CLI and LSP)
    href: /platform-spec/compiler/build-pipeline/diagnostics-parity/
    relation: Diagnostic gates during lowering
  - type: Area
    title: Compiler Mods
    href: /platform-spec/compiler/compiler-mods/
    relation: Mod host flow and typed merge rounds
---

import SpecArticleChrome from '@beskid/docs-ui/platform-spec/SpecArticleChrome.astro';

<SpecArticleChrome />



1. Resolve source path/text through services (manifest, workspace graph, `CompilePlan`, optional materialized workspace).
2. Parse program and produce syntax diagnostics.
3. **Mod host (when transitive `Mod` dependencies exist)** — Load AOT artifacts, discover contracts, run collect → generate → analyze → rewrite under **[Mod host bridge](/platform-spec/compiler/compiler-mods/mod-host-bridge/)** policy, merge typed AST contributions, and re-parse as required (bounded by `maxGeneratorRounds`). Skip when no mod dependencies apply.
4. Run builtin staged semantic rules when diagnostics are enabled (same diagnostic channel as mod diagnostics).
5. Abort on error diagnostics.
6. Transform to HIR, resolve symbols, and type-check.
7. Lower typed program to backend artifact for JIT/AOT.

The canonical lowering entrypoint remains `beskid_codegen::services::lower_source` (and `_with_pipeline`); mod orchestration **must** live in `beskid_analysis` services (or a dedicated host module re-exported there) so CLI, LSP, and codegen share one scheduling spine.
