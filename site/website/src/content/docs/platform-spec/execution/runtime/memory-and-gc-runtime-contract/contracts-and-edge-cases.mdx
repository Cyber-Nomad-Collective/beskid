---
title: Contracts and edge cases
description: Normative guarantees, constraints, and edge-case handling for `Memory and GC runtime contract`.
template: splash
tableOfContents: true
specLevel: article
status: Standard
owner:
  name: Piotr Mikstacki
  email: pmikstacki@cybernomad.it
submitter:
  name: Piotr Mikstacki
  email: pmikstacki@cybernomad.it
relatedTopics:
  - type: Area
    title: Runtime
    href: /platform-spec/execution/runtime/
    relation: Parent area
  - type: Feature
    title: Memory and GC runtime contract
    href: /platform-spec/execution/runtime/memory-and-gc-runtime-contract/
    relation: Parent feature hub
---

import SpecArticleChrome from '@beskid/docs-ui/platform-spec/SpecArticleChrome.astro';

<SpecArticleChrome />


## What this article covers
This article explains the `contracts-and-edge-cases` view for `Memory and GC runtime contract` in concrete, onboarding-first language.

## Newcomer walk-through
Start from the parent feature hub to learn the contract boundaries, then map the behavior to the listed implementation anchors before reading test coverage.

## Code anchors
- Runtime memory exports in `compiler/crates/beskid_runtime/src/lib.rs`
- ABI contracts in `compiler/crates/beskid_abi/src/lib.rs`
- JIT linking and call setup in `compiler/crates/beskid_engine/src/jit_module.rs`

## Practical guidance
When changing behavior in this area, update both the contract text and the corresponding compiler/runtime or tooling tests in the same change so the specification remains executable.
