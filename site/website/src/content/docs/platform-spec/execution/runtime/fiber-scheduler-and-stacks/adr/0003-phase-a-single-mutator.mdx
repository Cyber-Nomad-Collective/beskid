---
title: Phase A single GC mutator on scheduler threads
description: Only one thread holds Beskid mutator role for allocation at a time.
specLevel: adr
status: Standard
adrId: D-EXEC-RT-0003
adrStatus: Accepted
adrDate: 2025-11-15
lastReviewed: 2026-05-22
owner:
  name: Piotr Mikstacki
  email: pmikstacki@cybernomad.it
submitter:
  name: Piotr Mikstacki
  email: pmikstacki@cybernomad.it
relatedTopics:
  - type: Feature
    title: Fiber scheduler and stacks
    href: /platform-spec/execution/runtime/fiber-scheduler-and-stacks/
    relation: Parent feature hub
  - type: Feature
    title: Memory and GC runtime contract
    href: /platform-spec/execution/runtime/memory-and-gc-runtime-contract/
    relation: Phase B parallel mutators
---

import SpecAdrChrome from '@beskid/docs-ui/platform-spec/SpecAdrChrome.astro';

<SpecAdrChrome />

## Context

Fibers may run on multiple OS threads while Phase A GC still uses one process-wide arena (`enter_runtime_scope`). Syscall pool workers must not become second mutators.

## Decision

| Rule | Detail |
| --- | --- |
| Phase A | **One** thread at a time may execute Beskid allocations as GC mutator |
| Scheduler | Transfers mutator execution between fibers on that contract |
| Syscall pool | Workers run blocking host work **without** arbitrary Beskid mutator code or generated allocation |
| Phase B | Parallel mutators require spec update in memory/GC feature before implementation |

Aligns with [D-CORE-CONC-0007](/platform-spec/core-library/concurrency/concurrency-package/adr/0007-gc-phase-a-single-mutator/).

## Consequences

`run_blocking` paths park fibers and resume on scheduler threads for mutator work.

## Verification anchors

`beskid_runtime` scheduler + `enter_runtime_scope`; concurrency runtime tests.