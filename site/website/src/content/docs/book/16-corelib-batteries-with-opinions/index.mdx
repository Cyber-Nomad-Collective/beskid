---
title: "Corelib: batteries with opinions"
description: The standard library is injected, opinionated, and documented through api.json—not a grab bag of copy-pasted helpers.
tableOfContents: true
---

import { Aside } from '@astrojs/starlight/components';

<Aside type="caution">
This chapter is **informative**. Enforceable rules live under the [Platform specification](/platform-spec/).
</Aside>


The **corelib** package is not "stdlib.zip you found on a forum." The compiler **injects** it, the runtime backs part of it, and **pckg** serves the rest as normal, immutable package artifacts. Each `.bpk` carries its exact semver and registry dependency pins, while source `.bproj` files can keep workspace-local paths for development.

If you expected a monolithic `System.IO` graveyard, adjust expectations now.

## What you will find here

| Section | Topic |
| --- | --- |
| [corelib command](/book/16-corelib-batteries-with-opinions/corelib-command/) | Materialize the embedded template with `beskid dev build corelib`. |
| [Injection and resolution](/book/16-corelib-batteries-with-opinions/injection-and-resolution/) | How every project gets the same canonical package. |
| [Terminal and console](/book/16-corelib-batteries-with-opinions/terminal-and-console/) | ANSI, streams, and why console is not syscall soup. |
| [Doc and api.json](/book/16-corelib-batteries-with-opinions/doc-and-api-json/) | `beskid dev syntax doc`, structured API, and pckg docs UI. |
| [Runtime-backed surfaces](/book/16-corelib-batteries-with-opinions/runtime-backed-surfaces/) | What must call through the runtime vs pure Beskid. |

```mermaid
flowchart LR
  accTitle: Corelib delivery
  accDescr: The CLI injects corelib sources, doc generation produces api.json for pckg registry docs, and runtime-backed surfaces feed the same sources.
  CLI[beskid CLI] --> inj[Corelib injection]
  inj --> src[Corelib sources]
  src --> doc[beskid dev syntax doc / api.json]
  doc --> pckg[pckg registry docs]
  runtime[Runtime-backed surfaces] --> src
```

The sources under `compiler/corelib/packages` are split into these packages.

```mermaid
architecture-beta
  accTitle: Corelib packages
  accDescr: The corelib sources are split into foundation, runtime, concurrency, console, network, http, interop, glue, compiler-sdk and pest-gen-schema packages.
  group packages(server)[compiler/corelib/packages]
  service foundation(server)[foundation] in packages
  service runtime(server)[runtime] in packages
  service concurrency(server)[concurrency] in packages
  service console(server)[console] in packages
  service network(server)[network] in packages
  service http(server)[http] in packages
  service interop(server)[interop] in packages
  service glue(server)[glue] in packages
  service compiler_sdk(server)[compiler-sdk] in packages
  service pest_gen_schema(server)[pest-gen-schema] in packages
  foundation:R --> L:network
  network:R --> L:http
```

**Text equivalent:** The packages are foundation, runtime, concurrency, console, network, http, interop, glue and compiler-sdk. Arrows point from a package to the package that depends on it: `network` depends on `foundation`, and `http` depends on `network`.

## Next

[17. Execution: ABI, host, and runtime](/book/17-execution-abi-host-runtime/)
