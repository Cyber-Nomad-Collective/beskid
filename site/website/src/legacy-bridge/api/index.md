---
title: API Reference
description: Generated API reference from `beskid doc`.
---

The API reference is generated by the compiler CLI:

```bash
beskid doc <entry-file.bd> --out ./doc-out
```

This command emits:

- `api.json` for machine consumption
- `index.md` for rendered Markdown docs

Use `api.json` to feed custom tooling, and publish `index.md` into this section when generating website docs in CI.
