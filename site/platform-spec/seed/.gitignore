# Generated native-shape seed workspace. `bun run seed:static` regenerates it
# from the OpenSpec authority (openspec/specs + catalog.json); the image build
# bakes it and the entrypoint upserts it into SQLite/Memgraph. Never committed.
*
!.gitignore
