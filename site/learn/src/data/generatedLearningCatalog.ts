// Generated by scripts/build-curriculum.mjs. Do not edit by hand.
import type { LearnExercise } from "./learningCatalog";

export const generatedLearnExercises = [
	{
		"id": "orientation_learning_loop",
		"title": "Learn by predicting, checking, and explaining",
		"objective": "Use the Beskid Learn loop to turn each compiler check into evidence about one programming rule.",
		"slug": "01-learning-loop",
		"starterCode": "i32 Main() {\n  return 1;\n}",
		"command": "analyze",
		"mode": "interactive",
		"hints": [
			"Make one prediction before every check.",
			"Change one idea at a time so feedback stays legible."
		],
		"lessonPath": "/site/learn/curriculum/00-orientation/01-learning-loop/lesson.md",
		"difficulty": "beginner",
		"category": "orientation",
		"prerequisites": [],
		"detailedContent": "\n## Hook and goal\n\nLearning Beskid is a series of small expeditions, not a race to make the screen green. Each lesson asks you to predict behavior, observe evidence, explain the rule, make one controlled change, and retrieve the rule later without a hint. Your goal is to use that loop when a program succeeds *and* when it fails.\n\n## Predict\n\nBefore changing a lesson, write one falsifiable sentence: “If I change ___, the analyzer should ___ because ___.” Avoid predictions such as “it should work”; name the expected acceptance or the particular rule that should be rejected.\n\n## Run\n\n```beskid\ni32 Main() {\n  return 0;\n}\n```\n\nReference-only: this orientation lesson explains the workflow rather than claiming a standalone source fixture. In an interactive lesson, use the command stated by its package. `analyze` checks program structure and semantic acceptance; it does not replace a runtime observation when a lesson asks about execution.\n\n## Investigate\n\nThe loop is deliberately narrow:\n\n1. **Predict** one outcome before the tool tells you.\n2. **Run** the lesson's declared check.\n3. **Investigate** the smallest relevant source span and the cited language rule.\n4. **Modify** one bounded idea, then recheck.\n5. **Make and retrieve** a small transfer task and explain the rule from memory.\n\nThis is productive friction. A clean check is evidence that one revision met one command's contract; it is not proof that you understand the rule or that every possible program is correct. A failed check is equally useful when you can connect it to the change you made.\n\n## Modify\n\nChoose a completed lesson and create a one-line learning log with four fields: prediction, observed result, rule, next experiment. Make the next experiment differ by one semantic decision only—such as a value, a branch outcome, or one argument.\n\n## Make and retrieve\n\nMake a “compiler conversation” card for a peer: *I predict …; I checked with …; the evidence says …; therefore the rule is …*. Then close the lesson and retrieve the five loop stages in order. Use the card on a different lesson rather than copying a solution.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nIf a check fails after many edits, do not pile on repairs. Restore the smallest known state if available, reproduce one change, and read the earliest diagnostic. If a check passes but you cannot explain why, keep the result and perform a contrasting one-change experiment; green is a result, not an explanation.\n\n## Recap and next link\n\nThe Learn loop converts guesses into evidence through small, explainable changes. Next, learn to read a diagnostic as a location, a rule, and a repair boundary rather than as a cryptic verdict.\n",
		"questions": [
			{
				"id": "orientation_learning_loop_q1",
				"text": "What should you write before pressing a check button?",
				"options": [
					"A prediction about one observable rule",
					"A larger unrelated rewrite"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "orientation_diagnostics",
		"title": "Read diagnostics as evidence",
		"objective": "Locate the first relevant compiler diagnostic, identify its phase, and make a narrow repair.",
		"slug": "02-reading-diagnostics",
		"starterCode": "i32 Main() {\n  return 7;\n}",
		"command": "parse",
		"mode": "interactive",
		"hints": [
			"Start with the earliest actionable diagnostic.",
			"Separate the marked code from follow-on messages."
		],
		"lessonPath": "/site/learn/curriculum/00-orientation/02-reading-diagnostics/lesson.md",
		"difficulty": "beginner",
		"category": "orientation",
		"prerequisites": [
			"orientation_learning_loop"
		],
		"detailedContent": "\n## Hook and goal\n\nA compiler diagnostic is a trail marker, not an insult and not a patch recipe. It tells you where the compiler lost a required relationship and often which phase noticed it. Your goal is to translate that marker into a small hypothesis, repair the cause, and recheck without creating a larger mystery.\n\n## Predict\n\nImagine a function whose declared return type and returned value disagree. Predict the phase most likely to complain: parsing, name resolution, or type checking. Then predict what facts the message needs to show you before you edit: location, expected shape, found shape, and rule.\n\n## Run\n\n```beskid\ni32 Main() {\n  return 42;\n}\n```\n\nReference-only: diagnostic wording and source locations are compiler-version evidence, so this lesson does not freeze a made-up error transcript. In an interactive package, run the command named in that package and preserve the observed message when reporting a real issue.\n\n## Investigate\n\nRead feedback in this order:\n\n1. **Location** — open the primary marked span and enough surrounding code to see its role.\n2. **Phase** — decide whether the issue is syntax/parsing, name resolution, type compatibility, or a later command boundary.\n3. **Expectation** — state what the position promised, such as a declared type, known name, or closing delimiter.\n4. **Observed code** — state what the compiler actually saw.\n5. **Smallest cause** — identify the earliest edit that could make expectation and observation agree.\n\nOne early parse problem can cascade into noisy later messages. Conversely, a well-formed parse can still contain a name that resolves nowhere or a value that violates a type promise. The phase changes the repair strategy.\n\n## Modify\n\nTranslate three hypothetical messages into plain language:\n\n- missing delimiter → the program's written shape cannot be parsed;\n- unknown identifier → this use has no declaration in scope;\n- incompatible return → the expression does not meet the function's promised output.\n\nFor each, write one narrow repair and one tempting but unrelated edit you will avoid.\n\n## Make and retrieve\n\nMake a diagnostic note with five headings: *location, phase, expected, found, smallest repair*. Now cover the lesson and classify those three hypothetical messages from memory. On the next real error, fill in the note before changing source.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nDo not fix the last message first when an earlier parse diagnostic remains. Do not “solve” an unknown name by renaming unrelated declarations. Do not widen a public type merely to quiet one incompatible value. Fix the first causal mismatch, rerun the same command, and only then interpret remaining diagnostics.\n\n## Recap and next link\n\nDiagnostics become useful when you locate the code, name the phase, and repair one causal mismatch. With that habit established, the foundations lessons introduce typed values, bindings, expressions, and functions.\n",
		"questions": [
			{
				"id": "orientation_diagnostics_q1",
				"text": "Which diagnostic should you usually investigate first?",
				"options": [
					"The earliest actionable diagnostic",
					"The message with the most unfamiliar words"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "foundations_main",
		"title": "Main and exit codes",
		"objective": "Make a valid entry point and explain what its result communicates.",
		"slug": "01-main-and-exit-codes",
		"starterCode": "i32 Main() {\n  return 1;\n}",
		"command": "analyze",
		"mode": "interactive",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/01-foundations/01-main-and-exit-codes/lesson.md",
		"difficulty": "beginner",
		"category": "foundations",
		"prerequisites": [],
		"detailedContent": "\n## Hook and goal\n\nA program begins at a named, typed doorway. Explain `Main`, its return promise, and the host-facing result.\n\n## Predict\n\nPredict which token chooses the entry point and whether `return 0` is a command or a value.\n\n## Run\n\n```beskid\ni32 Main() {\n  return 0;\n}\n```\n\nReference-only until this lesson has a current compiler fixture. Compare the active entry-point specification with the existing Learn smoke example before promoting source to interactive.\n\n## Investigate\n\n`Main` is an entrypoint contract; its signature states the shape of completion. A return expression must meet the declared return type. Diagnostics are evidence about a marked source location and rule.\n\n## Modify\n\nChange the intended exit value on paper, then state what has not changed: entrypoint identity and return type.\n\n## Make and retrieve\n\nWrite a launch card: entrypoint, return type, success result. From memory, explain why the type belongs in the signature.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nDo not guess at a marked token. Separate an entrypoint-name problem from an incompatible-return problem, then fix the earliest diagnostic.\n\n## Recap and next link\n\nTyped entrypoints make program boundaries explicit. Next, distinguish written literals from their types.\n",
		"questions": [
			{
				"id": "foundations_main_q1",
				"text": "Which line supplies the value promised by an i32 Main signature?",
				"options": [
					"The return expression",
					"The closing brace"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "foundations_values",
		"title": "Values, literals, and types",
		"objective": "Explain literals as values and types as operation contracts.",
		"slug": "02-values-literals-and-types",
		"starterCode": "i32 Main() {\n  let name = 41;\n  if name == 42 {\n    return 0;\n  }\n  return 1;\n}",
		"command": "analyze",
		"mode": "interactive",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/01-foundations/02-values-literals-and-types/lesson.md",
		"difficulty": "beginner",
		"category": "foundations",
		"prerequisites": [
			"foundations_main"
		],
		"detailedContent": "\n## Hook and goal\n\nAn inventory label prevents a map, a key, and a coin being confused. Learn the difference between a written literal and its promised shape.\n\n## Predict\n\nIn `i32 score = 7`, predict which part is a value, which is a name, and which is a type.\n\n## Run\n\n```beskid\ni32 Main() {\n  let name = 42;\n  return 0;\n}\n```\n\nReference-only while literals and declaration syntax are reverified against the active parser.\n\n## Investigate\n\nA literal writes an immediate value. A type describes permitted operations and required shapes at a boundary. Explicit types make callers' expectations visible.\n\n## Modify\n\nClassify three planned values as integer, Boolean, or text, then state one operation each category should not silently accept.\n\n## Make and retrieve\n\nMake a source/value/type table. From memory, finish: “a literal is written; a type is …”.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nWhen types conflict, first write expected and found. An annotation cannot make an incompatible value mean something else.\n\n## Recap and next link\n\nValues have shapes; names keep them available.\n",
		"questions": [
			{
				"id": "foundations_values_q1",
				"text": "In let name = 42, what is 42?",
				"options": [
					"An integer literal bound to name",
					"The name of the variable"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "foundations_bindings",
		"title": "Names and immutability",
		"objective": "Trace a typed binding from declaration to use.",
		"slug": "03-names-and-immutability",
		"starterCode": "i32 Main() {\n  let score = 3;\n  if score == 4 {\n    return 0;\n  }\n  return 1;\n}",
		"command": "analyze",
		"mode": "interactive",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/01-foundations/03-names-and-immutability/lesson.md",
		"difficulty": "beginner",
		"category": "foundations",
		"prerequisites": [
			"foundations_values"
		],
		"detailedContent": "\n## Hook and goal\n\nNames are labels on boxes, not the values inside. Track where a value enters view and where it may be used.\n\n## Predict\n\nPredict whether `return score` returns the spelling `score` or the value bound to that name.\n\n## Run\n\n```beskid\ni32 Main() {\n  let score = 4;\n  return score;\n}\n```\n\nReference-only until a compiler fixture confirms the exact binding and mutability forms.\n\n## Investigate\n\nA declaration introduces an identifier and a type. Scope bounds where the identifier resolves. Immutability makes each use easier to reason from its declaration.\n\n## Modify\n\nDraw declaration → use → return for a quest score; mark the scope boundary.\n\n## Make and retrieve\n\nMake a name trail. From memory, name the three checks for an unknown identifier: spelling, scope, declaration.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nDo not change annotations for an unknown name. Resolution needs a declaration in scope.\n\n## Recap and next link\n\nBindings make data readable. Next, compute with expressions.\n",
		"questions": [
			{
				"id": "foundations_bindings_q1",
				"text": "What must exist before score can be used in a condition?",
				"options": [
					"A declaration of score in scope",
					"A second return statement"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "foundations_expressions",
		"title": "Operators and expressions",
		"objective": "Evaluate a small expression and its type before running a tool.",
		"slug": "04-operators-and-expressions",
		"starterCode": "i32 Main() {\n  i32 first = 1;\n  i32 second = 1;\n  if first + second == 3 {\n    return 0;\n  }\n  return 1;\n}",
		"command": "analyze",
		"mode": "interactive",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/01-foundations/04-operators-and-expressions/lesson.md",
		"difficulty": "beginner",
		"category": "foundations",
		"prerequisites": [
			"foundations_bindings"
		],
		"detailedContent": "\n## Hook and goal\n\nExpressions are workshop stations: each takes values and produces a value. Learn to trace the stations before asking the compiler.\n\n## Predict\n\nFor a nested calculation, predict each inner result and its type.\n\n## Run\n\n```beskid\ni32 Main() {\n  return 1 + 2;\n}\n```\n\nReference-only until the precise operator surface is compiler-verified for this curriculum.\n\n## Investigate\n\nAn expression produces a value; operators require compatible operands and produce a result type. Parentheses make intended grouping visible when precedence would distract a reader.\n\n## Modify\n\nAdd parentheses to a paper expression and say whether behavior or only readability changes.\n\n## Make and retrieve\n\nMake a trace table: subexpression, input types, result. Retrieve why an operand error may not be an operator error.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nInspect operand types before replacing an operator. Preserve intended grouping while repairing the mismatched input.\n\n## Recap and next link\n\nExpressions compute values. Next, name a computation as a function.\n",
		"questions": [
			{
				"id": "foundations_expressions_q1",
				"text": "What is evaluated before the equality comparison in first + second == 3?",
				"options": [
					"The addition first + second",
					"The return statement"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "foundations_mutation",
		"title": "Mutation and assignment",
		"objective": "Reassign a local only when its binding explicitly permits mutation.",
		"slug": "05-mutation-and-assignment",
		"starterCode": "// Reference-only: lesson.md cites the authoritative behavior and verification boundary.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict a concrete state transition.",
			"Keep unsupported syntax reference-only."
		],
		"lessonPath": "/site/learn/curriculum/01-foundations/05-mutation-and-assignment/lesson.md",
		"difficulty": "intermediate",
		"category": "foundations",
		"prerequisites": [
			"foundations_expressions"
		],
		"detailedContent": "\n## Hook and goal\n\nThis lesson turns one hidden control decision into an explicit, reviewable rule.\n\n## Predict\n\nState what happens before running the example, including the value or control path that changes.\n\n## Run\n\nReference-only: the active Learn compiler fixture is unavailable, so this source illustrates the cited contract rather than claiming interactive acceptance.\n\n```beskid\nlet mut total = 0;\ntotal = total + 5;\n```\n\nThe first line declares a mutable inferred binding. The second computes a replacement value and assigns it to that binding.\n\n## Investigate\n\nRead the cited normative source first. Identify the declaration or condition, the state it governs, and the boundary it protects.\n\n## Modify\n\nMake one semantic change to the scenario and explain which invariant or control path changes; do not merely rename an identifier.\n\n## Make and retrieve\n\nWrite a one-sentence rule and apply it to a fresh quest-log scenario without looking back at this lesson.\n\n## Failure clinic\n\nBroken code:\n\n```beskid\nlet total = 0;\ntotal = total + 1;\n```\n\nObserved condition: E1214: assignment targets an immutable binding.\nRepair: Declare let mut total only when replacement is intentional; otherwise bind a new value.\n\n## Recap and next link\n\nThe rule is useful only when its trigger, affected state, and repair boundary are explicit.\n",
		"questions": [
			{
				"id": "foundations_mutation_q1",
				"text": "Which declaration permits later reassignment of total?",
				"options": [
					"let mut total = 0",
					"let total = 0"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "flow_functions",
		"title": "Functions and contracts",
		"objective": "Read a function as an explicit input-output promise.",
		"slug": "01-functions-and-contracts",
		"starterCode": "i32 Add(i32 a, i32 b) {\n  return a;\n}\n\ni32 Main() {\n  i32 sum = Add(1, 2);\n  if sum == 3 { return 0; }\n  return 1;\n}",
		"command": "analyze",
		"mode": "interactive",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/02-control-flow/01-functions-and-contracts/lesson.md",
		"difficulty": "intermediate",
		"category": "control-flow",
		"prerequisites": [
			"foundations_expressions"
		],
		"detailedContent": "\n## Hook and goal\n\nA function is a trailhead sign: it says what supplies you bring and what outcome comes back.\n\n## Predict\n\nPredict how arguments at a call correspond to parameters in a two-input function.\n\n## Run\n\n```beskid\ni32 Add(i32 a, i32 b) {\n  return a + b;\n}\n```\n\nReference-only until this package has a current verified function fixture.\n\n## Investigate\n\nParameters are inputs owned by a function body; arguments are values supplied by a caller. The signature publishes input and output shapes, and each reachable return must honor the output promise.\n\n## Modify\n\nWrite a one-responsibility contract for `ScoreBonus`: inputs, output, and one invariant.\n\n## Make and retrieve\n\nMake a function card. From memory, distinguish a parameter from an argument.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nFor an arity error, count positions. For a type error, align each argument with its corresponding parameter.\n\n## Recap and next link\n\nFunctions package a computation. Next, select behavior with a Boolean choice.\n",
		"questions": [
			{
				"id": "flow_functions_q1",
				"text": "Which names belong to the Add function body rather than the caller?",
				"options": [
					"Parameters a and b",
					"Arguments 1 and 2"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "flow_choices",
		"title": "Choices with if",
		"objective": "Trace both Boolean paths and account for every return.",
		"slug": "02-choices-with-if",
		"starterCode": "i32 IsEven(i32 n) {\n  if n % 2 == 0 { return 1; }\n  return 0;\n}\n\ni32 Main() { return IsEven(4); }",
		"command": "analyze",
		"mode": "interactive",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/02-control-flow/02-choices-with-if/lesson.md",
		"difficulty": "intermediate",
		"category": "control-flow",
		"prerequisites": [
			"flow_functions"
		],
		"detailedContent": "\n## Hook and goal\n\nAt a trail fork, a condition chooses one route. Understand both routes before walking either.\n\n## Predict\n\nPredict the return for one true input and one false input.\n\n## Run\n\n```beskid\ni32 IsEven(i32 n) {\n  if n % 2 == 0 { return 1; }\n  return 0;\n}\n```\n\nReference-only while the current `if` grammar and diagnostics are captured for this package.\n\n## Investigate\n\nA Boolean condition selects a branch. A typed function still promises one compatible result for every reachable route; one happy-path test does not explain the false path.\n\n## Modify\n\nMake a two-row branch table: condition outcome and resulting action.\n\n## Make and retrieve\n\nExplain from memory why testing both rows matters.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nDo not add a branch just to silence a missing-return error; decide the intended behavior for the unaccounted route.\n\n## Recap and next link\n\nChoices make paths visible. Next, repeat a path only with an invariant.\n",
		"questions": [
			{
				"id": "flow_choices_q1",
				"text": "Which input reaches the true branch of n % 2 == 0?",
				"options": [
					"An even integer such as 4",
					"An odd integer such as 3"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "flow_repetition",
		"title": "Repetition with loops",
		"objective": "Design repeated work using an invariant and termination measure.",
		"slug": "03-repetition-with-loops",
		"starterCode": "// Reference-only lesson: see lesson.md and source cited in front matter.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/02-control-flow/03-repetition-with-loops/lesson.md",
		"difficulty": "intermediate",
		"category": "control-flow",
		"prerequisites": [
			"flow_choices"
		],
		"detailedContent": "\n## Hook and goal\n\nA repeated hike needs a start, a marker that advances, and a finish. Design those before choosing syntax.\n\n## Predict\n\nFor a three-score total, predict initial sum, each intermediate sum, and the stopping condition.\n\n## Run\n\n```beskid\n// Trace plan: start sum at 0; visit one score; update sum.\n```\n\nReference-only: loop source forms await parser-backed verification.\n\n## Investigate\n\nAn invariant remains true before and after each iteration. An accumulator carries the partial result; a termination measure moves toward a bound.\n\n## Modify\n\nRepair a paper loop whose counter never changes and state its intended invariant.\n\n## Make and retrieve\n\nMake an initialize/test/update/result card. Retrieve all four labels.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nNever flip a comparison at random. Identify initialization, boundary, progress, or accumulation as the failed rule.\n\n## Recap and next link\n\nLoops are controlled state transitions. Next, retrieve the flow rules without a new construct.\n",
		"questions": [
			{
				"id": "flow_repetition_q1",
				"text": "What must change to show that a loop can terminate?",
				"options": [
					"A measure that progresses toward its bound",
					"Only the loop's final result"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "flow_retrieval",
		"title": "Control-flow retrieval",
		"objective": "Consolidate functions, choices, and repetition through prediction.",
		"slug": "04-control-flow-retrieval",
		"starterCode": "// Reference-only lesson: see lesson.md and source cited in front matter.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/02-control-flow/04-control-flow-retrieval/lesson.md",
		"difficulty": "intermediate",
		"category": "control-flow",
		"prerequisites": [
			"flow_repetition"
		],
		"detailedContent": "\n## Hook and goal\n\nA dungeon rehearsal works only if you can navigate without the map. Retrieve the control-flow rules before adding data structures.\n\n## Predict\n\nPredict a trace for a function that chooses then repeats; name the first three state changes.\n\n## Run\n\n```beskid\ni32 choose(i32 score) {\n  if score == 3 { return 0; }\n  return 1;\n}\n```\n\nReference-only synthesis: use paper traces and the governing sources rather than unverified code.\n\n## Investigate\n\nFunctions establish contracts, choices select a path, and loops repeat a transition under an invariant. A trace records values at each boundary and exposes missing paths or broken bounds.\n\n## Modify\n\nCreate a quest-score algorithm with one helper, one condition, and one repetition plan.\n\n## Make and retrieve\n\nFrom memory, explain argument/parameter, true/false paths, and loop invariant to a teammate.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nWhen a trace fails, repair the earliest boundary where expected and observed state differ.\n\n## Recap and next link\n\nYou can now control a computation. Next, model its data.\n",
		"questions": [
			{
				"id": "flow_retrieval_q1",
				"text": "What does a loop invariant connect?",
				"options": [
					"Carried state to the already visited work",
					"A function name to its file path"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "flow_booleans",
		"title": "Boolean logic and short-circuiting",
		"objective": "Trace compound conditions and identify when a right-hand test is skipped.",
		"slug": "05-boolean-logic-and-short-circuiting",
		"starterCode": "// Reference-only: lesson.md cites the authoritative behavior and verification boundary.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict a concrete state transition.",
			"Keep unsupported syntax reference-only."
		],
		"lessonPath": "/site/learn/curriculum/02-control-flow/05-boolean-logic-and-short-circuiting/lesson.md",
		"difficulty": "intermediate",
		"category": "control-flow",
		"prerequisites": [
			"flow_retrieval"
		],
		"detailedContent": "\n## Hook and goal\n\nThis lesson turns one hidden control decision into an explicit, reviewable rule.\n\n## Predict\n\nState what happens before running the example, including the value or control path that changes.\n\n## Run\n\nReference-only: the active Learn compiler fixture is unavailable, so this source illustrates the cited contract rather than claiming interactive acceptance.\n\n```beskid\nif hasKey && doorIsLocked {\n  return 0;\n}\n```\n\nThe left condition is evaluated first. The right condition can matter only if the left condition leaves an AND result possible. The body runs only for a true complete condition.\n\n## Investigate\n\nRead the cited normative source first. Identify the declaration or condition, the state it governs, and the boundary it protects.\n\n## Modify\n\nMake one semantic change to the scenario and explain which invariant or control path changes; do not merely rename an identifier.\n\n## Make and retrieve\n\nWrite a one-sentence rule and apply it to a fresh quest-log scenario without looking back at this lesson.\n\n## Failure clinic\n\nBroken code:\n\n```beskid\nif chestIsUnlocked && chestExists {\n  return 0;\n}\n```\n\nObserved condition: The intended existence guard is evaluated too late.\nRepair: Place chestExists first, then verify the exact operator grammar in the cited specification.\n\n## Recap and next link\n\nThe rule is useful only when its trigger, affected state, and repair boundary are explicit.\n",
		"questions": [
			{
				"id": "flow_booleans_q1",
				"text": "When the left side of AND is false, what happens to the right side?",
				"options": [
					"It is skipped because the result is already false",
					"It must run to find a new result"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "flow_loop_exits",
		"title": "Break and continue make loop exits visible",
		"objective": "Predict whether a loop stops or skips the remaining work of one iteration.",
		"slug": "06-break-and-continue",
		"starterCode": "// Reference-only: lesson.md cites the authoritative behavior and verification boundary.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict a concrete state transition.",
			"Keep unsupported syntax reference-only."
		],
		"lessonPath": "/site/learn/curriculum/02-control-flow/06-break-and-continue/lesson.md",
		"difficulty": "intermediate",
		"category": "control-flow",
		"prerequisites": [
			"flow_booleans"
		],
		"detailedContent": "\n## Hook and goal\n\nThis lesson turns one hidden control decision into an explicit, reviewable rule.\n\n## Predict\n\nState what happens before running the example, including the value or control path that changes.\n\n## Run\n\nReference-only: the active Learn compiler fixture is unavailable, so this source illustrates the cited contract rather than claiming interactive acceptance.\n\n```beskid\nif entryIsDamaged {\n  continue;\n}\nif entryIsTreasure {\n  break;\n}\n```\n\nThe first branch skips only the remaining current body. The second abandons the enclosing loop. Neither replaces a loop's ordinary termination condition.\n\n## Investigate\n\nRead the cited normative source first. Identify the declaration or condition, the state it governs, and the boundary it protects.\n\n## Modify\n\nMake one semantic change to the scenario and explain which invariant or control path changes; do not merely rename an identifier.\n\n## Make and retrieve\n\nWrite a one-sentence rule and apply it to a fresh quest-log scenario without looking back at this lesson.\n\n## Failure clinic\n\nBroken code:\n\n```beskid\nif entryIsTreasure {\n  continue;\n}\nreturn foundTreasure;\n```\n\nObserved condition: Continue skips the return on the treasure iteration.\nRepair: Record the treasure and break when finding it should end the search.\n\n## Recap and next link\n\nThe rule is useful only when its trigger, affected state, and repair boundary are explicit.\n",
		"questions": [
			{
				"id": "flow_loop_exits_q1",
				"text": "What remains after continue executes?",
				"options": [
					"The rest of this iteration is skipped",
					"The whole loop ends"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "data_structs",
		"title": "Structs and fields",
		"objective": "Group related facts under one identity and state its invariant.",
		"slug": "01-structs-and-fields",
		"starterCode": "// Reference-only lesson: see lesson.md and source cited in front matter.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/03-data-modeling/01-structs-and-fields/lesson.md",
		"difficulty": "intermediate",
		"category": "data-modeling",
		"prerequisites": [
			"flow_retrieval"
		],
		"detailedContent": "\n## Hook and goal\n\nA character sheet holds facts about one character, not every fact in the game. Model identity before fields.\n\n## Predict\n\nPredict whether name, health, and location belong to one entity or unrelated locals.\n\n## Run\n\n```beskid\n// Source-shaped model; verify declaration syntax before compiling.\nstruct Quest { i32 reward; }\n```\n\nReference-only until active compiler fixtures establish struct declaration and construction syntax.\n\n## Investigate\n\nA struct gives a named shape to related fields. Its invariant describes meaningful combinations; fields should share identity and lifecycle, not merely be convenient to store together.\n\n## Modify\n\nSketch a `Quest` record with required, optional, and derived facts.\n\n## Make and retrieve\n\nMake a record card: entity, fields with types, invariant. Retrieve why it is more than a large variable.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nIf a record becomes a junk drawer, split by identity or responsibility; do not duplicate facts that can disagree.\n\n## Recap and next link\n\nStructs hold related facts. Next, give a typed receiver behavior.\n",
		"questions": [
			{
				"id": "data_structs_q1",
				"text": "What makes two fields belong in the same struct?",
				"options": [
					"They describe one identity and lifecycle",
					"They happen to share a primitive type"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "data_methods",
		"title": "Methods and receivers",
		"objective": "Resolve a method call from the receiver's static type.",
		"slug": "02-methods-and-receivers",
		"starterCode": "// Reference-only lesson: see lesson.md and source cited in front matter.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/03-data-modeling/02-methods-and-receivers/lesson.md",
		"difficulty": "intermediate",
		"category": "data-modeling",
		"prerequisites": [
			"data_structs"
		],
		"detailedContent": "\n## Hook and goal\n\nAsking a lantern to `Light()` works only if its known type publishes that ability. Start resolution at the receiver, not a wish.\n\n## Predict\n\nPredict the facts needed for `receiver.method(args)`: receiver type, member name, and argument shapes.\n\n## Run\n\n```beskid\n// Source-shaped call; verify declarations in the cited dispatch specification.\nquest.Complete()\n```\n\nReference-only while example syntax is verified. The governing rule requires a matching member on the receiver's static type.\n\n## Investigate\n\nMethod calls use postfix member access. Resolution matches member name, arity, and argument types; v0.1 dispatch is static. New extensions use `extend type`.\n\n## Modify\n\nWrite a method contract: receiver, inputs, output, invariant preserved.\n\n## Make and retrieve\n\nMake a dispatch checklist: static type, member set, signature, result. Retrieve it without notes.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nAn unknown member is not fixed by a runtime branch. Inspect the static receiver type and its declared members.\n\n## Recap and next link\n\nMethods organize behavior around data. Next, enumerate mutually exclusive states.\n",
		"questions": [
			{
				"id": "data_methods_q1",
				"text": "What does static dispatch inspect first for receiver.method()?",
				"options": [
					"The receiver's static type",
					"A guessed runtime branch"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "data_enums",
		"title": "Enums for states",
		"objective": "Model a finite state space with named alternatives.",
		"slug": "03-enums-for-states",
		"starterCode": "// Reference-only lesson: see lesson.md and source cited in front matter.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/03-data-modeling/03-enums-for-states/lesson.md",
		"difficulty": "intermediate",
		"category": "data-modeling",
		"prerequisites": [
			"data_methods"
		],
		"detailedContent": "\n## Hook and goal\n\nA quest cannot be simultaneously not started and completed in one well-formed state. Name the alternatives.\n\n## Predict\n\nCompare two Boolean flags with three named states; predict which permits contradiction.\n\n## Run\n\n```beskid\n// Canonical Result variants from the cited core specification.\nResult<i32, string> outcome = Ok(7);\n```\n\nReference-only while enum construction syntax receives an active compiler fixture. `Result<T,E>` is a normative enum with `Ok(T)` and `Error(E)`.\n\n## Investigate\n\nAn enum selects one variant from a finite set. A variant may carry only data relevant to that state, making invalid combinations harder to express.\n\n## Modify\n\nModel delivery as named states and choose the smallest useful payload for each.\n\n## Make and retrieve\n\nMake a state table: variant, data, transition in/out. Retrieve why Boolean flags drift.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nDo not add a vague catch-all state to avoid modeling a real case; describe the information it carries.\n\n## Recap and next link\n\nEnums name possibilities. Next, handle every possibility deliberately.\n",
		"questions": [
			{
				"id": "data_enums_q1",
				"text": "Why use enum variants instead of two independent state flags?",
				"options": [
					"Variants make mutually exclusive states explicit",
					"Flags automatically carry error details"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "data_match",
		"title": "Match and exhaustiveness",
		"objective": "Plan a response for every enum alternative.",
		"slug": "04-match-and-exhaustiveness",
		"starterCode": "// Reference-only lesson: see lesson.md and source cited in front matter.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/03-data-modeling/04-match-and-exhaustiveness/lesson.md",
		"difficulty": "intermediate",
		"category": "data-modeling",
		"prerequisites": [
			"data_enums"
		],
		"detailedContent": "\n## Hook and goal\n\nAt the city gate, each kind of traveller needs a desk. A hidden queue is a bug waiting for a name.\n\n## Predict\n\nList variants and predict the response for each before drafting a case expression.\n\n## Run\n\n```beskid\n// Source-shaped case plan; current grammar must verify it before use.\nmatch state { Ready => 1, Done => 0 }\n```\n\nReference-only until `match` grammar and exhaustiveness diagnostics are captured in this curriculum.\n\n## Investigate\n\nMatching relates a pattern to a case. Exhaustiveness makes every declared alternative visible and turns a new variant into an actionable prompt at each decision site.\n\n## Modify\n\nAdd a state to a paper enum and update every response row.\n\n## Make and retrieve\n\nMake a match plan: input, variants, arm result, default policy. Retrieve why wildcards can hide future work.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nFor a missing case, choose behavior rather than adding a wildcard merely to remove feedback.\n\n## Recap and next link\n\nExhaustive matches keep state models honest. Next, retrieve collections with an explicit bounds story.\n",
		"questions": [
			{
				"id": "data_match_q1",
				"text": "What useful prompt appears when an enum gains a new variant?",
				"options": [
					"Every incomplete match must decide its behavior",
					"All old matches silently choose a default"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "data_collections",
		"title": "Arrays and collections",
		"objective": "Reason about ordered elements, indexes, and bounds.",
		"slug": "05-arrays-and-collections",
		"starterCode": "// Reference-only lesson: see lesson.md and source cited in front matter.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict before checking.",
			"Use the named source; do not infer syntax from another language."
		],
		"lessonPath": "/site/learn/curriculum/03-data-modeling/05-arrays-and-collections/lesson.md",
		"difficulty": "intermediate",
		"category": "data-modeling",
		"prerequisites": [
			"data_match"
		],
		"detailedContent": "\n## Hook and goal\n\nAn array is a numbered shelf: ordered slots, finite length, and an edge you must not pretend is another slot.\n\n## Predict\n\nFor a three-item shelf, list valid positions and the first invalid position.\n\n## Run\n\n```beskid\n// The cited API models a missing indexed value explicitly.\nOption<i32> score = quests.Get(3);\n```\n\nReference-only until collection literals, indexing, and iteration forms are compiler-verified. The core indexed API documents `Get(i64 index) -> Option<T>` for ordinary absence.\n\n## Investigate\n\nA collection has an element type, order, and bounds. An index selects a position, not an element; safe access needs a documented absent-case policy.\n\n## Modify\n\nTrace a five-slot quest log and label length, valid positions, and one absent lookup.\n\n## Make and retrieve\n\nMake a bounds sentence: valid when ___; otherwise ___. Retrieve why zero is not a universal missing sentinel.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\nDo not “fix” an out-of-range request by inventing a value. Preserve the difference between absent and present-zero.\n\n## Recap and next link\n\nCollections close this data-modeling context. Next, organize those models across modules and packages.\n",
		"questions": [
			{
				"id": "data_collections_q1",
				"text": "What does an index identify in a collection?",
				"options": [
					"A position within its bounds",
					"The value stored at every position"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "data_events",
		"title": "Events and subscribers",
		"objective": "Describe an event trigger, owner, and subscriber responsibility without assuming delivery order.",
		"slug": "06-events-and-subscribers",
		"starterCode": "// Reference-only: lesson.md cites the authoritative behavior and verification boundary.",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Predict a concrete state transition.",
			"Keep unsupported syntax reference-only."
		],
		"lessonPath": "/site/learn/curriculum/03-data-modeling/06-events-and-subscribers/lesson.md",
		"difficulty": "intermediate",
		"category": "data-modeling",
		"prerequisites": [
			"data_collections"
		],
		"detailedContent": "\n## Hook and goal\n\nThis lesson turns one hidden control decision into an explicit, reviewable rule.\n\n## Predict\n\nState what happens before running the example, including the value or control path that changes.\n\n## Run\n\nReference-only: the active Learn compiler fixture is unavailable, so this source illustrates the cited contract rather than claiming interactive acceptance.\n\n```beskid\nevent OnCancelled();\n// Fiber<T> owns the cancellation notification.\n```\n\nThe event declaration belongs to the Fiber handle. The cited concurrency contract raises it on the child before parked operations unblock; it does not promise arbitrary subscriber timing.\n\n## Investigate\n\nRead the cited normative source first. Identify the declaration or condition, the state it governs, and the boundary it protects.\n\n## Modify\n\nMake one semantic change to the scenario and explain which invariant or control path changes; do not merely rename an identifier.\n\n## Make and retrieve\n\nWrite a one-sentence rule and apply it to a fresh quest-log scenario without looking back at this lesson.\n\n## Failure clinic\n\nBroken code:\n\n```beskid\nevent OnCancelled();\nreturn 0;\n```\n\nObserved condition: An event declaration alone does not perform cancellation or deliver a result.\nRepair: Specify the triggering state transition and handle cancellation through the documented Fiber API.\n\n## Recap and next link\n\nThe rule is useful only when its trigger, affected state, and repair boundary are explicit.\n",
		"questions": [
			{
				"id": "data_events_q1",
				"text": "Who owns the OnCancelled event in the fiber model?",
				"options": [
					"The Fiber handle",
					"An arbitrary spawn closure"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "corelib_discovery",
		"title": "Find batteries without guessing",
		"objective": "Navigate corelib as a resolved dependency and distinguish public APIs from runtime internals.",
		"slug": "01-discovering-corelib-and-apis",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Corelib is a resolved public toolkit, not a private intrinsic catalog."
		],
		"lessonPath": "/site/learn/curriculum/08-core-library/01-discovering-corelib-and-apis/lesson.md",
		"difficulty": "intermediate",
		"category": "core-library",
		"prerequisites": [
			"structure_projects"
		],
		"detailedContent": "\n## Hook and goal\n\nNavigate corelib as a resolved dependency and distinguish public APIs from runtime internals. Corelib is a resolved public toolkit, not a private intrinsic catalog.\n\n## Predict\n\nPredict why a public wrapper is a better application dependency than an internal double-underscore operation.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Public corelib surface, resolved by the host project.\nuse Concurrency.Channel;\n```\n\n## Investigate\n\nReference-only: corelib availability is project-scoped. Inspect resolved packages rather than treating a scratch file as proof.\n\n## Modify\n\nChoose a need—console, time, results, or concurrency—and identify the public package area first.\n\n## Make and retrieve\n\nMake a lookup route and retrieve why intrinsics are not your first API.\n\n## Failure clinic\n\n```beskid\n// Bad application boundary: private runtime intrinsic instead of public API.\n__channel_send(0, 42);\n```\n\nDo not disable corelib or paste private declarations to repair resolution.\n\n## Recap and next link\n\nCorelib offers public tools. Next: learn to read an API signature.\n",
		"questions": [
			{
				"id": "corelib_discovery_q1",
				"text": "Predict why a public wrapper is a better application dependency than an internal double-underscore operation.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "corelib_api_docs",
		"title": "Read the promise before the implementation",
		"objective": "Use signatures and documentation to predict a corelib call’s inputs, output, and failure modes.",
		"slug": "02-reading-api-docs-and-signatures",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"A signature is a compact contract; docs add purpose and edge cases."
		],
		"lessonPath": "/site/learn/curriculum/08-core-library/02-reading-api-docs-and-signatures/lesson.md",
		"difficulty": "intermediate",
		"category": "core-library",
		"prerequisites": [
			"corelib_discovery"
		],
		"detailedContent": "\n## Hook and goal\n\nUse signatures and documentation to predict a corelib call’s inputs, output, and failure modes. A signature is a compact contract; docs add purpose and edge cases.\n\n## Predict\n\nPredict which facts are known from Result<T, E> before reading implementation source.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Read receiver, success payload, and error payload before implementation.\nCore.Results.Result<T, ChannelError> Receive<T>(Channel<T> self)\n```\n\n## Investigate\n\nReference-only: inspect public package source and governing specification before creating a runnable import lesson.\n\n## Modify\n\nAnnotate a fictional signature with parameter role, return shape, ownership, and failure path.\n\n## Make and retrieve\n\nMake a signature-reading checklist and retrieve it.\n\n## Failure clinic\n\n```beskid\n// Bad assumption: Receive can fail on closed or cancelled state.\nT value = channel.Receive();\n```\n\nDo not infer undocumented behavior from a convenient implementation detail.\n\n## Recap and next link\n\nRead promises before machinery. Next: choose practical collection shapes.\n",
		"questions": [
			{
				"id": "corelib_api_docs_q1",
				"text": "Predict which facts are known from Result<T, E> before reading implementation source.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "corelib_collections",
		"title": "Choose a shape for your data",
		"objective": "Choose arrays and optional presence without hidden sentinel conventions.",
		"slug": "03-practical-collections",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Arrays, empty values, and Option-style absence are different states."
		],
		"lessonPath": "/site/learn/curriculum/08-core-library/03-practical-collections/lesson.md",
		"difficulty": "intermediate",
		"category": "core-library",
		"prerequisites": [
			"corelib_api_docs"
		],
		"detailedContent": "\n## Hook and goal\n\nChoose arrays and optional presence without hidden sentinel conventions. Arrays, empty values, and Option-style absence are different states.\n\n## Predict\n\nPredict what information is lost when -1 means missing and is also a possible value.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// An array and an optional score describe different shapes.\ni32[] scores = [3, 5, 8];\nOption<i32> maybeScore = Option::None;\n```\n\n## Investigate\n\nNormative-pending: establish package and compiler support before an interactive collection exercise. The type model has T[] and no null literal.\n\n## Modify\n\nRedesign FindQuest so success data and absence are represented separately.\n\n## Make and retrieve\n\nMake a three-row table for empty collection, empty string, and absence. Retrieve it.\n\n## Failure clinic\n\n```beskid\n// Bad sentinel: -1 may be a valid score.\ni32 FindScore(i32 questId) { return -1; }\n```\n\nDo not use empty text as a universal missing marker.\n\n## Recap and next link\n\nData shapes keep absence visible. Next: make conversion and formatting boundaries explicit.\n",
		"questions": [
			{
				"id": "corelib_collections_q1",
				"text": "Predict what information is lost when -1 means missing and is also a possible value.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "corelib_conversions",
		"title": "Convert with a receipt",
		"objective": "Treat conversions and formatting as explicit boundary operations with specified failure behavior.",
		"slug": "04-conversions-and-formatting",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Conversions should preserve or report the information they cannot preserve."
		],
		"lessonPath": "/site/learn/curriculum/08-core-library/04-conversions-and-formatting/lesson.md",
		"difficulty": "intermediate",
		"category": "core-library",
		"prerequisites": [
			"corelib_collections"
		],
		"detailedContent": "\n## Hook and goal\n\nTreat conversions and formatting as explicit boundary operations with specified failure behavior. Conversions should preserve or report the information they cannot preserve.\n\n## Predict\n\nPredict whether formatting a number and parsing text back are automatically inverse operations.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\nstring source = \"42\";\n// A documented parser must state its invalid-input outcome.\n```\n\n## Investigate\n\nReference-only: use documented conversion APIs only after their exact corelib surface is verified for the target.\n\n## Modify\n\nFor an input field, list source representation, target representation, invalid-input outcome, and presentation format.\n\n## Make and retrieve\n\nMake a conversion receipt and retrieve its four fields.\n\n## Failure clinic\n\n```beskid\n// Bad conversion policy: malformed text becomes a plausible score.\ni32 ParseScore(string text) { return 0; }\n```\n\nDo not silently coerce an invalid value into a plausible default.\n\n## Recap and next link\n\nExplicit conversion protects meaning. Next: inspect advanced surfaces with the same discipline.\n",
		"questions": [
			{
				"id": "corelib_conversions_q1",
				"text": "Predict whether formatting a number and parsing text back are automatically inverse operations.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "structure_modules",
		"title": "Give code an address",
		"objective": "Explain how modules and use declarations give names an unambiguous origin.",
		"slug": "01-modules-and-use",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"A module owns declarations; use brings a deliberate path into local scope."
		],
		"lessonPath": "/site/learn/curriculum/04-program-structure/01-modules-and-use/lesson.md",
		"difficulty": "intermediate",
		"category": "program-structure",
		"prerequisites": [],
		"detailedContent": "\n## Hook and goal\n\nExplain how modules and use declarations give names an unambiguous origin. A module owns declarations; use brings a deliberate path into local scope.\n\n## Predict\n\nPredict why a bare name is hard to review without an owning path.\n\n## Run\n\n```beskid\n// Module and use form from the cited program-structure specification.\nuse Core.Console as Console;\n```\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Module declarations and use paths are resolved in project context.\nuse Concurrency.Channel;\n```\n\n## Investigate\n\nModule declarations establish namespaces. `use Path as Name;` resolves an explicit path; it is not textual copy-and-paste.\n\n## Modify\n\nMap Console.WriteLine and Concurrency.Channel to their owning paths.\n\n## Make and retrieve\n\nMake an import policy, then retrieve the difference between a module and a use declaration.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\n```beskid\n// Bad: the name has no declaration or imported path in this unit.\nChannel.Create<i32>();\n```\n\nAn unknown name calls for checking declaration, path, visibility, then project resolution—not inventing a spelling.\n\n## Recap and next link\n\nModules give names addresses. Next: decide which addresses are public promises.\n",
		"questions": [
			{
				"id": "structure_modules_q1",
				"text": "What does a use declaration contribute to local code?",
				"options": [
					"A deliberate path-based name",
					"A copy of another module's source"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "structure_visibility",
		"title": "Draw the public doorway",
		"objective": "Distinguish a public API promise from a private implementation detail.",
		"slug": "02-visibility-and-public-apis",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"A public declaration is a promise across a module boundary."
		],
		"lessonPath": "/site/learn/curriculum/04-program-structure/02-visibility-and-public-apis/lesson.md",
		"difficulty": "intermediate",
		"category": "program-structure",
		"prerequisites": [
			"structure_modules"
		],
		"detailedContent": "\n## Hook and goal\n\nDistinguish a public API promise from a private implementation detail. A public declaration is a promise across a module boundary.\n\n## Predict\n\nPredict whether changing a public parameter or a private helper is more likely to affect another module.\n\n## Run\n\n```beskid\n// A public signature is a consumer-facing promise.\npub i32 Score();\n```\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\npub type QuestSummary {\n  pub i32 score,\n}\n```\n\n## Investigate\n\nVisibility marks what consumers may depend on. Keep public surfaces narrow and expose capabilities rather than storage details.\n\n## Modify\n\nSort Send, Receive, a queue id, and a status decoder into public counter or private workshop.\n\n## Make and retrieve\n\nMake a two-column API list and retrieve why `pub` is more than decoration.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\n```beskid\n// Bad boundary: external code reaches an implementation detail.\nsummary.internalQueue;\n```\n\nDo not expose every ancestor to cure one access error; locate the intended boundary.\n\n## Recap and next link\n\nPublic APIs are promises. Next: collect modules into a resolved project.\n",
		"questions": [
			{
				"id": "structure_visibility_q1",
				"text": "Which change is riskier for other modules?",
				"options": [
					"Changing a public parameter type",
					"Refactoring a private helper"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "structure_projects",
		"title": "Assemble a project, not a pile",
		"objective": "Explain how manifests, dependencies, corelib, analyze, build, and run fit together.",
		"slug": "03-projects-packages-and-builds",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"A project supplies dependency and corelib context that one file cannot."
		],
		"lessonPath": "/site/learn/curriculum/04-program-structure/03-projects-packages-and-builds/lesson.md",
		"difficulty": "intermediate",
		"category": "program-structure",
		"prerequisites": [
			"structure_visibility"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain how manifests, dependencies, corelib, analyze, build, and run fit together. A project supplies dependency and corelib context that one file cannot.\n\n## Predict\n\nPredict whether parse success proves that dependencies and executable tooling are ready.\n\n## Run\n\n```beskid\ni32 Main() {\n  return 0;\n}\n```\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// A project manifest resolves packages before this entrypoint is built.\ni32 Main() { return 0; }\n```\n\n## Investigate\n\nProject commands resolve manifests, locks, dependencies, and implicit corelib. Analyze checks semantics; build and run add artifact and platform obligations.\n\n## Modify\n\nDraw source to resolution to analysis to artifact to execution, placing one possible failure at each boundary.\n\n## Make and retrieve\n\nMake a project preflight checklist and retrieve why linker failure is not a type error.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\n```beskid\n// Bad project condition: an imported package is absent from the dependency graph.\nuse Missing.Package;\n```\n\nDo not paste dependencies into a source file to repair manifest resolution.\n\n## Recap and next link\n\nA project is declared context. Next: turn failures into durable evidence.\n",
		"questions": [
			{
				"id": "structure_projects_q1",
				"text": "Which stage adds an executable artifact after analysis?",
				"options": [
					"Build",
					"Name resolution"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "structure_tests",
		"title": "Turn failures into evidence",
		"objective": "Choose diagnostics or tests based on the claim being protected.",
		"slug": "04-tests-and-diagnostics",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"A diagnostic proves rejection; a test protects behavior."
		],
		"lessonPath": "/site/learn/curriculum/04-program-structure/04-tests-and-diagnostics/lesson.md",
		"difficulty": "intermediate",
		"category": "program-structure",
		"prerequisites": [
			"structure_projects"
		],
		"detailedContent": "\n## Hook and goal\n\nChoose diagnostics or tests based on the claim being protected. A diagnostic proves rejection; a test protects behavior.\n\n## Predict\n\nPredict whether an invalid return type belongs in runtime output testing or a diagnostic fixture.\n\n## Run\n\n```beskid\ni32 Main() {\n  return 0;\n}\n```\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\ntest AddReturnsSum() {\n  // A test item owns a focused behavioral claim.\n}\n```\n\n## Investigate\n\nDiagnostics describe invalid source at a phase; tests establish intended behavior for valid source. Keep a fixture small enough to explain one rule.\n\n## Modify\n\nClassify unknown type, arithmetic output, and closed-channel behavior by the evidence each needs.\n\n## Make and retrieve\n\nMake a bug card: smallest source, command, observation, expected rule. Retrieve why it is useful.\n\n## Failure clinic\n\nBroken code, used only to isolate a parser condition:\n\n```beskid\ni32 Main( {\n  return 0;\n}\n```\n\nObserved condition: parsing cannot form the parameter list after Main. Repair: write the empty parameter list as Main(), then rerun the lesson command before interpreting any follow-on messages.\n\n```beskid\n// Bad diagnostic fixture: the return violates its declared result type.\ni32 Score() { return \"high\"; }\n```\n\nDo not assert only a nonzero exit; state which rule must be rejected.\n\n## Recap and next link\n\nTests protect behavior and diagnostics protect rules. Next: model expected failure as data.\n",
		"questions": [
			{
				"id": "structure_tests_q1",
				"text": "Which evidence protects rejection of invalid source?",
				"options": [
					"A diagnostic fixture",
					"A runtime output assertion"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "reliability_option",
		"title": "Option: say when a value is absent",
		"objective": "Model a value that may be absent without using null or a sentinel value.",
		"slug": "01-option-and-absence",
		"starterCode": "// Reference-only lesson package; see lesson.md for the normative-pending Option<T> sketch.\ni32 Main() {\n  return 0;\n}",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Absence belongs in the result type.",
			"A valid zero is not automatically a missing value."
		],
		"lessonPath": "/site/learn/curriculum/05-reliability-and-abstraction/01-option-and-absence/lesson.md",
		"difficulty": "intermediate",
		"category": "reliability-and-abstraction",
		"prerequisites": [
			"corelib_discovery",
			"data_enums"
		],
		"detailedContent": "\n## Hook and goal\n\nA quest with score `0` is not the same as a quest that has never been scored. Your goal is to make that distinction visible in the return type instead of hiding it in a magic number.\n\n## Predict\n\nIf `FindScore` returns `Option<i32>`, predict what its caller must handle before it can add the score to a total.\n\n## Run\n\nThis is **reference-only** until the selected corelib package/import fixture is proven in Learn. The language rule is concrete: Beskid has no null literal or nullable type syntax; optional presence uses `Option<T>` or an explicit enum.\n\n```beskid\n// Normative-pending corelib sketch: the result carries either a score or absence.\nOption<i32> FindScore(i32 questId) {\n    return Option::None;\n}\n```\n\n## Investigate\n\n`Option<T>` models two states: present with a `T`, or absent. That forces the caller to choose behavior for each state, usually by matching the result. It is appropriate when “not found” is an expected outcome, not an exceptional operational failure that needs an error reason.\n\n## Modify\n\nRedesign a paper `FindScore` that returns `-1` when no record exists. List one valid negative score policy, one absence policy, and the exact caller action for each Option shape.\n\n## Make and retrieve\n\nMake a three-row comparison: empty string, empty array, and `Option::None`. Retrieve why these states cannot be collapsed into one generic “empty” value.\n\n## Failure clinic\n\n```beskid\n// Bad design: -1 may be a valid score, so this loses information.\ni32 FindScore(i32 questId) {\n    return -1;\n}\n```\n\nThe causal problem is not syntax: the signature claims every call produces an `i32`, while the implementation is encoding a second state in a value. Repair by returning `Option<i32>` for ordinary absence, or `Result<i32, QuestError>` when callers need a reason for failure.\n\n## Recap and next link\n\n`Option<T>` makes absence part of the API contract. Next, use `Result<T, E>` when a caller needs the reason an otherwise expected operation did not succeed.\n",
		"questions": [
			{
				"id": "reliability_option_q1",
				"text": "Why is Option<i32> clearer than returning -1 for a missing score?",
				"options": [
					"It separates absence from every valid i32 value",
					"It makes every missing score equal to zero"
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "reliability_result",
		"title": "Put failure in the return parcel",
		"objective": "Model expected unsuccessful work as an explicit outcome a caller can inspect.",
		"slug": "01-result-and-fallible-work",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Result keeps success and expected failure visible to callers."
		],
		"lessonPath": "/site/learn/curriculum/05-reliability-and-abstraction/01-result-and-fallible-work/lesson.md",
		"difficulty": "intermediate",
		"category": "reliability-and-abstraction",
		"prerequisites": [
			"structure_tests"
		],
		"detailedContent": "\n## Hook and goal\n\nModel expected unsuccessful work as an explicit outcome a caller can inspect. Result keeps success and expected failure visible to callers.\n\n## Predict\n\nPredict what a caller can do with typed error data that a sentinel number cannot support.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Normative-pending corelib sketch: a caller receives an outcome, not -1.\nCore.Results.Result<i32, QuestError> FindScore(i32 questId) {\n  return Results.Failure<i32, QuestError>(QuestError::Missing(questId));\n}\n```\n\n## Investigate\n\n`Result<T, E>` represents success carrying T or an expected failure carrying E. It differs from a compiler diagnostic, which rejects source before execution.\n\n## Modify\n\nReplace a `-1` failure convention with named success and error outcomes.\n\n## Make and retrieve\n\nMake a caller question: what can recover, and which facts are needed? Retrieve the answer.\n\n## Failure clinic\n\n```beskid\n// Bad design: -1 can be both a score and a missing-value sentinel.\ni32 FindScore(i32 questId) {\n  return -1;\n}\n```\n\nDo not label every broken invariant as recoverable work; preserve the distinction.\n\n## Recap and next link\n\nResult makes ordinary failure inspectable. Next: give error cases names.\n",
		"questions": [
			{
				"id": "reliability_result_q1",
				"text": "Predict what a caller can do with typed error data that a sentinel number cannot support.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "reliability_errors",
		"title": "Name the ways a plan can fail",
		"objective": "Use error variants and payloads to make recovery decisions explicit.",
		"slug": "02-errors-as-data",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Variants represent distinct outcomes; payloads preserve useful facts."
		],
		"lessonPath": "/site/learn/curriculum/05-reliability-and-abstraction/02-errors-as-data/lesson.md",
		"difficulty": "intermediate",
		"category": "reliability-and-abstraction",
		"prerequisites": [
			"reliability_result"
		],
		"detailedContent": "\n## Hook and goal\n\nUse error variants and payloads to make recovery decisions explicit. Variants represent distinct outcomes; payloads preserve useful facts.\n\n## Predict\n\nPredict whether one vague message or distinct closed/cancelled cases offers better recovery.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\nenum QuestError {\n  Missing(i32 questId),\n  Cancelled(i64 reason),\n}\n```\n\n## Investigate\n\nEnums model a finite set of error shapes. Match handling makes each caller confront those shapes and their payloads.\n\n## Modify\n\nTurn DeliveryFailed into closed, cancelled, and invalid-address cases with one useful payload.\n\n## Make and retrieve\n\nMake a recovery table: variant, fact, next action. Retrieve why strings are weak protocols.\n\n## Failure clinic\n\n```beskid\n// Bad design: callers cannot choose a repair from one undifferentiated case.\nenum QuestError { Failed }\n```\n\nDo not discard every variant with a generic ignore branch.\n\n## Recap and next link\n\nErrors are domain data. Next: reuse behavior with generic contracts.\n",
		"questions": [
			{
				"id": "reliability_errors_q1",
				"text": "Predict whether one vague message or distinct closed/cancelled cases offers better recovery.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "reliability_generics",
		"title": "Reuse the rule, not the accident",
		"objective": "Explain how type parameters make reusable code vary only where its contract allows.",
		"slug": "03-generic-reusable-code",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"A generic varies a declared type role rather than weakening all checking."
		],
		"lessonPath": "/site/learn/curriculum/05-reliability-and-abstraction/03-generic-reusable-code/lesson.md",
		"difficulty": "intermediate",
		"category": "reliability-and-abstraction",
		"prerequisites": [
			"reliability_errors"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain how type parameters make reusable code vary only where its contract allows. A generic varies a declared type role rather than weakening all checking.\n\n## Predict\n\nPredict why an ordering helper needs a comparison capability instead of assuming numbers.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\ntype Box<T> {\n  T value,\n}\n```\n\n## Investigate\n\nGeneric arguments fill named type parameters. Arity is checked at use sites; generic code remains constrained by its stated operations.\n\n## Modify\n\nDescribe a Cache<T>: which operations are independent of T and which require more capability?\n\n## Make and retrieve\n\nMake the sentence “generic code varies ___.” Retrieve its missing word.\n\n## Failure clinic\n\n```beskid\n// Bad use: Box declares one type parameter, not two.\nBox<i32, string> score;\n```\n\nDo not add a generic just to hide an unclear API.\n\n## Recap and next link\n\nGenerics make variation explicit. Next: state behavioral conformance.\n",
		"questions": [
			{
				"id": "reliability_generics_q1",
				"text": "Predict why an ordering helper needs a comparison capability instead of assuming numbers.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "reliability_contracts",
		"title": "Promise behavior, not resemblance",
		"objective": "Use contracts to declare the capabilities a type offers to callers.",
		"slug": "04-contracts-and-conformance",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"A contract is an explicit capability promise checked by conformance."
		],
		"lessonPath": "/site/learn/curriculum/05-reliability-and-abstraction/04-contracts-and-conformance/lesson.md",
		"difficulty": "intermediate",
		"category": "reliability-and-abstraction",
		"prerequisites": [
			"reliability_generics"
		],
		"detailedContent": "\n## Hook and goal\n\nUse contracts to declare the capabilities a type offers to callers. A contract is an explicit capability promise checked by conformance.\n\n## Predict\n\nPredict why two lookalike fields do not necessarily mean two types share a supported behavior.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\ncontract QuestStore {\n  // The owning package declares callable members here.\n}\n```\n\n## Investigate\n\nContracts name behavior that callers may rely on; conformance is a checked declaration rather than a coincidence of member shapes.\n\n## Modify\n\nFor a storage capability, list operations callers need and details they must not know.\n\n## Make and retrieve\n\nMake a capability statement and retrieve why it belongs at a boundary.\n\n## Failure clinic\n\n```beskid\n// Bad boundary: consumers depend on storage instead of a contract operation.\nstore.entries;\n```\n\nDo not make a contract for a single local helper with no interchangeable provider.\n\n## Recap and next link\n\nContracts protect behavior across implementations. Next: put local behavior into callable values.\n",
		"questions": [
			{
				"id": "reliability_contracts_q1",
				"text": "Predict why two lookalike fields do not necessarily mean two types share a supported behavior.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "context_lambdas",
		"title": "Pack a small behavior",
		"objective": "Recognize a lambda as a callable value whose type must be inferable or annotated.",
		"slug": "01-lambdas",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"A lambda is callable data, not untyped code."
		],
		"lessonPath": "/site/learn/curriculum/06-functions-with-context/01-lambdas/lesson.md",
		"difficulty": "intermediate",
		"category": "functions-with-context",
		"prerequisites": [
			"reliability_contracts"
		],
		"detailedContent": "\n## Hook and goal\n\nRecognize a lambda as a callable value whose type must be inferable or annotated. A lambda is callable data, not untyped code.\n\n## Predict\n\nPredict why `value => value` is ambiguous without an expected callable type.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Normative-pending lambda syntax: parameter annotation supplies its type.\ni32 doubled = ((i32 value) => value + value)(21);\n```\n\n## Investigate\n\nNormative-pending: lambda syntax and inference require a verified compiler fixture before an interactive claim. The specification permits expression or block bodies.\n\n## Modify\n\nList input type, output type, and outside values for a proposed filter rule.\n\n## Make and retrieve\n\nMake a lambda review card and retrieve the four entries.\n\n## Failure clinic\n\n```beskid\n// E1202 when no expected callable type can infer value's parameter type.\nvalue => value\n```\n\nWhen inference fails, add type information at the boundary that owns the decision.\n\n## Recap and next link\n\nLambdas package typed local behavior. Next: inspect what they capture.\n",
		"questions": [
			{
				"id": "context_lambdas_q1",
				"text": "Predict why `value => value` is ambiguous without an expected callable type.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "context_capture",
		"title": "Know what the closure carries",
		"objective": "Explain capture, definite assignment, and lifetime boundaries for closures.",
		"slug": "02-capture-and-lifetime-boundaries",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Captured storage must remain valid for the closure’s life."
		],
		"lessonPath": "/site/learn/curriculum/06-functions-with-context/02-capture-and-lifetime-boundaries/lesson.md",
		"difficulty": "intermediate",
		"category": "functions-with-context",
		"prerequisites": [
			"context_lambdas"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain capture, definite assignment, and lifetime boundaries for closures. Captured storage must remain valid for the closure’s life.\n\n## Predict\n\nPredict why a local that has not been assigned cannot provide dependable captured data.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\ni32 bonus = 5;\n// bonus is captured; score is a parameter.\n(i32 score) => score + bonus\n```\n\n## Investigate\n\nNormative-pending: capture diagnostics and lifetime behavior need compiler evidence. Captured locals must be definitely assigned; mutable capture may be rejected.\n\n## Modify\n\nSeparate a discount closure’s parameters from its immutable shop-policy capture.\n\n## Make and retrieve\n\nMake a capture audit: value, owner, mutability, required lifetime. Retrieve it.\n\n## Failure clinic\n\n```beskid\n// Bad: bonus is not definitely assigned before the closure captures it.\ni32 bonus;\n(i32 score) => score + bonus\n```\n\nDo not fix a capture complaint by smuggling state into a global.\n\n## Recap and next link\n\nClosures carry valid luggage. Next: choose an appropriate behavior boundary.\n",
		"questions": [
			{
				"id": "context_capture_q1",
				"text": "Predict why a local that has not been assigned cannot provide dependable captured data.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "context_composition",
		"title": "Choose the right behavior boundary",
		"objective": "Choose a named function, lambda, or contract-backed collaborator based on reuse and ownership.",
		"slug": "03-composing-behaviour",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Named, local, and supplied behavior solve different design needs."
		],
		"lessonPath": "/site/learn/curriculum/06-functions-with-context/03-composing-behaviour/lesson.md",
		"difficulty": "intermediate",
		"category": "functions-with-context",
		"prerequisites": [
			"context_capture"
		],
		"detailedContent": "\n## Hook and goal\n\nChoose a named function, lambda, or contract-backed collaborator based on reuse and ownership. Named, local, and supplied behavior solve different design needs.\n\n## Predict\n\nPredict which form serves a second caller better: inline rule, named function, or contract-backed service.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\ni32 ClampScore(i32 score) {\n  if score < 0 { return 0; }\n  return score;\n}\n```\n\n## Investigate\n\nA named function suits stable reuse, a lambda suits concise local behavior, and a contract-backed collaborator suits interchangeable implementations or managed lifetimes.\n\n## Modify\n\nRefactor a quest-title formatter through all three forms and compare what each reveals.\n\n## Make and retrieve\n\nMake a three-question selector and retrieve the matching form.\n\n## Failure clinic\n\n```beskid\n// Bad boundary: a hidden global makes the rule's dependency untestable.\ni32 Score() { return GlobalRules.Clamp(0); }\n```\n\nDo not turn every callback into a contract or every function into a lambda.\n\n## Recap and next link\n\nBehavior boundaries should be honest and small. Next: start cooperative work with explicit handles.\n",
		"questions": [
			{
				"id": "context_composition_q1",
				"text": "Predict which form serves a second caller better: inline rule, named function, or contract-backed service.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "fibers_model",
		"title": "Many tasks, explicit handoffs",
		"objective": "Explain cooperative fibers without making timing or run-order promises.",
		"slug": "01-cooperative-concurrency-model",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Fiber correctness coordinates observable facts, never a presumed schedule."
		],
		"lessonPath": "/site/learn/curriculum/07-fibers-and-channels/01-cooperative-concurrency-model/lesson.md",
		"difficulty": "intermediate",
		"category": "fibers-and-channels",
		"prerequisites": [
			"context_composition",
			"corelib_discovery"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain cooperative fibers without making timing or run-order promises. Fiber correctness coordinates observable facts, never a presumed schedule.\n\n## Predict\n\nPredict why “the child will finish first” is not a valid rule after one convenient run.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Normative-pending: this creates a handle, not a promised execution order.\nFiber<i32> worker = spawn DoWork(42);\n```\n\n## Investigate\n\nNormative-pending: this curriculum has no CLI proof of scheduling behavior. A fiber is cooperative work with explicit communication and observation boundaries.\n\n## Modify\n\nReplace a fixed print-order assumption with a message or completed outcome.\n\n## Make and retrieve\n\nMake the rule “coordinate facts, not timing guesses,” then retrieve one explicit fact.\n\n## Failure clinic\n\n```beskid\n// Bad requirement: logs do not establish a portable scheduling contract.\n// Expect WorkerLog before MainLog\n```\n\nIf a design works only under one observed order, add a result, channel, or coordination condition.\n\n## Recap and next link\n\nFibers need explicit handoffs. Next: spawn a child and inspect its handle.\n",
		"questions": [
			{
				"id": "fibers_model_q1",
				"text": "Predict why “the child will finish first” is not a valid rule after one convenient run.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "fibers_spawn",
		"title": "Start work; keep the receipt",
		"objective": "Explain that spawn returns Fiber<T>, not the child value T.",
		"slug": "02-spawn-and-fiber-handles",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"spawn CallableReturning<T> produces a Fiber<T> handle."
		],
		"lessonPath": "/site/learn/curriculum/07-fibers-and-channels/02-spawn-and-fiber-handles/lesson.md",
		"difficulty": "intermediate",
		"category": "fibers-and-channels",
		"prerequisites": [
			"fibers_model",
			"corelib_api_docs"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain that spawn returns Fiber<T>, not the child value T. spawn CallableReturning<T> produces a Fiber<T> handle.\n\n## Predict\n\nPredict the handle type when a callable returns i32.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Normative-pending API sketch: T comes from DoWork's result.\ni32 DoWork(i32 input) { return input + 1; }\nFiber<i32> worker = spawn DoWork(41);\n```\n\n## Investigate\n\nNormative-pending: target compatibility and spawn lowering require active runtime proof. The handle, not the entry callable, exposes lifecycle operations.\n\n## Modify\n\nLabel a worker’s child return type separately from the parent’s immediate handle type.\n\n## Make and retrieve\n\nMake the spawn equation and retrieve it.\n\n## Failure clinic\n\n```beskid\n// Bad: spawn returns a Fiber<i32> handle, not an immediate i32.\ni32 worker = spawn DoWork(41);\n```\n\nDo not treat a Fiber<T> handle as its T value; choose a later observation.\n\n## Recap and next link\n\nSpawn returns a receipt for work. Next: collect its outcome with Join.\n",
		"questions": [
			{
				"id": "fibers_spawn_q1",
				"text": "Predict the handle type when a callable returns i32.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "fibers_join",
		"title": "Collect a child outcome honestly",
		"objective": "Explain Join as a fallible observation of a Fiber<T> outcome.",
		"slug": "03-join-and-fiber-results",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Join returns the entry value or FiberError through Result."
		],
		"lessonPath": "/site/learn/curriculum/07-fibers-and-channels/03-join-and-fiber-results/lesson.md",
		"difficulty": "intermediate",
		"category": "fibers-and-channels",
		"prerequisites": [
			"fibers_spawn",
			"corelib_api_docs"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain Join as a fallible observation of a Fiber<T> outcome. Join returns the entry value or FiberError through Result.\n\n## Predict\n\nPredict why a join needs an error case even when a child normally returns an integer.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Normative-pending Join shape: outcome may contain T or FiberError.\nFiber<i32> worker = spawn DoWork(41);\nCore.Results.Result<i32, FiberError> outcome = worker.Join();\n```\n\n## Investigate\n\nNormative-pending: no verified Learn execution proof exists for Join. The corelib contract specifies normal, cancelled, panicked, and stack-overflow outcomes.\n\n## Modify\n\nMake a response plan for success, cancellation, and failure.\n\n## Make and retrieve\n\nMake an outcome table and retrieve why joining an ancestor is forbidden.\n\n## Failure clinic\n\n```beskid\n// Bad: joining an ancestor is rejected because it would create a wait cycle.\nparent.Join();\n```\n\nDo not discard join errors because a local demo happened to end normally.\n\n## Recap and next link\n\nJoin observes outcomes, not a timetable. Next: manage lifecycle intentionally.\n",
		"questions": [
			{
				"id": "fibers_join_q1",
				"text": "Predict why a join needs an error case even when a child normally returns an integer.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "fibers_control",
		"title": "Manage a child’s lifecycle deliberately",
		"objective": "Distinguish Yield, Cancel, and Detach and their responsibility boundaries.",
		"slug": "04-yield-cancel-and-detach",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Yield, Cancel, and Detach are distinct lifecycle choices."
		],
		"lessonPath": "/site/learn/curriculum/07-fibers-and-channels/04-yield-cancel-and-detach/lesson.md",
		"difficulty": "intermediate",
		"category": "fibers-and-channels",
		"prerequisites": [
			"fibers_join",
			"corelib_api_docs"
		],
		"detailedContent": "\n## Hook and goal\n\nDistinguish Yield, Cancel, and Detach and their responsibility boundaries. Yield, Cancel, and Detach are distinct lifecycle choices.\n\n## Predict\n\nPredict why cancellation does not already give the caller a final child result.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Normative-pending lifecycle sketch; no next-runner claim is implied.\nworker.Cancel(7);\n// worker.Detach(); explicitly waives shutdown Join responsibility.\n```\n\n## Investigate\n\nNormative-pending: no order or latency claim is made. Cancel is observed through the handle; Detach waives shutdown-join responsibility.\n\n## Modify\n\nFor a map refresh, choose join or detach and name who owns failure observation.\n\n## Make and retrieve\n\nMake three lifecycle flashcards and retrieve the one that changes shutdown responsibility.\n\n## Failure clinic\n\n```beskid\n// Bad ownership decision: detaching only to hide an unobserved child failure.\nworker.Detach();\n```\n\nDo not detach merely to silence an inconvenient child error.\n\n## Recap and next link\n\nLifecycle control is ownership. Next: move values through a channel.\n",
		"questions": [
			{
				"id": "fibers_control_q1",
				"text": "Predict why cancellation does not already give the caller a final child result.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "fibers_channels",
		"title": "Send facts through the mountain pass",
		"objective": "Model cross-fiber payload transfer through Channel<T> outcomes.",
		"slug": "05-channels-and-message-passing",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Channels are the approved cross-fiber payload path."
		],
		"lessonPath": "/site/learn/curriculum/07-fibers-and-channels/05-channels-and-message-passing/lesson.md",
		"difficulty": "intermediate",
		"category": "fibers-and-channels",
		"prerequisites": [
			"fibers_control",
			"corelib_api_docs"
		],
		"detailedContent": "\n## Hook and goal\n\nModel cross-fiber payload transfer through Channel<T> outcomes. Channels are the approved cross-fiber payload path.\n\n## Predict\n\nPredict what a receiver may infer after observing a value from a successful Send.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Normative-pending Channel<T> sketch: messages carry values across fibers.\nChannel<QuestUpdate> updates = Channel.Create<QuestUpdate>();\n// updates.Send(update); updates.Receive();\n```\n\n## Investigate\n\nNormative-pending: channel parking and timing need runtime proof. Successful Send happens-before the Receive that observes its value; closed and cancelled results are data.\n\n## Modify\n\nDesign a QuestUpdate payload, sender, receiver, and closed-result response.\n\n## Make and retrieve\n\nMake the cross-fiber payload law and retrieve it.\n\n## Failure clinic\n\n```beskid\n// Bad boundary: a stack reference must not escape through a channel payload.\nChannel<pointer> updates = Channel.Create<pointer>();\n```\n\nDo not use an ad-hoc flag or stack reference as a mailbox.\n\n## Recap and next link\n\nChannels carry typed facts. Next: coordinate a complete quest-log design.\n",
		"questions": [
			{
				"id": "fibers_channels_q1",
				"text": "Predict what a receiver may infer after observing a value from a successful Send.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "fibers_capstone",
		"title": "Build a quest log with explicit owners",
		"objective": "Design a multi-fiber quest log with channels for payloads and coordination for invariants.",
		"slug": "06-concurrent-quest-log-capstone",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Payload transfer, state ownership, and completion each need an explicit boundary."
		],
		"lessonPath": "/site/learn/curriculum/07-fibers-and-channels/06-concurrent-quest-log-capstone/lesson.md",
		"difficulty": "intermediate",
		"category": "fibers-and-channels",
		"prerequisites": [
			"fibers_channels",
			"corelib_api_docs"
		],
		"detailedContent": "\n## Hook and goal\n\nDesign a multi-fiber quest log with channels for payloads and coordination for invariants. Payload transfer, state ownership, and completion each need an explicit boundary.\n\n## Predict\n\nPredict the bug created when several fibers edit one quest record directly.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Normative-pending design: scouts send; one owner receives and mutates.\nChannel<QuestUpdate> updates = Channel.Create<QuestUpdate>();\n// logOwner receives updates; no scout order is promised.\n```\n\n## Investigate\n\nNormative-pending capstone: review a design against the concurrency specification instead of claiming a scheduler demonstration. Mutex and WaitGroup coordinate invariants; Hub multiplexes homogeneous channels fairly.\n\n## Modify\n\nDraw scouts, Channel<QuestUpdate>, one log owner, and a completion observer.\n\n## Make and retrieve\n\nMake an ownership ledger and retrieve it before implementation.\n\n## Failure clinic\n\n```beskid\n// Bad: a mutex is not the cross-fiber payload path.\nMutex<QuestUpdate> mailbox;\n```\n\nDo not use a mutex as a mailbox or assume one ready channel always wins.\n\n## Recap and next link\n\nCorrect concurrent designs expose owners, messages, and outcomes. Next: navigate the practical core library.\n",
		"questions": [
			{
				"id": "fibers_capstone_q1",
				"text": "Predict the bug created when several fibers edit one quest record directly.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "advanced_extend",
		"title": "Extend without stealing the house",
		"objective": "Explain extend type as an external member contribution with a clear ownership boundary.",
		"slug": "01-extending-types",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"extend type adds members externally; it is not an excuse to split a coherent owner."
		],
		"lessonPath": "/site/learn/curriculum/09-advanced-language/01-extending-types/lesson.md",
		"difficulty": "intermediate",
		"category": "advanced-language",
		"prerequisites": [
			"corelib_conversions"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain extend type as an external member contribution with a clear ownership boundary. extend type adds members externally; it is not an excuse to split a coherent owner.\n\n## Predict\n\nPredict when a method belongs inline with a type versus in an external extension.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\nextend type Quest {\n  pub i32 Priority() { return 0; }\n}\n```\n\n## Investigate\n\nReference-only: verify project and module context before treating extension syntax as runnable.\n\n## Modify\n\nClassify three methods as owning-type behavior, cross-module extension, or generated contribution.\n\n## Make and retrieve\n\nMake an extension review question and retrieve it.\n\n## Failure clinic\n\n```beskid\n// Bad external extension: private owning storage is not its API.\nquest.privateScore;\n```\n\nDo not use extensions to hide an unclear module dependency.\n\n## Recap and next link\n\nExtensions contribute deliberately. Next: attach metadata without changing runtime logic.\n",
		"questions": [
			{
				"id": "advanced_extend_q1",
				"text": "Predict when a method belongs inline with a type versus in an external extension.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "advanced_attributes",
		"title": "Attach a label with consequences",
		"objective": "Explain attributes as declared metadata whose meaning belongs to a consuming tool or rule.",
		"slug": "02-attributes-and-annotations",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"An attribute is metadata; its effect must be specified by a consumer."
		],
		"lessonPath": "/site/learn/curriculum/09-advanced-language/02-attributes-and-annotations/lesson.md",
		"difficulty": "intermediate",
		"category": "advanced-language",
		"prerequisites": [
			"advanced_extend"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain attributes as declared metadata whose meaning belongs to a consuming tool or rule. An attribute is metadata; its effect must be specified by a consumer.\n\n## Predict\n\nPredict why an unknown annotation should not be assumed to change a program.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n[Trace]\ni32 Score() { return 0; }\n```\n\n## Investigate\n\nReference-only: attribute consumers and exact accepted arguments require feature-specific verification.\n\n## Modify\n\nFor a proposed attribute, name its target, arguments, consumer, and observable effect.\n\n## Make and retrieve\n\nMake an annotation contract card and retrieve it.\n\n## Failure clinic\n\n```beskid\n// Bad: unknown/invalid attribute target has no declared consumer.\n[Trace]\nlet score = 0;\n```\n\nDo not add decorative metadata with no specified reader.\n\n## Recap and next link\n\nAttributes are contracts with tools. Next: write documentation comments that attach correctly.\n",
		"questions": [
			{
				"id": "advanced_attributes_q1",
				"text": "Predict why an unknown annotation should not be assumed to change a program.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "advanced_docs",
		"title": "Leave a map for the next traveler",
		"objective": "Use documentation comments as attached item documentation, not ordinary narration.",
		"slug": "03-documentation-comments",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Exactly /// begins an item documentation run; // is an ordinary comment."
		],
		"lessonPath": "/site/learn/curriculum/09-advanced-language/03-documentation-comments/lesson.md",
		"difficulty": "intermediate",
		"category": "advanced-language",
		"prerequisites": [
			"advanced_attributes"
		],
		"detailedContent": "\n## Hook and goal\n\nUse documentation comments as attached item documentation, not ordinary narration. Exactly /// begins an item documentation run; // is an ordinary comment.\n\n## Predict\n\nPredict whether //// attaches documentation under the lexical rule.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n/// Returns the visible score for a completed quest.\ni32 Score() { return 0; }\n```\n\n## Investigate\n\nReference-only: comment attachment needs a parser/documentation fixture before UI validation.\n\n## Modify\n\nWrite one item’s purpose, inputs, output, failure behavior, and ownership note.\n\n## Make and retrieve\n\nMake a documentation checklist and retrieve the slash-count rule.\n\n## Failure clinic\n\n```beskid\n//// This does not form an attached documentation run.\ni32 Score() { return 0; }\n```\n\nDo not document what code already states while omitting why the API exists.\n\n## Recap and next link\n\nDocs preserve intent at the declaration. Next: inspect macro boundaries.\n",
		"questions": [
			{
				"id": "advanced_docs_q1",
				"text": "Predict whether //// attaches documentation under the lexical rule.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "advanced_macros",
		"title": "Generate structure, keep the source honest",
		"objective": "Explain macro definitions and invocations as syntax-level generation with explicit expansion boundaries.",
		"slug": "04-macros",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Macros generate structured syntax; they do not waive later semantic checking."
		],
		"lessonPath": "/site/learn/curriculum/09-advanced-language/04-macros/lesson.md",
		"difficulty": "intermediate",
		"category": "advanced-language",
		"prerequisites": [
			"advanced_docs"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain macro definitions and invocations as syntax-level generation with explicit expansion boundaries. Macros generate structured syntax; they do not waive later semantic checking.\n\n## Predict\n\nPredict whether a macro expansion can make an invalid type relationship valid by hiding it.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\nmacro Repeat(expression value) {\n  // Host-driven expansion contributes structured syntax.\n}\n```\n\n## Investigate\n\nReference-only: macro host support and invocation syntax require verified fixtures.\n\n## Modify\n\nList the repetition that a macro would remove and the readable source it must still preserve.\n\n## Make and retrieve\n\nMake a macro justification: repeated structure, input shape, expansion owner, diagnostics plan.\n\n## Failure clinic\n\n```beskid\n// Bad use: macro expansion cannot hide an invalid return relationship.\ni32 Score() { return \"high\"; }\n```\n\nDo not use a macro to conceal ordinary control flow or an unclear API.\n\n## Recap and next link\n\nMacros reduce structural repetition. Next: learn where compiler mods fit.\n",
		"questions": [
			{
				"id": "advanced_macros_q1",
				"text": "Predict whether a macro expansion can make an invalid type relationship valid by hiding it.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "advanced_mods",
		"title": "Let tools contribute under a contract",
		"objective": "Explain compiler mods as constrained tooling contributions rather than hidden source mutation.",
		"slug": "05-mods-and-conditional-compilation",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Mods are governed contributions with declared capabilities and diagnostics."
		],
		"lessonPath": "/site/learn/curriculum/09-advanced-language/05-mods-and-conditional-compilation/lesson.md",
		"difficulty": "intermediate",
		"category": "advanced-language",
		"prerequisites": [
			"advanced_macros"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain compiler mods as constrained tooling contributions rather than hidden source mutation. Mods are governed contributions with declared capabilities and diagnostics.\n\n## Predict\n\nPredict why generated code still needs the same semantic checks as handwritten code.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Reference-only compiler-mod flow:\n// source -> declared capability -> typed AST contribution -> analysis\n```\n\n## Investigate\n\nReference-only: mod host execution needs a project artifact and capability setup.\n\n## Modify\n\nDraw input source, mod capability, generated contribution, analysis, and diagnostics.\n\n## Make and retrieve\n\nMake a mod review ledger and retrieve who owns each generated declaration.\n\n## Failure clinic\n\n```beskid\n// Bad mod behavior: emitting untyped formatted text bypasses the host contract.\nmacro GeneratedText() { }\n```\n\nDo not allow an unconstrained generator to bypass language or project rules.\n\n## Recap and next link\n\nMods extend tooling under contracts. Next: make serialization boundaries explicit.\n",
		"questions": [
			{
				"id": "advanced_mods_q1",
				"text": "Predict why generated code still needs the same semantic checks as handwritten code.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "advanced_serialization",
		"title": "Turn values into portable records carefully",
		"objective": "Treat serialization as an explicit contract between data shape, version, and error handling.",
		"slug": "06-serialization",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Serialization preserves a stated representation, not every implementation detail."
		],
		"lessonPath": "/site/learn/curriculum/09-advanced-language/06-serialization/lesson.md",
		"difficulty": "intermediate",
		"category": "advanced-language",
		"prerequisites": [
			"advanced_mods"
		],
		"detailedContent": "\n## Hook and goal\n\nTreat serialization as an explicit contract between data shape, version, and error handling. Serialization preserves a stated representation, not every implementation detail.\n\n## Predict\n\nPredict what breaks when a stored field changes without a version or compatibility rule.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Reference-only serialization design: the Mod owns [Serialize] generation.\n[Serialize]\ntype QuestRecord { string title, i32 score }\n```\n\n## Investigate\n\nReference-only: serialization APIs and generated forms require verified feature support.\n\n## Modify\n\nDefine one record’s stable fields, absent-field policy, version, and malformed-input outcome.\n\n## Make and retrieve\n\nMake a serialization contract card and retrieve it.\n\n## Failure clinic\n\n```beskid\n// Bad compatibility policy: stored shape changes without a version/migration rule.\ntype QuestRecord { i32 points }\n```\n\nDo not serialize private layout merely because it is convenient today.\n\n## Recap and next link\n\nPortable data needs a versioned agreement. Next: cross an FFI boundary cautiously.\n",
		"questions": [
			{
				"id": "advanced_serialization_q1",
				"text": "Predict what breaks when a stored field changes without a version or compatibility rule.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "advanced_ffi",
		"title": "Cross the border with a manifest",
		"objective": "Explain FFI as a declared ABI boundary with explicit types, ownership, and linking rules.",
		"slug": "07-ffi-boundaries",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Foreign calls need an ABI contract, not a guessed signature."
		],
		"lessonPath": "/site/learn/curriculum/09-advanced-language/07-ffi-boundaries/lesson.md",
		"difficulty": "intermediate",
		"category": "advanced-language",
		"prerequisites": [
			"advanced_serialization"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain FFI as a declared ABI boundary with explicit types, ownership, and linking rules. Foreign calls need an ABI contract, not a guessed signature.\n\n## Predict\n\nPredict why matching a function name is insufficient evidence that a foreign call is safe.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Reference boundary record: ABI C, symbol quest_score, i32 -> i32.\nextern i32 quest_score(i32 questId);\n```\n\n## Investigate\n\nReference-only: FFI requires project link metadata and target-specific verification.\n\n## Modify\n\nFor a foreign function, record ABI, symbol, parameter representation, result representation, ownership, and failure mode.\n\n## Make and retrieve\n\nMake an FFI border card and retrieve every field.\n\n## Failure clinic\n\n```beskid\n// Bad FFI assumption: a name alone does not establish pointer ownership or ABI.\nextern pointer quest_score(pointer questId);\n```\n\nDo not call unmanifested symbols or assume platform layouts coincide.\n\n## Recap and next link\n\nFFI is a strict boundary. Next: understand the memory rules that keep all boundaries safe.\n",
		"questions": [
			{
				"id": "advanced_ffi_q1",
				"text": "Predict why matching a function name is insufficient evidence that a foreign call is safe.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	},
	{
		"id": "advanced_memory",
		"title": "Keep references where they can live",
		"objective": "Explain the memory/reference model and why boundaries reject unsafe escaping references.",
		"slug": "08-memory-and-ownership",
		"starterCode": "// Reference-only lesson package. The curriculum tool requires a safe placeholder.\\ni32 Main() {\\n  return 0;\\n}\\n",
		"command": "reference",
		"mode": "reference-only",
		"hints": [
			"Memory safety depends on ownership, lifetime, and safe cross-boundary representation."
		],
		"lessonPath": "/site/learn/curriculum/09-advanced-language/08-memory-and-ownership/lesson.md",
		"difficulty": "intermediate",
		"category": "advanced-language",
		"prerequisites": [
			"advanced_ffi"
		],
		"detailedContent": "\n## Hook and goal\n\nExplain the memory/reference model and why boundaries reject unsafe escaping references. Memory safety depends on ownership, lifetime, and safe cross-boundary representation.\n\n## Predict\n\nPredict why a stack reference cannot simply be carried into independently scheduled work.\n\n## Run\n\nReference-only: this capability has no lesson-specific compiler proof in the current Learn runtime. Read the cited specification and do not present this package as an interactive acceptance exercise.\n\n```beskid\n// Boundary audit: update ownership transfers through a typed channel.\nChannel<QuestUpdate> updates = Channel.Create<QuestUpdate>();\n```\n\n## Investigate\n\nReference-only: memory diagnostics require target/compiler evidence; the normative model remains the guide.\n\n## Modify\n\nFor a value crossing an API or fiber boundary, identify owner, lifetime, representation, and mutation rule.\n\n## Make and retrieve\n\nMake a boundary memory audit and retrieve it.\n\n## Failure clinic\n\n```beskid\n// Bad: a short-lived stack reference cannot escape into independent work.\nspawn () => localScore;\n```\n\nDo not infer safety from a program that happened not to fail in one run.\n\n## Recap and next link\n\nOwnership makes long-lived systems explainable. You can now revisit fibers, APIs, and FFI with a complete boundary vocabulary.\n",
		"questions": [
			{
				"id": "advanced_memory_q1",
				"text": "Predict why a stack reference cannot simply be carried into independently scheduled work.",
				"options": [
					"It follows the stated contract.",
					"It is a timing or implementation guess."
				],
				"correctIndex": 0
			}
		]
	}
] satisfies ReadonlyArray<LearnExercise>;
