# 05) Run a Program

Goal: combine local bindings and arithmetic in a complete program.

- This lesson is compile-backed for deployment stability.
- Your code should return `0` only when the branch condition succeeds.
