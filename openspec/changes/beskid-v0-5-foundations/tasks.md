## 1. Validate the normative baseline

- [ ] 1.1 Run `openspec validate beskid-v0-5-foundations --strict --no-interactive` and correct every change-layout or scenario error before implementation.
- [ ] 1.2 Preserve the acceptance matrix in `design.md`; each stable requirement ID MUST keep one named parser, semantic, runtime, corelib, JIT, AOT, or native proof target.

## 2. Introduce the language and corelib contracts

- [ ] 2.1 Introduce the bindable `spawn` grammar and AST form in `compiler/crates/beskid_analysis/src/beskid.pest` and `compiler/crates/beskid_analysis/src/syntax/expressions/spawn_expression.rs`; add parser snapshots for `Fiber<i64> f = spawn Compute();` and a captured spawn block.
- [ ] 2.2 Introduce spawn type inference, ignored-handle diagnostics, transferable-capture checks, and terminal-handle diagnostics in `compiler/crates/beskid_analysis/src/types/checker/spawn.rs`, `compiler/crates/beskid_analysis/src/types/checker/statements.rs`, and `compiler/crates/beskid_analysis/src/types/result.rs`.
- [ ] 2.3 Extend the existing `Fiber<T>`, `FiberError`, `Channel<T>`, `ChannelError`, and `ChannelOptions` contracts in `compiler/corelib/packages/concurrency/src/Concurrency/` without changing the established `Detach() -> unit`, `Cancel() -> unit`, closed payload-bearing `FiberError`, or channel-option surfaces. Preserve `Cancelled(i64 reason, i64 cancelerId)`, `StackOverflow(i64 limitBytes, i64 requestedBytes)`, and `Panicked(i64 code, string message)`; enforce child-panic code `2_i64` as a value invariant while retaining the message and all recorded terminal diagnostics.
- [ ] 2.4 Introduce `use` grammar and scoped-binding AST nodes, then require `Disposable.Dispose() -> Result<unit, DisposeError>`, lexical non-escape diagnostics, a `Result<T, E>` enclosing callable, and exactly one explicit cleanup conversion from `DisposeError` to `E`; add missing/ambiguous-conversion and non-`Result` diagnostics.
- [ ] 2.5 Introduce `Core.Disposable`, `Core.IO.Reader`, `Writer`, `Closer`, `Stream`, and `IoError` in `compiler/corelib/packages/foundation/src/Core/`; implement `ReadExact`, `WriteAll`, EOF, no-progress, invalid-range validation order, zero-length bypass, idempotent close, and the explicit `DisposeError -> IoError::CloseFailed` cleanup conversion.
- [ ] 2.6 Introduce overlap-safe `Core.Bytes.Copy`, checked reader/writer cursors, strict Utf8/Hex/Base64 validators, the HTTP ASCII helper, and the `ReadBytesWith -> Result<u8[], SyscallError>` declaration correction.
- [ ] 2.7 Implement the proposed Core.Time delta (`BSP-REQ-D613601481B2`, `8851DD840BC2`, `CB2C00464517`, `75347DC0B10F`): `Sleep(Duration) -> Result<unit, TimerError>` in `compiler/corelib/packages/foundation/src/Core/Time/Time.bd` and exactly four payload-free TimerError variants in `Core/Time/TimerError.bd`. Preserve validation order, checked monotonic addition, zero-duration cancellation observation, closed runtime status validation, scheduler-fiber admission, and sticky cancellation. Defer `SleepUntil(Instant)` until domain identity is enforceable; add no public handle or alternate timer/IO API.
- [ ] 2.8 Implement the `BSP-REQ-F25A4DF4DEA0` transfer-cause ruling (`docs/superpowers/specs/2026-09-22-networking-v05-rulings.md`, item 1): add the closed payload-free `Core.IO.TransferFailure` enum in `compiler/corelib/packages/foundation/src/Core/IO/TransferFailure.bd` and its `pub mod` line in `IO.bd`; change `IoError::ReadFailed` and `WriteFailed` in `Core/IO/IoError.bd` to carry exactly one `TransferFailure cause`; return `TransferFailure::Unspecified()` from the two `Read`/`Write` invariant sites in `IO.bd`; keep `CloseFailed()` payload-free and `FromDisposeError` unchanged. Confirm first that the front end accepts an enum-typed payload in a corelib package enum.

- [ ] 2.9 Implement `BSP-REQ-35580A7D7B75` (`DeadCollectionGrowth`): add a generation-bound `dead_collection_growth` semantic fact in `compiler/crates/beskid_queries/src/semantic_contract/` that fires only for a discarded canonical growth call whose owner is a `mut T[]` parameter with no publishing read in the enclosing body; gate `build_typed_program` on it so lowering fails closed with the source site. Add `beskid_queries` tests for the firing shape and for the non-firing corelib shapes (returned parameter as in `Http/Codec.bd` `AppendText`/`AppendCrlf`, local owner, aggregate-field owner, bound or returned result). Confirm `HttpSerializationTests` and `HttpCodecTests` still compile and pass.

## 3. Migrate runtime ownership and completion paths

- [ ] 3.1 Replace scalar fiber result and capture transport through generation-safe semantic facts, generated ISLE rules, `compiler/runtime_manifest.bsol`, and canonical Beskid runtime modules under `compiler/runtime/beskid/src/Runtime/` with the canonical typed ABI-value descriptor and GC-rooted slots.
- [ ] 3.2 Implement one traced ABI-value channel representation in canonical Beskid runtime modules under `compiler/runtime/beskid/src/Runtime/`; expose only the trusted manifest intrinsics needed for barriers and parking, with no Rust runtime or scalar fallback.
- [ ] 3.3 Route external completions through scheduler IDs, inbound commands, and the owner wake primitive in canonical Beskid runtime modules; target adapters expose only manifest-authorized platform operations and MUST NOT create a second scheduler implementation.
- [ ] 3.4 Verify and complete monotonic timer registration/cancellation and the shared atomic winner transition in the existing canonical runtime scheduler state. The F4 research records this internal implementation at compiler revision `615c1cae`; preserve its owner routing, generation tokens, heap, accounting, and register/park/release path. Change `External.bd` only if a focused failing acceptance case establishes a defect.
- [ ] 3.5 Lower scoped `use` cleanup through semantic facts and generated ISLE rules on normal completion, `return`, postfix `?`, and supported nested exits in reverse declaration order.
- [ ] 3.6 Add one `__timer_sleep_until` soft builtin in `compiler/runtime_manifest.bsol` bound to existing `beskid_rt_v5_external_sleep_until(i64) -> usize`. Preserve the generated analysis/ABI authority and word-sized result; run `cargo test -p beskid_manifest` and `cargo test -p beskid_abi --test runtime_bootstrap_contract` from the compiler checkout.

## 4. Delete superseded paths

- [ ] 4.1 Delete the scalar-only fiber and channel result paths after the generic ABI-value tests pass.
- [ ] 4.2 Delete TLS-local external completion delivery after owner-routed wake tests pass.
- [ ] 4.3 Preserve ordinary `use Package.Module;` imports, delete only the ambiguous fallback that interprets `use Type name = expression;` as an import, and prohibit a `using` compatibility alias after parser and semantic migration pass.
- [ ] 4.4 Delete independent partial-transfer loops in foundation-level stream APIs after `Core.IO` is the public contract.

## 5. Verify Foundation evidence

- [ ] 5.1 Run `cargo test -p beskid_tests analysis::spawn`, generation-safe query tests, generated ISLE/codegen suites, and canonical runtime conformance fixtures from `compiler/`.
- [ ] 5.2 Run focused JIT, AOT, and native runtime-kit fixtures for aggregate fiber results, `Channel<OwnedResource>` using a Foundation-local opaque disposable fixture, owner wake, timer cancellation, scoped `use`, bytes, encoding, and Core.IO.
- [ ] 5.3 Run `just corelib`, `just compiler`, and the available native runtime-kit scheduler smoke from `compiler/`.
- [ ] 5.4 Run `openspec validate beskid-v0-5-foundations --strict --no-interactive` and `bun run openspec:validate`; reserve release-wide catalog regeneration and publication for the final HTTP/release change.
- [ ] 5.5 Compile Foundation-local examples for spawn/join, aggregate fiber results, generic value/resource channels, owner-routed completion, timers, scoped `use`, bytes/encoding, and Core.IO through analysis, JIT, AOT, and every available native kit; do not depend on Networking or HTTP types.
- [ ] 5.6 Start F4 with failing public API coverage in `compiler/crates/beskid_engine/tests/fixtures/timer_value_transfer.bd` and `source_timer_sleep_has_typed_outcomes_and_owned_registration` in `compiler/crates/beskid_engine/tests/fiber_value_transfer.rs`. Reuse the actual Core.Time closure plus TimerError and the existing JIT/static/shared execution helper. Require failure on the missing API rather than an unrelated import; then close every F4-S case from `design.md` by running `cargo test -p beskid_engine --test fiber_value_transfer source_timer_sleep_has_typed_outcomes_and_owned_registration -- --nocapture` and the full `fiber_value_transfer` harness.
- [ ] 5.7 Extend `compiler/crates/beskid_engine/tests/fixtures/external_wait.c` through `external_wait_native.rs` with F4-R full legal shared-capacity admission using real handles, independent duplicate-registration rejection, full slot reuse, stale cancellation, and timer-winner integrity. Preserve deterministic heap advancement and existing shutdown/race evidence. Run `cargo test -p beskid_engine --test external_wait_native -- --nocapture` and `cargo test -p beskid_abi --test external_owner_transport -- --nocapture`; do not use C-ABI callback JIT coverage as public source evidence.
- [ ] 5.8 Extend `compiler/corelib/beskid_corelib/tests/corelib_tests/src/system/TimeTests.bd` and `compiler/corelib/beskid_corelib/docs/Core/Time.md` with F4-C typed results and an explicit spawn/Join Sleep example. Run `cargo test -p beskid_tests_projects system_time_tests_front_end_typechecks -- --ignored --nocapture --test-threads=1` and `cargo test -p beskid_tests_projects corelib_tests_front_end_typechecks_matrix -- --nocapture --test-threads=1` from the compiler checkout. These are typechecking gates; task 5.11 owns executable clock-failure, checked-addition boundary, and impossible-status proof.
- [ ] 5.9 Close F4-W by porting `compiler/crates/beskid_engine/tests/{fiber_value_transfer,external_wait_native}.rs`, `compiler/crates/beskid_engine/tests/fixtures/{fiber_value_transfer,timer_validation,external_wait}.c`, and `compiler/crates/beskid_abi/tests/{external_owner_transport.rs,fixtures/external_owner_transport.c}` build/load/thread/alarm mechanics, including F4-V's native callback and fatal-child protocol. Run the same F4-S/F4-V/F4-R/owner-transport Cargo commands on `x86_64-pc-windows-msvc` with nonzero test counts; `cfg(unix)`-excluded zero-test binaries are not acceptance.
- [ ] 5.10 Preserve macOS/Linux/Windows logs with revision, target, same-revision native kit identity, executed case counts, and actual source JIT/static/shared results. Record missing tools or kits as unavailable cells; keep F7 target acceptance open until all required cells execute. Perform the installation-affecting `just corelib` / `just replace` gate only as the explicit final integration decision, not during F4 documentation or focused acceptance.
- [ ] 5.11 Implement the F4-V production-validation seam: private `SleepDeadlineFromSample` and `SleepResultFromStatus` in `compiler/corelib/packages/foundation/src/Core/Time/Time.bd` remain the sole validation implementations called by public Sleep. In `compiler/crates/beskid_engine/tests/fiber_value_transfer.rs`, assemble the real unchanged Core.Time dependency closure; select canonical `Sleep`, production `FromNanoseconds`, `SleepDeadlineFromSample`, and `SleepResultFromStatus` by genuine syntax item identity; and require `direct_callees(Sleep)` plus `reachable_items(Sleep)` to contain the exact selected helpers. Lower their real reachable closure through `CodegenInput`, `SyntaxModuleItem`, and `lower_syntax_program`. Add `compiler/crates/beskid_engine/tests/fixtures/timer_validation.c` as the one native callback/launcher driver: Engine supplies real function pointers; static/shared artifacts selectively export the same emitted functions. Obtain item signatures and specialized nominal layouts from semantic facts, register managed roots across allocating calls, and fail before invocation on signature/layout mismatch. Exercise the exact negative-clock, checked-addition boundary/overflow, valid-status, and impossible-status cases in `design.md` through Engine JIT, static native, and shared native. Isolate invariant failures in child processes and require both the intended diagnostic and unsuccessful termination; retain real runtime exports and the existing single timer path. Do not append or splice source, copy validation into Rust or C, expose a public helper, hard-code result offsets, or add a clock/status override. Run `cargo test -p beskid_engine --test fiber_value_transfer source_timer_sleep_deadline_validation_uses_production_helpers -- --exact --nocapture` and `cargo test -p beskid_engine --test fiber_value_transfer source_timer_sleep_invalid_runtime_status_fails_closed -- --exact --nocapture`. Include both tests, the native driver, and subprocess mechanics in F4-W portability and nonzero-case acceptance.
- [ ] 5.12 Verify `BSP-REQ-F61E094A4838` payload compatibility and `BSP-REQ-8CE166C9D003` terminal results using the existing `corelib/beskid_corelib/tests/corelib_tests/src/concurrency/FiberHandleTests.bd`, `crates/beskid_codegen/tests/isle_adapter/enum_match_result.rs`, and `crates/beskid_engine/tests/{fiber_value_transfer.rs,fixtures/fiber_value_transfer.bd}` evidence targets in the compiler checkout. Exercise constructors and exhaustive two-field matches for all three variants, nonzero cancellation and stack-overflow detail transfer, and exact child-panic code/message preservation after consumed storage is released or reused. Use the existing runtime fixture seam for terminal details that public no-argument `Cancel()` cannot supply; do not add a public overload or an alternate join/error path. Run source execution through the existing JIT/static/shared helper and record absent cases as open, not as passes inferred from typechecking or C-ABI status tests.
- [ ] 5.13 Run a second acceptance pass over the `BSP-REQ-F25A4DF4DEA0` and `BSP-REQ-0A5892A2DB9C` Core.IO cells of the `design.md` evidence matrix after task 2.8 lands. The transfer-cause ruling reopens the accepted Core.IO contract: every `IoError::ReadFailed`/`WriteFailed` constructor and pattern gains a `TransferFailure` payload, so the contract-resolution fixture, Core.IO and Disposable corelib tests, IO JIT/AOT fixtures, and IO smoke MUST be re-executed and re-recorded; earlier green cells for these two requirements are not acceptance evidence. Add cause-propagation coverage for `ReadExact`/`WriteAll` and the `PeerReset` versus `Ok(0)` scenario using a Foundation-local fixture reader; do not depend on Networking types.

## 6. Growable managed heap (`BSP-REQ-88A15BB6918A`, `BSP-REQ-539C3C627D46`, `BSP-REQ-8C1FA4687232`)

Design authority: `docs/superpowers/specs/2026-09-22-growable-gc-heap-design.md`. Paths are relative to `compiler/`. Every item that edits `runtime/beskid/src/**`, `runtime_manifest.bsol`, or `crates/beskid_abi/assembly/**` requires a runtime kit rebuild before its native evidence runs.

- [ ] 6.1 Rewrite the `BeskidHeapState` layout in `runtime_manifest.bsol` to the 816-byte table in the design (region chain fields at 0..32, policy fields at 72..104, 64-byte diagnostic at 104, external roots at 176, handles at 688) and add the 64-byte `BeskidHeapRegion` layout; regenerate the ABI artifacts through `crates/beskid_abi/build.rs`. Proof: `crates/beskid_abi/tests/runtime_bootstrap_contract.rs` asserts both layouts field by field.
- [ ] 6.2 Add a `crates/beskid_abi/src/runtime_source/tests.rs` case that parses every `const HEAP_*` and `REGION_*` in `runtime/beskid/src/Runtime/Mem/Gc/*.bd` and `runtime/beskid/src/Runtime/Bootstrap/Lifecycle.bd` and asserts equality with the manifest layouts. It must fail on the current 720-versus-768 drift before 6.3 lands.
- [ ] 6.3 Update the `HEAP_*` constants in `State.bd`, `Allocation.bd`, `Collection.bd`, `Sweep.bd`, `RootsHandles.bd`, and `Lifecycle.bd`; add `REGION_*` constants and region-header accessors to `State.bd`; change `HeapInit` to `(heapStruct, region, regionSize, capBytes)` writing the region header. Proof: 6.2 passes; `control_flow_closures_spawn.rs` fixture updated to a headed region and 816-byte state still passes.
- [ ] 6.4 Convert `HeapContains`, `IsAllocatedObject`, `ResetMarkedObjects`, `ProcessGrayStack`, and `Sweep` to the region-chain iteration; `Sweep` resets coalescing at region boundaries and rebuilds `free_bytes` and `largest_free` per region. Proof: new `isle_adapter` JIT test with a 4 KiB first region that allocates across three regions, validates earlier addresses, collects, and checks `largest_free`.
- [ ] 6.5 Make `AllocateFromFreeList` per region with the `largest_free` skip and make `GcAlloc` bump only in `current_region`; implement `GrowHeap` with the pacing, doubling, dedicated-region, and cap rules; set `collection_threshold` after each collection in `GcCollect`. Proof: `isle_adapter` pacing test (garbage loop keeps one region) and free-list skip test.
- [ ] 6.6 Add exports `beskid_rt_v5_heap_set_cap`, `gc_heap_cap`, `gc_heap_committed`, `gc_heap_region_count`, `gc_heap_failure_reason` to `Collection.bd` and `runtime_manifest.bsol` (exports and corelib services `__gc_heap_*`). Proof: `crates/beskid_codegen/tests/isle_adapter/corelib_services.rs` import assertions and `crates/beskid_abi/tests/abi_v5_contract.rs` export list.
- [ ] 6.7 Implement exhaustion reporting in `GcAlloc`: store `failure_reason` and `failure_request_bytes`, render the decimal diagnostic into the heap-state buffer through a shared `WriteDecimal` helper in `State.bd`, and call `Trap(5, message, length)`. Proof: `heap_growth_native.rs` cap-hit fixture (exit 101, stderr contains `out_of_memory (5)` and `cap=2097152`).
- [ ] 6.8 Render `BESKID_TRAP_NAME_<CODE>` defines and a `BESKID_TRAP_NAME(code)` lookup in `crates/beskid_manifest/src/v5/render.rs::render_c_header`; print `<name> (<code>): <message>` in `beskid_rt_v5_intrinsic_trap` in all three `platform_host.c` files. Proof: `beskid_manifest` header render test; existing `traced_abi_value_native.rs` and `external_wait_native.rs` stderr assertions stay green.
- [ ] 6.9 Read `BESKID_HEAP_MAX_BYTES` in `crates/beskid_abi/assembly/common/executable_bootstrap.c` after `process_init`, parse decimal with `K`/`M`/`G`, apply through `beskid_rt_v5_heap_set_cap`, and route failures through `beskid_rt_v5_trap(5, ...)`. Proof: `heap_growth_native.rs` cap-configuration cases (`abc` and `512K` exit 101; `2M` is observable through `gc_heap_cap`).
- [ ] 6.10 Walk and free the region chain in `ProcessShutdown` and keep every `ProcessInit` early-exit path freeing the initial region. Proof: `crates/beskid_engine/tests/abi_v5_jit_runtime_kit.rs` and the runtime-kit lifecycle tests pass under the native kit; no leak reported by the shutdown trap checks.
- [ ] 6.11 Add `crates/beskid_engine/tests/heap_growth_native.rs` with `fixtures/heap_growth.bd` covering growth past 1 MiB, garbage-loop pacing, huge-object dedicated region, cap hit, and cap configuration in JIT, static AOT, and native kit, following `fiber_value_transfer.rs`.
- [ ] 6.12 Run `just corelib` and confirm `TextRegexIntegrationTests.bd` and `PestGrammarParseTests.bd` pass without the 1 MiB ceiling; record the run in the design evidence matrix.
