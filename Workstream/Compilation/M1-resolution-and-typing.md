# M1: Name resolution and typing

Goals
- Close gaps in resolution and typing relative to spec (contracts, generics, lambdas)

Tasks
- Validate contract embedding + method order/signatures across inheritance
- Try operator typing rules (Option<T>); diagnostics for misuse
- Lambda type inference from expected types; capture non-escape diagnostics

Exit criteria
- New unit tests for contracts, try operator typing, and lambda inference

